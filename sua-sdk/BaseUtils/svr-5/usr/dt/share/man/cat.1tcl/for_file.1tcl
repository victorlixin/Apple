

for_file(1tcl)                                     for_file(1tcl)

SSyynnooppssiiss
       for_file var filename { code }

DDeessccrriippttiioonn
       This  procedure  implements  a loop over the contents of a
       file.  For each line in filename, it sets var to the  line
       and executes code.

       The break and continue commands work as with foreach.

EExxaammpplleess
       The  command  for_file line /etc/passwd {echo $line} would
       echo all the lines in the password file.

                                                                1

