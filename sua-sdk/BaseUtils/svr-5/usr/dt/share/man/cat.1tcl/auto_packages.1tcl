

auto_packages(1tcl)                           auto_packages(1tcl)

SSyynnooppssiiss
       auto_packages [-location]

DDeessccrriippttiioonn
       Returns  a  list  of the names of all defined packages. If
       -location is specified, auto_packages returns a list  con-
       taining  pairs  of values. The first value in each pair is
       the package name; the second is  its  corresponding  .tlib
       pathname, offset, and length (within the .tlib file).

                                                                1

