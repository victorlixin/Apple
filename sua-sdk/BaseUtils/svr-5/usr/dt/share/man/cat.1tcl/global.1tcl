

global(1tcl)                                         global(1tcl)

SSyynnooppssiiss
       global varname [varname ...]

DDeessccrriippttiioonn
       This  command  is  ignored unless a Tcl procedure is being
       interpreted. If so then it declares the given varnames  to
       be  global variables rather than local ones. For the dura-
       tion of the current procedure (and only while executing in
       the  current  procedure), any reference to any of the var-
       names will refer to the global variable by the same  name.

                                                                1

