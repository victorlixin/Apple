

lvarcat(1tcl)                                       lvarcat(1tcl)

SSyynnooppssiiss
       lvarcat var string [string ...]

DDeessccrriippttiioonn
       This  command  treats  each  string argument as a list and
       concatenates them to the end of the contents of var, form-
       ing a a single list.  The list is stored back into var and
       also returned as the result.  If var does not exist, it is
       created.

                                                                1

