

chroot(1tcl)                                         chroot(1tcl)

SSyynnooppssiiss
       chroot dirname

DDeessccrriippttiioonn
       Changes  the  root  directory  to dirname, by invoking the
       POSIX system call.  This command only succeeds if  running
       as root.

                                                                1

