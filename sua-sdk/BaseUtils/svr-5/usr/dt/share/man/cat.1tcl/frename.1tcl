

frename(1tcl)                                       frename(1tcl)

SSyynnooppssiiss
       frename oldPath newPath

DDeessccrriippttiioonn
       Renames oldPath to newPath.  This command does not support
       renaming across filesystems unless the rename system  call
       supports them.  If renaming across filesystems is desired,
       exec the command.

                                                                1

