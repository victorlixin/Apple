

unlink(1tcl)                                         unlink(1tcl)

SSyynnooppssiiss
       unlink [-nocomplain] filelist

DDeessccrriippttiioonn
       Deletes  (unlinks)  the  files whose names are in the list
       filelist.  If -nocomplain is specified,  then  errors  are
       ignored.

                                                                1

