

mkdir(1tcl)                                           mkdir(1tcl)

SSyynnooppssiiss
       mkdir [-path] dirList

DDeessccrriippttiioonn
       Creates  each of the directories in the list dirList.  The
       mode on the new directories is 777, modified by the umask.
       If -path is specified, then any non-existent parent direc-
       tories in the specified path(s) are also created.

                                                                1

