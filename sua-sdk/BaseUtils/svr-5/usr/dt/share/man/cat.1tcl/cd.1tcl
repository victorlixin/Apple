

cd(1tcl)                                                 cd(1tcl)

SSyynnooppssiiss
       cd [dirName]

DDeessccrriippttiioonn
       Changes  the  current  working directory to dirName, or to
       the home directory (as specified in the  HOME  environment
       variable)  if dirName is not given. If dirName starts with
       a tilde(~), then tilde-expansion is done as described  for
       Tcl_TildeSubst. Returns an empty string.

                                                                1

