

pushd(1tcl)                                           pushd(1tcl)

SSyynnooppssiiss
       pushd [dir]

DDeessccrriippttiioonn
       This  procedure  pushes  the  current  directory  onto the
       directory stack and cd's to the specified  directory.   If
       the directory is not specified, then the current directory
       is pushed, but remains unchanged.

                                                                1

