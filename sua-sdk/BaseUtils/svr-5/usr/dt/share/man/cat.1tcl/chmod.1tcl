

chmod(1tcl)                                           chmod(1tcl)

SSyynnooppssiiss
       chmod mode filelist

DDeessccrriippttiioonn
       Sets permissions of each of the files in the list filelist
       to mode, where mode is an absolute numeric  mode  or  sym-
       bolic permissions as in the command.  To specify a mode as
       octal, it should be prefixed with a 0 (for example, 0622).

                                                                1

