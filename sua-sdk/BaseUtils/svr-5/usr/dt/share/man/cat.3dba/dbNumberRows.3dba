

dbNumberRows(3dba)                             dbNumberRows(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbNumberRows(DBresult *result);

DDeessccrriippttiioonn
       dbNumberRows returns the number of records either inserted
       or  deleted  from  a database using This function does not
       return the number of records returned from an  SQL  select
       using dbQuery.

   AArrgguummeennttss
       (Input)  A pointer to a DBresult structure returned from a
       previous call to

RReettuurrnn vvaalluueess
       If successful, dbNumberRows returns the number of  records
       returned by the previous query.  Otherwise, it returns -1.

DDiiaaggnnoossttiiccss
       An application  can  call  to  obtain  the  error  message
       returned from the database.

RReeffeerreenncceess

                                                                1

