

dbPrivStr(3dba)                                   dbPrivStr(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       char *dbPrivsStr(unsigned int privs);

DDeessccrriippttiioonn
       dbPrivStr returns a printable string based on a  privilege
       bitmask.  Use  this  routine  to print out privileges in a
       human readable form.  This can be used in conjunction with
       the and routines to print out user privileges.

   AArrgguummeennttss
       (Input) A privilege bitmask such as the one passed to

RReettuurrnn vvaalluueess
       On success dbGetUserPriv returns a printable string repre-
       senting the privilege bitmask. On failure it returns NULL.

RReeffeerreenncceess

                                                                1

