

dbCheckTable(3dba)                             dbCheckTable(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbCheckTable(DBConn *conn, char *table);

DDeessccrriippttiioonn
       Use dbcheckTable to determine if a table exists in a given
       database.  Before you can use this routine, you must first
       connect to the database using and then pass  the  returned
       connection handle to dbCheckTable.

   AArrgguummeennttss
       (Input)  A  database  connection  that was returned from a
       previous call to dbConnect.  (Input) The name of the table
       to check for.

RReettuurrnn vvaalluueess
       Returns  0 if table does not exist, a value greater than 0
       if the table does exists,  or  -1  if  an  internal  error
       occured.

RReeffeerreenncceess

                                                                1

