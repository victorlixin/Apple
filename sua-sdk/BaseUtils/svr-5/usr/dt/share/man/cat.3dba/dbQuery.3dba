

dbQuery(3dba)                                       dbQuery(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       DBResult *dbQuery(      DBConn *conn,      char *query);

DDeessccrriippttiioonn
       dbQuery sends an SQL query to the database.  Because  each
       database supports its own unique method for delimiting SQL
       queries, dbQuery adds the  appropriate  delimiter  to  the
       query. Callers of this function should supply only the SQL
       query.

   AArrgguummeennttss
       (Input) A connection to a database from a previous call to
       (Input) The SQL query to be sent to the database.

RReettuurrnn vvaalluueess
       If the query is successful, dbQuery returns a pointer to a
       DBResult structure.  Otherwise, dbQuery returns NULL.

DDiiaaggnnoossttiiccss
       An application  can  call  to  obtain  the  error  message
       returned from the database.

RReeffeerreenncceess

                                                                1

