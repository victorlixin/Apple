

dbInitialize(3dba)                             dbInitialize(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbInitialize();

DDeessccrriippttiioonn
       dbInitialize is called once in the  lifetime  of  an  EELS
       system   to   initialize   the   administrative   database
       (EELS_Admin) that is used to hold the Database Table which
       stores  information about the databases and tables used by
       EELS.

   AArrgguummeennttss
       None.

RReettuurrnn vvaalluueess
       If successful,  dbInitialize  returns  0.   Otherwise,  it
       returns -1.

DDiiaaggnnoossttiiccss
       An  application  can  call  to  obtain  the  error message
       returned from the database.

RReeffeerreenncceess

                                                                1

