

dbGetMaxUniqEventID(3dba)               dbGetMaxUniqEventID(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       double dbGetMaxUniqEventID(void);

DDeessccrriippttiioonn
       dbGetMaxUniqEventID returns the maximum Uniq Event ID from
       the EELS_UniqEventID table in the EELS_Admin database.

RReettuurrnn vvaalluueess
       dbGetMaxUniqEventID  returns  the maximum Uniq Event ID on
       success, otherwise it returns -1.

RReeffeerreenncceess

                                                                1

