

dbDeleteTable(3dba)                           dbDeleteTable(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbDeleteTable(      DBConn *conn,      char *name);

DDeessccrriippttiioonn
       dbDeleteTable removes the named table  from  the  database
       described  by conn and the entry associated with it in the
       EELS_Database table of the EELS_Admin database.

   AArrgguummeennttss
       (Input) A database connection that was returned by a  pre-
       vious call to (Input) The name of the table to be deleted.

RReettuurrnn vvaalluueess
       If successful, dbDeleteTable  returns  0.   Otherwise,  it
       returns -1.

DDiiaaggnnoossttiiccss
       An  application  can  call  to  obtain  the  error message
       returned from the database.

RReeffeerreenncceess

                                                                1

