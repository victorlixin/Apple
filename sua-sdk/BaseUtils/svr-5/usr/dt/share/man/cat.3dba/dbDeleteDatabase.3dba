

dbDeleteDatabase(3dba)                     dbDeleteDatabase(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbDeleteDatabase(char *name);

DDeessccrriippttiioonn
       dbDeleteDatabase removes the named database and purges the
       entries    associated   with   the   database   from   the
       EELS_Database table  in  the  EELS_Admin  data  base.  The
       database must have been created by a prior call to

       An  error  is  returned  if  the specified database is not
       known to EELS.

   AArrgguummeennttss
       (Input) The name of the database to be deleted.

RReettuurrnn vvaalluueess
       dbDeleteDatabase returns 0 if  successful.  Otherwise,  it
       returns -1.

DDiiaaggnnoossttiiccss
       An  application  can  call  to  obtain  the  error message
       returned from the database.

RReeffeerreenncceess

                                                                1

