

dbDeleteUserPriv(3dba)                     dbDeleteUserPriv(3dba)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -kthread -leels [library] ...
       #include <eelsdba.h>

       int dbDeleteUserPriv( char *host,      char *user );

DDeessccrriippttiioonn
       dbDeleteUserPriv deletes user privileges added by

       The word any can be used as a wildcard  for  the  host  or
       user  field. For example, if any is specified for the user
       field, then all privileges are  deleted  for  every  user.
       This  is  not advisable because if you delete all database
       privileges then no one (not even root) will be able to add
       privileges.

   AArrgguummeennttss
       (Input)  IP/Hostname  from which the user is connecting to
       the database (Input) The name of the user that  is  having
       its privileges removed.

RReettuurrnn vvaalluueess
       dbDeleteUserPriv  returns  0 if successful.  Otherwise, it
       returns -1.

RReeffeerreenncceess

                                                                1

