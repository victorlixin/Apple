

string(3bsd)                                         string(3bsd)

SSyynnooppssiiss
       /usr/ucb/cc [flag . . . ] file . . .

       int strcasecmp(char *s1, char *s2);

       int strncasecmp(char *s1, char *s2, int n);

DDeessccrriippttiioonn
       The   strcasecmp  and  strncasecmp  routines  compare  the
       strings and ignore differences in  case.   These  routines
       assume  the  ASCII  character  set when equating lower and
       upper case characters.

       These functions operate on null-terminated strings.   They
       do not check for overflow of any receiving string.

RReeffeerreenncceess
NNoottiicceess
       strcasecmp and strncasecmp use native character comparison
       as above and assume the ASCII character set.

                     BSD System Compatibility                   1

