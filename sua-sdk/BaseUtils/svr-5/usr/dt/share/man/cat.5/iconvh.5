

iconvh(5)                                               iconvh(5)

SSyynnooppssiiss
       #include <iconv.h>

DDeessccrriippttiioonn
       The <iconv.h> header defines the following data type using
       typedef: Identifies the conversion from one  code  set  to
       another.

       The  following  are  declared  functions  and  may also be
       declared  as  macros:  iconv_t  iconv_open   (const   char
       *tocode, const char *fromcode);

       size_t    iconv   (iconv_t   cd,   char   **inbuf,  size_t
       *inbytesleft,
                      char **outbuf, size_t *outbytesleft);

       int     iconv_close (iconv_t cd);

RReeffeerreenncceess

                                                                1

