

menu_cursor(3ocurses)                       menu_cursor(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lmenu -locurses [library . . .]

       #include <menu.h>

       int pos_menu_cursor(MENU *menu);

DDeessccrriippttiioonn
       pos_menu_cursor  moves the cursor in the window of menu to
       the correct position to resume menu processing.   This  is
       needed  after  the  application calls a curses library I/O
       routine.

 vvaalluuee
       This routine returns one of the following:
       ----------------------------------------------------------------------
       E_OK             -   The routine returned successfully.
       E_SYSTEM_ERROR   -   System error.
       E_BAD_ARGUMENT   -   An incorrect argument was passed to the routine.
       E_NOT_POSTED     -   The menu has not been posted.

NNoottiicceess
       The header file menu.h automatically includes  the  header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

