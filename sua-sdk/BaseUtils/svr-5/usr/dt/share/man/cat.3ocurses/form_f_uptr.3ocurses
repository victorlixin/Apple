

form_field_userptr(3ocurses)         form_field_userptr(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lform -locurses [library . . .]

       #include <form.h>

       int   set_field_userptr(FIELD  *field,  char  *ptr);  char
       *field_userptr(FIELD *field);

DDeessccrriippttiioonn
       Every field has an associated user  pointer  that  can  be
       used  to store pertinent data.  set_field_userptr sets the
       user pointer of field.   field_userptr  returns  the  user
       pointer of field.

 vvaalluuee
       field_userptr  returns  NULL  on error.  set_field_userptr
       returns one of the following:
       ---------------------------------------------------------
       E_OK             -   The function returned successfully.
       E_SYSTEM_ERROR   -   System error.

NNoottiicceess
       The header file form.h automatically includes  the  header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

