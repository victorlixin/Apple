

menu_pattern(3ocurses)                     menu_pattern(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lmenu -locurses [library . . .]

       #include <menu.h>

       int   set_menu_pattern(MENU   *menu,   char   *pat);  char
       *menu_pattern(MENU *menu);

DDeessccrriippttiioonn
       Every menu has a pattern buffer to match entered data with
       menu  items.   set_menu_pattern sets the pattern buffer to
       pat and tries to find the first item that matches the pat-
       tern.   If  it does, the matching item becomes the current
       item.   If  not,  the  current  item  does   not   change.
       menu_pattern  returns  the string in the pattern buffer of
       menu.

 vvaalluuee
       menu_pattern  returns  NULL  on  error.   set_menu_pattern
       returns one of the following:
       ----------------------------------------------------------------------
       E_OK             -   The routine returned successfully.
       E_SYSTEM_ERROR   -   System error.
       E_BAD_ARGUMENT   -   An incorrect argument was passed to the routine.
       E_NO_MATCH       -   The character failed to match.

NNoottiicceess
       The  header  file menu.h automatically includes the header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

