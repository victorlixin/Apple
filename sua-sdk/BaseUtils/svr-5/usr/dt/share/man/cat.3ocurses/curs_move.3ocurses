

curs_move(3ocurses)                           curs_move(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -locurses [library . . .]

       #include <ocurses.h>

       int  move(int y, int x); int wmove(WINDOW *win, int y, int
       x);

DDeessccrriippttiioonn
       With these routines, the cursor associated with the window
       is  moved  to  line y and column x.  This routine does not
       move the physical cursor of the terminal until refresh  is
       called.   The  position specified is relative to the upper
       left-hand corner of the window, which is (0,0).

   RReettuurrnn vvaalluueess
       These routines return the integer ERR upon failure and  an
       integer value other than ERR upon successful completion.

RReeffeerreenncceess
NNoottiicceess
       The  header  file  ocurses.h  automatically  includes  the
       header files stdio.h and unctrl.h.

       Note that move may be a macro.

                                                                1

