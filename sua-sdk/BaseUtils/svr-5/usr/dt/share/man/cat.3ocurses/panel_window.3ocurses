

panel_window(3ocurses)                     panel_window(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lpanel -locurses [library . . .]

       #include <panel.h>

       WINDOW        *panel_window(PANEL       *panel);       int
       replace_panel(PANEL *panel, WINDOW *win);

DDeessccrriippttiioonn
       panel_window returns a pointer to the window of panel.

       replace_panel replaces the current window  of  panel  with
       win.

 vvaalluuee
       panel_window returns NULL on failure.

       replace_panel  returns  OK  on  successful completion, ERR
       otherwise.

NNoottiicceess
       The header file panel.h automatically includes the  header
       file ocurses.h.

RReeffeerreenncceess

                                                                1

