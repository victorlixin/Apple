

form_opts(3ocurses)                           form_opts(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lform -locurses [library . . .]

       #include <form.h>

       int   set_form_opts(FORM   *form,   OPTIONS   opts);   int
       form_opts_on(FORM    *form,     OPTIONS     opts);     int
       form_opts_off(FORM    *form,    OPTIONS   opts);   OPTIONS
       form_opts(FORM *form);

DDeessccrriippttiioonn
       set_form_opts turns on the  named  options  for  form  and
       turns off all remaining options.  Options are boolean val-
       ues which can be OR-ed together.

       form_opts_on turns on the named options; no other  options
       are changed.

       form_opts_off  turns  off  the  named  options;  no  other
       options are changed.

       form_opts returns the options set for form.
       -----------------------------------------------------------------------
       Form Options:

              _OO____NN_LL____OO_VV_EE_RR_LL_OO_AA_DD     Overload the REQ_NEW_LINE form driver request.
       _OO____BB_SS____OO_VV_EE_RR_LL_OO_AA_DD     Overload the REQ_DEL_PREV form driver request.

 vvaalluuee
       set_form_opts, form_opts_on and form_opts_off  return  one
       of the following:
       ---------------------------------------------------------
       E_OK             -   The function returned successfully.
       E_SYSTEM_ERROR   -   System error.

NNoottiicceess
       The  header  file form.h automatically includes the header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

