

form_new_page(3ocurses)                   form_new_page(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lform -locurses [library . . .]

       #include <form.h>

       int    set_new_page(FIELD    *field,    int   bool);   int
       new_page(FIELD *field);

DDeessccrriippttiioonn
       set_new_page marks field as the beginning of a new page on
       the form.

       new_page returns a boolean value indicating whether or not
       field begins a new page of the form.

 vvaalluuee
       new_page returns TRUE or FALSE.

       set_new_page returns one of the following:
       ---------------------------------------------------------------
       E_OK             -   The function returned successfully.
       E_CONNECTED      -   The field is already connected to a form.
       E_SYSTEM_ERROR   -   System error.

NNoottiicceess
       The header file form.h automatically includes  the  header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

