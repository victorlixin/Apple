

curs_printw(3ocurses)                       curs_printw(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -locurses [library . . .]

       #include <ocurses.h>

       int  printw(char  *fmt  [, arg] . . .); int wprintw(WINDOW
       *win, char *fmt [, arg] . . .); int mvprintw(int y, int x,
       char  *fmt  [, arg] . . .); int mvwprintw(WINDOW *win, int
       y, int x, char *fmt [, arg] . . .);

       #include <varargs.h>

       int vwprintw(WINDOW *win, char *fmt, va_list varglist);

DDeessccrriippttiioonn
       The printw, wprintw, mvprintw and mvwprintw  routines  are
       analogous  to printf [see In effect, the string that would
       be output by printf is output instead  as  though  waddstr
       was used on the given window.

       The vwprintw routine is analogous to vprintf [see and per-
       forms a wprintw using a variable argument list.  The third
       argument  is  a va_list, a pointer to a list of arguments,
       as defined in varargs.h.

   RReettuurrnn vvaalluueess
       All routines return the integer ERR upon  failure  and  an
       integer value other than ERR upon successful completion.

RReeffeerreenncceess
NNoottiicceess
       The  header  file  ocurses.h  automatically  includes  the
       header files stdio.h and unctrl.h.

                                                                1

