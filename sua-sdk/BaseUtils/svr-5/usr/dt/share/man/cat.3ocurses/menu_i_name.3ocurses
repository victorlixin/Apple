

menu_item_name(3ocurses)                 menu_item_name(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lmenu -locurses [library . . .]

       #include <menu.h>

       char  *item_name(ITEM  *item); char *item_description(ITEM
       *item);

DDeessccrriippttiioonn
       item_name returns a pointer to the name of item.

       item_description returns a pointer to the  description  of
       item.

 vvaalluuee
       These routines return NULL on error.

NNoottiicceess
       The  header  file menu.h automatically includes the header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

