

panel_above(3ocurses)                       panel_above(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lpanel -locurses [library . . .]

       #include <panel.h>

       PANEL *panel_above(PANEL *panel); PANEL *panel_below(PANEL
       *panel);

DDeessccrriippttiioonn
       panel_above returns a pointer  to  the  panel  just  above
       panel,  or  NULL  if  panel is the top panel.  panel_below
       returns a pointer to the panel just below panel,  or  NULL
       if panel is the bottom panel.

       If NULL is passed for panel, panel_above returns a pointer
       to the bottom panel in the deck, and panel_below returns a
       pointer to the top panel in the deck.

 vvaalluuee
       NULL is returned if an error occurs.

NNoottiicceess
       These  routines  allow  traversal of the deck of currently
       visible panels.

       The header file panel.h automatically includes the  header
       file ocurses.h.

RReeffeerreenncceess

                                                                1

