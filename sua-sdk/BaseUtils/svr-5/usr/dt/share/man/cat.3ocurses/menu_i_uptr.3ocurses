

menu_item_userptr(3ocurses)           menu_item_userptr(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lmenu -locurses [library . . .]

       #include <menu.h>

       int  set_item_userptr(ITEM  *item,  char  *userptr);  char
       *item_userptr(ITEM *item);

DDeessccrriippttiioonn
       Every item has an associated user pointer that can be used
       to  store relevant information.  set_item_userptr sets the
       user pointer  of  item.   item_userptr  returns  the  user
       pointer of item.

 vvaalluuee
       item_userptr  returns  NULL  on  error.   set_item_userptr
       returns one of the following:
       --------------------------------------------------------
       E_OK             -   The routine returned successfully.
       E_SYSTEM_ERROR   -   System error.

NNoottiicceess
       The header file menu.h automatically includes  the  header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

