

form_cursor(3ocurses)                       form_cursor(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lform -locurses [library . . .]

       #include <form.h>

       int pos_form_cursor(FORM *form);

DDeessccrriippttiioonn
       pos_form_cursor  moves the form window cursor to the loca-
       tion required by the form driver to resume  form  process-
       ing.   This  may  be  needed after the application calls a
       curses library I/O routine.

 vvaalluuee
       pos_form_cursor returns one of the following:
       -----------------------------------------------------
       E_OK             The function returned successfully.
       E_SYSTEM_ERROR   System error.
       E_BAD_ARGUMENT   An argument is incorrect.
       E_NOT_POSTED     The form is not posted.

NNoottiicceess
       The header file form.h automatically includes  the  header
       files eti.h and ocurses.h.

RReeffeerreenncceess

                                                                1

