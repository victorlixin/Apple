

panel_new(3ocurses)                           panel_new(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lpanel -locurses [library . . .]

       #include <panel.h>

       PANEL   *new_panel(WINDOW   *win);   int   del_panel(PANEL
       *panel);

DDeessccrriippttiioonn
       new_panel creates a new  panel  associated  with  win  and
       returns the panel pointer.  The new panel is placed on top
       of the panel deck.

       del_panel destroys panel, but not its associated window.

 vvaalluuee
       new_panel returns NULL if an error occurs.

       del_win returns OK if successful, ERR otherwise.

NNoottiicceess
       The header file panel.h automatically includes the  header
       file ocurses.h.

RReeffeerreenncceess

                                                                1

