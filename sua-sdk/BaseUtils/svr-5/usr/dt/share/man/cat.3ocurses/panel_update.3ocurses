

panel_update(3ocurses)                     panel_update(3ocurses)

SSyynnooppssiiss
       cc [flag . . .] file -lpanel -locurses [library . . .]

       #include <panel.h>

       void update_panels(void);

DDeessccrriippttiioonn
       update_panels  refreshes the virtual screen to reflect the
       depth relationships between the panels in the  deck.   The
       user  must  use  the  curses library call doupdate [see to
       refresh the physical screen.

NNoottiicceess
       The header file panel.h automatically includes the  header
       file ocurses.h.

RReeffeerreenncceess

                                                                1

