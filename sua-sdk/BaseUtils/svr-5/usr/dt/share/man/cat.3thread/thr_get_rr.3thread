

thr_get_rr_interval(3thread)         thr_get_rr_interval(3thread)

SSyynnooppssiiss
       cc [options] -Kthread file

       #include <thread.h>

       void thr_get_rr_interval(timestruc_t *rr_time);

DDeessccrriippttiioonn
       thr_get_rr_interval stores the round-robin scheduling time
       quantum used by the  Threads  Library  implementation  for
       threads  using  the  SCHED_RR  scheduling  policy  in  the
       timestruc_t pointed to by rr_time.

   PPaarraammeetteerrss
       pointer   to   the    timestruc_t    object    in    which
       thr_get_rr_interval  will  place  the  value of the round-
       robin scheduling interval (set by thr_get_rr_interval)

       timestruc_t is defined in  time.h  as  follows:  struct  {
            time_t    tv_sec;        /*         seconds        */
            long tv_nsec;  /* nanoseconds */ } timestruc_t;

   SSeeccuurriittyy rreessttrriiccttiioonnss
       thr_get_rr_interval requires  no  special  permissions  or
       privilege.

RReettuurrnn vvaalluueess
       thr_get_rr_interval does not return a value.

   EErrrroorrss
       None

RReeffeerreenncceess

                                                                1

