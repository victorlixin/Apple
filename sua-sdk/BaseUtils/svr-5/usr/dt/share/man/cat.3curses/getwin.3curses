

getwin(3curses)                                   getwin(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       WINDOW *getwin(FILE *filep);

       int putwin(WINDOW *win, FILE *filep);

DDeessccrriippttiioonn
       The  function reads window-related data stored in the file
       by The function then creates and initialises a new  window
       using that data.

       The  function writes all data associated with win into the
       stdio stream to which filep points.  This information  can
       be retrieved later using

RReettuurrnn vvaalluuee
       Upon  successful completion, returns a pointer to the win-
       dow it created.  Otherwise, it returns a null pointer.

       Upon successful completion,  returns  OK.   Otherwise,  it
       returns ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

