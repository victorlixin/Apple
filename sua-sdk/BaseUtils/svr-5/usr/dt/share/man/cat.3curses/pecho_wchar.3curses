

pechochar(3curses)                             pechochar(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int pechochar(WINDOW *pad, chtype ch);

       int pecho_wchar(WINDOW *pad, const cchar_t *wch);

DDeessccrriippttiioonn
       The  and functions output one character to a pad and imme-
       diately refresh the pad.  They are equivalent to a call to
       or  respectively,  followed by a call to The last location
       of the pad on the screen is reused for the arguments to

RReettuurrnn vvaalluuee
       Upon successful completion,  these  functions  return  OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       The  function  is  only  guaranteed to operate reliably on
       character sets in which each character fits into a  single
       byte,  whose  attributes  can be expressed using only con-
       stants with the A_ prefix.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

