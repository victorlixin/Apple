

delay_output(3curses)                       delay_output(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int delay_output(int ms);

DDeessccrriippttiioonn
       On  terminals that support pad characters, pauses the out-
       put for at least ms milliseconds.  Otherwise,  the  length
       of the delay is unspecified.

RReettuurrnn vvaalluuee
       Upon  successful  completion,  returns  OK.  Otherwise, it
       returns ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       Whether or not the terminal supports pad  characters,  the
       function is not a precise method of timekeeping.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

