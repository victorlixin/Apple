

in_wchnstr(3curses)                           in_wchnstr(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int in_wchnstr(cchar_t *wchstr, int n);

       int in_wchstr(cchar_t *wchstr);

       int mvin_wchnstr(int y, int x, cchar_t *wchstr, int n);

       int mvin_wchstr(int y, int x, cchar_t *wchstr);

       int mvwin_wchnstr(WINDOW *win, int y, int x, cchar_t *wch-
       str, int n);

       int mvwin_wchstr(WINDOW *win, int y, int x, cchar_t  *wch-
       str);

       int win_wchnstr(WINDOW *win, cchar_t *wchstr, int n);

       int win_wchstr(WINDOW *win, cchar_t *wchstr);

DDeessccrriippttiioonn
       These  functions  extract  characters  from the current or
       specified window, starting at  the  current  or  specified
       position and ending at the end of the line, and place them
       in the array pointed to by wchstr.

       The and fill the array with at most n cchar_t elements.

RReettuurrnn vvaalluuee
       Upon successful completion,  these  functions  return  OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       Reading a line that overflows the array pointed to by wch-
       str with or causes  undefined  results.   The  use  of  or
       respectively, is recommended.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

