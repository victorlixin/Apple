

termattrs(3curses)                             termattrs(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       chtype termattrs(void);

       attr_t term_attrs(void);

DDeessccrriippttiioonn
       The  function extracts the video attributes of the current
       terminal which is supported by the chtype data type.

       The function extracts information for the video attributes
       of the current terminal which is supported for a cchar_t

RReettuurrnn vvaalluuee
       The  function  returns  a  logical  OR of A_ values of all
       video attributes supported by the terminal.

       The function returns a logical OR of  WA_  values  of  all
       video attributes supported by the terminal.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess
       Attributes in

                                                                1

