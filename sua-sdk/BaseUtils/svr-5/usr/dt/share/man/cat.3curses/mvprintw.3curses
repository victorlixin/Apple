

mvprintw(3curses)                               mvprintw(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int mvprintw(int y, int x, char *fmt, ...);

       int mvwprintw(WINDOW *win, int y, int x, char *fmt, ...);

       int printw(char *fmt, ...);

       int wprintw(WINDOW *win, char *fmt, ...);

DDeessccrriippttiioonn
       The  and  functions  are  analogous to The effect of these
       functions is as though were used to format the string, and
       then  were  used to add that multi-byte string to the cur-
       rent or specified window at the current or specified  cur-
       sor position.

RReettuurrnn vvaalluuee
       Upon  successful  completion,  these  functions return OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

