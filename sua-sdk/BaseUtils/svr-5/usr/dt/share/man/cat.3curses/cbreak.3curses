

cbreak(3curses)                                   cbreak(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int cbreak(void);

       int nocbreak(void);

       int noraw(void);

       int raw(void);

DDeessccrriippttiioonn
       The  function sets the input mode for the current terminal
       to cbreak mode and overrides a call to

       The function sets the input mode for the current  terminal
       to  Cooked  Mode  without  changing  the state of ISIG and
       IXON.

       The function sets the input mode for the current  terminal
       to Cooked Mode and sets the ISIG and IXON flags.

       The  function sets the input mode for the current terminal
       to Raw Mode.

RReettuurrnn vvaalluuee
       Upon successful completion,  these  functions  return  OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       If  the  application is not certain what the input mode of
       the process was at the time it called it should use  these
       functions to specify the desired input mode.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

