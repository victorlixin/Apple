

clear(3curses)                                     clear(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int clear(void);

       int erase(void);

       int wclear(WINDOW *win);

       int werase(WINDOW *win);

DDeessccrriippttiioonn
       The  and  functions clear every position in the current or
       specified window.

       The and functions also achieve the same effect as  calling
       so  that the window is cleared completely on the next call
       to for the window and is redrawn in its entirety.

RReettuurrnn vvaalluuee
       Upon successful completion,  these  functions  return  OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

