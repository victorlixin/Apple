

longname(3curses)                               longname(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       char *longname(void);

DDeessccrriippttiioonn
       The  function  generates a verbose description of the cur-
       rent terminal.  The maximum length of a  verbose  descrip-
       tion  is  128 bytes.  It is defined only after the call to
       or

RReettuurrnn vvaalluuee
       Upon successful  completion,  returns  a  pointer  to  the
       description specified above.  Otherwise, it returns a null
       pointer on error.

EErrrroorrss
       No errors are defined.

UUssaaggee
       The return value of may point to a static  area  which  is
       overwritten by a subsequent call to

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

