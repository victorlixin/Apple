

noqiflush(3curses)                             noqiflush(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       void noqiflush(void);

       void qiflush(void);

DDeessccrriippttiioonn
       The function causes all output in the display driver queue
       to be flushed whenever an interrupt key  (interrupt,  sus-
       pend, or quit) is pressed.  The causes no such flushing to
       occur.  The default for the option is inherited  from  the
       display driver settings.

RReettuurrnn vvaalluuee
       These functions do not return a value.

EErrrroorrss
       No errors are defined.

UUssaaggee
       Calling provides faster response to interrupts, but causes
       Curses to have the wrong idea of what is  on  the  screen.
       The  same  effect  is  achieved  outside  Curses using the
       NOFLSH local mode flag specified on

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

