

box_set(3curses)                                 box_set(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int  box_set(WINDOW  *win,  const  cchar_t  *verch,  const
       cchar_t *horch);

DDeessccrriippttiioonn
       The function draws a border around the edges of the speci-
       fied  window.   This  function does not advance the cursor
       position.  This function does not perform special  charac-
       ter  processing.  This function does not perform wrapping.

       The function box_set(win,  verch,  horch)  has  an  effect
       equivalent to:

       wborder_set(win, verch, verch, horch, horch,
         NULL, NULL, NULL, NULL);

RReettuurrnn vvaalluuee
       Upon  successful  completion,  this  function  returns OK.
       Otherwise, it returns ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

