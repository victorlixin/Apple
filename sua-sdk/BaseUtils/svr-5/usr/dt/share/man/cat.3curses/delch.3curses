

delch(3curses)                                     delch(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int delch(void);

       int mvdelch(int y, int x);

       int mvwdelch(WINDOW *win, int y, int x);

       int wdelch(WINDOW *win);

DDeessccrriippttiioonn
       These  functions  delete  the  character at the current or
       specified position in the  current  or  specified  window.
       This function does not change the cursor position.

RReettuurrnn vvaalluuee
       Upon  successful  completion,  these  functions return OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

