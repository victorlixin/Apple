

def_prog_mode(3curses)                     def_prog_mode(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int def_prog_mode(void);

       int def_shell_mode(void);

       int reset_prog_mode(void);

       int reset_shell_mode(void);

DDeessccrriippttiioonn
       The  function  saves  the  current  terminal  modes as the
       ``program'' (in Curses) state for use by

       The function saves  the  current  terminal  modes  as  the
       ``shell'' (not in Curses) state for use by

       The  function restores the terminal to the ``program'' (in
       Curses) state.

       The function restores the terminal to the  ``shell''  (not
       in Curses) state.

       These functions affect the mode of the terminal associated
       with the current screen.

RReettuurrnn vvaalluuee
       Upon successful completion,  these  functions  return  OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       The  function  achieves  the effect of calling to save the
       prior terminal settings so they can be restored during the
       call to and of calling to specify an initial definition of
       the program terminal mode.

       Applications normally do not need to refer  to  the  shell
       terminal  mode.   Applications  may find it useful to save
       and restore the program terminal mode.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

