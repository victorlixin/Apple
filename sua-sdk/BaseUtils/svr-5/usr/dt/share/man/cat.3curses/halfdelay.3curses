

halfdelay(3curses)                             halfdelay(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int halfdelay(int tenths);

DDeessccrriippttiioonn
       The function sets the input mode for the current window to
       Half-Delay Mode and specifies tenths tenths of seconds  as
       the half-delay interval.  The tenths argument must be in a
       range from 1 up to and including 255.

RReettuurrnn vvaalluuee
       Upon successful completion,  returns  OK.   Otherwise,  it
       returns ERR.

EErrrroorrss
       No errors are defined.

UUssaaggee
       The application can call to leave Half-Delay mode.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

