

mvscanw(3curses)                                 mvscanw(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int mvscanw(int y, int x, char *fmt, ...);

       int mvwscanw(WINDOW *win, int y, int x, char *fmt, ...);

       int scanw(char *fmt, ...);

       int wscanw(WINDOW *win, char *fmt, ...);

DDeessccrriippttiioonn
       These  functions  are similar to Their effect is as though
       were called to get a multi-byte character string from  the
       current  or  specified  window at the current or specified
       cursor position, and then were used to interpret and  con-
       vert that string.

RReettuurrnn vvaalluuee
       Upon  successful  completion,  these  functions return OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

