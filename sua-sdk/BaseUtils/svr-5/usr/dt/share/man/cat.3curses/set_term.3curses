

set_term(3curses)                               set_term(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       SCREEN *set_term(SCREEN *new);

DDeessccrriippttiioonn
       The  function switches between different screens.  The new
       argument specifies the new current screen.

RReettuurrnn vvaalluuee
       Upon successful completion, returns a pointer to the  pre-
       vious screen.  Otherwise, it returns a null pointer.

EErrrroorrss
       No errors are defined.

UUssaaggee
       This  is  the only function that manipulates SCREEN point-
       ers; all other functions affect only the current screen.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

