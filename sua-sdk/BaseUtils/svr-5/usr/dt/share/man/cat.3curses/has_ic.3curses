

has_ic(3curses)                                   has_ic(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       bool has_ic(void);

       bool has_il(void);

DDeessccrriippttiioonn
       The  function  indicates  whether the terminal has insert-
       and delete-character capabilities.

       The function indicates whether the  terminal  has  insert-
       and  delete-line  capabilities, or can simulate them using
       scrolling regions.

RReettuurrnn vvaalluuee
       The function returns TRUE if the terminal has insert-  and
       delete-character   capabilities.   Otherwise,  it  returns
       FALSE.

       The function returns TRUE if the terminal has insert-  and
       delete-line capabilities.  Otherwise, it returns FALSE.

EErrrroorrss
       No errors are defined.

UUssaaggee
       The  function  may  be  used  to  determine if it would be
       appropriate to turn on physical scrolling using

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

