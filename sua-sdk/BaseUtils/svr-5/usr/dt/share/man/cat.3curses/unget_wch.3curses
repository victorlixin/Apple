

ungetch(3curses)                                 ungetch(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int ungetch(int ch);

       int unget_wch(const wchar_t wch);

DDeessccrriippttiioonn
       The  function pushes the single-byte character ch onto the
       head of the input queue.

       The function pushes the wide character wch onto  the  head
       of the input queue.

       One  character  of push-back is guaranteed.  The result of
       successive calls without an intervening  call  to  or  are
       unspecified.

RReettuurrnn vvaalluuee
       Upon  successful  completion,  these  functions return OK.
       Otherwise, they return ERR.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The Single UNIX Specification, Version 2; The Open  Group.

RReeffeerreenncceess

                                                                1

