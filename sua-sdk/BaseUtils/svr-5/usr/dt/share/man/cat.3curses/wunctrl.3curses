

wunctrl(3curses)                                 wunctrl(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       wchar_t *wunctrl(cchar_t *wc);

DDeessccrriippttiioonn
       The  function  generates a wide character string that is a
       printable representation of the wide character wc.

       This function also performs the  following  processing  on
       the  input  argument:  Control characters are converted to
       the ^X notation.  Any rendition information is removed.

RReettuurrnn vvaalluuee
       Upon successful completion, returns the generated  string.
       Otherwise, it returns a null pointer.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

