

standend(3curses)                               standend(3curses)

SSyynnooppssiiss
       cc [options] file -lcurses #include <curses.h>

       int standend(void);

       int standout(void);

       int wstandend(WINDOW *win);

       int wstandout(WINDOW *win);

DDeessccrriippttiioonn
       The  and  functions turn off all attributes of the current
       or specified window.

       The and functions turn on the standout  attribute  of  the
       current or specified window.

RReettuurrnn vvaalluuee
       These functions always return 1.

EErrrroorrss
       No errors are defined.

SSttaannddaarrddss CCoonnffoorrmmaannccee
       The  Single UNIX Specification, Version 2; The Open Group.

RReeffeerreenncceess

                                                                1

