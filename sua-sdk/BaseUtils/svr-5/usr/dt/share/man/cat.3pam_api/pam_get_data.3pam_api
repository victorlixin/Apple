

pam_get_data(3pam_api)                     pam_get_data(3pam_api)

SSyynnooppssiiss
       #include <security/pam_appl.h>

       int pam_get_data(
               pam_handle_t *pamh,
               const char *module_data_name,
               void **data);

DDeessccrriippttiioonn
       PAM  modules  use pam_get_data to extract information from
       pamh, the PAM handle, that applies to module_data_name. On
       return, data points to the information that was requested.
       Do not free the memory  associated  with  data.   When  is
       called,  it  will be released by the cleanup function that
       was previously associated with  the  PAM  handle  by  This
       function takes the following arguments: (In) The authenti-
       cation handle returned by (In) The identifier of the  mod-
       ule  data to be retrieved.  This name should be unique for
       all services.  (Out) The information retrieved.

RReettuurrnn vvaalluueess
       pam_get_data returns PAM_SUCCESS  if  successful.   Other-
       wise,  it returns one of the following error codes: Buffer
       error.  No module data were found.  System error.

                                                                1

