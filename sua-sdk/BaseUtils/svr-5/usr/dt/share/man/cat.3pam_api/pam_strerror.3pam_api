

pam_strerror(3pam_api)                     pam_strerror(3pam_api)

SSyynnooppssiiss
       #include <security/pam_appl.h>

       const    char    *pam_strerror(pam_handle_t   *pamh,   int
       error_number);

DDeessccrriippttiioonn
       pam_strerror returns an error message corresponding  to  a
       PAM  error  number.   Do  not  change or free the returned
       string.  This function takes the following arguments: (In)
       The authentication handle returned by (In) Error number.

RReettuurrnn vvaalluueess
       If  successful, pam_strerror returns a pointer to an error
       message.  Otherwise, it returns null.

                                                                1

