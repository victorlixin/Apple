

pam_getenvlist(3pam_api)                 pam_getenvlist(3pam_api)

SSyynnooppssiiss
       #include <security/pam_appl.h>

       char** pam_getenvlist (pam_handle_t *pamh);

DDeessccrriippttiioonn
       pam_getenvlist  returns a pointer to an array of the iden-
       tifiers for all PAM environment variables.   The  applica-
       tion should free the memory that the PAM library allocates
       to hold the returned value.  This function takes the  fol-
       lowing  argument:  (In) The authentication handle returned
       by

RReettuurrnn vvaalluueess
       pam_getenvlist returns a pointer to a character  array  of
       all  PAM environment variables.  It returns a null pointer
       if no values have been allocated.

                                                                1

