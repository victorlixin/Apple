

pam_end(3pam_api)                               pam_end(3pam_api)

SSyynnooppssiiss
       #include <security/pam_appl.h>

       int pam_end (pam_handle_t *pamh, int status);

DDeessccrriippttiioonn
       pam_end terminates a transaction indicated by the PAM han-
       dle, pamh.  It releases any storage that is  allocated  to
       the PAM modules, and passes status to the cleanup function
       previously associated with pamh by status is  usually  the
       value  returned  by the immediately previous call to a PAM
       function.

       This function takes  the  following  arguments:  (In)  The
       authentication handle returned by (In) A value identifying
       a state that is specific to a module.

RReettuurrnn vvaalluueess
       pam_end returns PAM_SUCCESS if successful.  Otherwise,  it
       returns  one  of  the following error codes: Buffer error.
       System error.

                                                                1

