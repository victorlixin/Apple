

pam_close_session(3pam_api)           pam_close_session(3pam_api)

SSyynnooppssiiss
       #include <security/pam_appl.h>

       int pam_close_session (pam_handle_t *pamh, int flags);

DDeessccrriippttiioonn
       pam_close_session  closes a user session previously opened
       by

       This function takes  the  following  arguments:  (In)  The
       authentication  handle returned by (In) The following flag
       may be set to control the operation of the session manage-
       ment service: Do not generate any messages.

RReettuurrnn vvaalluueess
       pam_close_session returns PAM_SUCCESS if successful.  Oth-
       erwise, it returns  one  of  the  following  error  codes:
       Buffer  error.   Conversation error.  An attempt to load a
       service module failed.   Access  denied.   Service  module
       error.   An  attempt  to  create or delete a session entry
       failed.  A service module  did  not  recognize  a  symbol.
       System error.

                                                                1

