

aio_fsync(3aio)                                   aio_fsync(3aio)

SSyynnooppssiiss
       cc [options] -Kthread file

       #include <aio.h>

       int aio_fsync(int op, struct aiocb *aiocbp);

       int aio_fsync64(int op, struct aiocb64 *aiocbp);

DDeessccrriippttiioonn
       aio_fsync asynchronously forces I/O operations to the syn-
       chronized I/O completion state.  aio_fsync is  not  imple-
       mented  in  SVR4.2  or SVR4.2MP.  This function returns -1
       and sets errno to ENOSYS.  ENOMEM  is  returned  if  there
       were  no  internal  kernal aio control blocks available to
       service the request (number of kernel aio  control  blocks
       is tunable via the NUMAIO kernel parameter; see

RReeffeerreenncceess
NNoottiicceess
   CCoonnssiiddeerraattiioonnss ffoorr llaarrggee ffiillee ssuuppppoorrtt
       aio_fsync64 supports large files, but is otherwise identi-
       cal to aio_fsync.  For details on  programming  for  large
       file capable applications, see on intro(2).

                                                                1

