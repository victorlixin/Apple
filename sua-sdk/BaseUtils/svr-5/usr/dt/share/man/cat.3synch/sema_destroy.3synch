

sema_destroy(3synch)                         sema_destroy(3synch)

SSyynnooppssiiss
       cc [options] -Kthread file

       #include <synch.h>

       int sema_destroy(sema_t *sema);

DDeessccrriippttiioonn
       sema_destroy  destroys  the  semaphore pointed to by sema.
       This includes invalidating sema and freeing any associated
       dynamically allocated resources.

   PPaarraammeetteerrss
       pointer to the semaphore to destroy

   sseemmaa ppaarraammeetteerr
       sema  must  have  been  previously  initialized, either by
       sema_init or statically (see sema_init(3synch)).

RReettuurrnn vvaalluueess
       sema_destroy returns zero for success and an error  number
       for failure.

   EErrrroorrss
       If   any   of   the   following  conditions  is  detected,
       sema_destroy returns the corresponding value:  sema  still
       has threads waiting invalid argument specified

RReeffeerreenncceess

                                                                1

