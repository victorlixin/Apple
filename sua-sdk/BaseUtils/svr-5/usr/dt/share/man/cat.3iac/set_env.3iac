

set_env(3iac)                                       set_env(3iac)

SSyynnooppssiiss
       cc [options] file -liaf #include <ia.h> #include <iaf.h>

       int set_env(void);

DDeessccrriippttiioonn
       The  set_env  routine  sets  the  user's  environment with
       information assumed to have been passed via the  Identifi-
       cation and Authentication Facility module and is retrieved
       via the getava routine.

RReettuurrnn vvaalluueess
       The routine returns zero on success  and  non-zero  if  an
       error occurs.

RReeffeerreenncceess

                                                                1

