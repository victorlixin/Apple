

VxOptionMenuReplaceOptions(1vtcl)VxOptionMenuReplaceOptions(1vtcl)

SSyynnooppssiiss
       VxOptionMenuReplaceOptions widgetName [ options] selection

DDeessccrriippttiioonn
       Dynamically changes the options (in an option menu created
       by VxOptionMenu) to the new options passed in.

   OOppttiioonnss
       Name  of  menu  returned by VxOptionMenu.  Option which is
       initially selected (for example, ``one'').   Tcl  list  of
       options (to be used as labels for the pushbuttons).

                                                                1

