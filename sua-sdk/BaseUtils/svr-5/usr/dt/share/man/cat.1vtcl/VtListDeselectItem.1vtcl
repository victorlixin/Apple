

VtListDeselectItem(1vtcl)               VtListDeselectItem(1vtcl)

SSyynnooppssiiss
       VtListDeselectItem list_widgetName [options]

DDeessccrriippttiioonn
       Deselects one or more items from a List.

   OOppttiioonnss
       Deselects  the entire List.  Deselects a List item.  Dese-
       lects a list  of  items.   Calls  the  -callback  for  the
       selected item.  Specifies the position of the item to des-
       elect.  (The base position is 1. To indicate the last item
       on  the  List,  use  0.)  Specifies a list of positions to
       deselect.

RReettuurrnn vvaalluueess
       If VtListDeselectItem fails, it returns one of the follow-
       ing error messages: the position value is outside the List
       range no option was specified couldn't find the referenced
       field  referenced widget is not a List too many or too few
       options specified number of records specifed exceeds  num-
       ber of available records See

                                                                1

