

VtDrawnListGetItem(1vtcl)               VtDrawnListGetItem(1vtcl)

SSyynnooppssiiss
       VtDrawnListGetItem drawnlist_widgetName [options]

DDeessccrriippttiioonn
       Gets  the records from the list.  Returns a list of lists;
       a two dimension list of the items in the DrawnList.  {
          { 0 {string} {data}}
          { 0 {string} {data}} }

   OOppttiioonnss
       Gets the entire list.  Gets fields on the basis of a match
       on  columns  of data.  For example, if your list contains:
       {0 1 "String one"} {0 1 "String two"} {0 1 "String three"}
       specifying  -field  2 "String two" matches item 2.  Speci-
       fies an item to get by position.  (The base position is 1.
       To  indicate the last item on the list, use 0.)  Specifies
       a list of  item  positions  to  get.  If  this  option  is
       selected,  VtDrawnListGetSelectedItem  returns  a  list of
       positions instead of the normal return value.

RReettuurrnn vvaalluueess
       If VtDrawnListGetItem fails, it returns one of the follow-
       ing error messages: the position value is outside the list
       range couldn't find the referenced field no  options  were
       specified referenced widget is not a DrawnList too many or
       too few  options  specified  number  of  records  specifed
       exceeds number of available records

                                                                1

