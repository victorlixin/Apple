

VtLabel(1vtcl)                                     VtLabel(1vtcl)

SSyynnooppssiiss
       VtLabel widgetName [options]

DDeessccrriippttiioonn
       Creates  a Label (either text or a pixmap) and returns the
       widget name.

   OOppttiioonnss
RReettuurrnn vvaalluueess
       If VtLabel fails, it returns one of  the  following  error
       messages: cannot find or create given pixmap See

                                                                1

