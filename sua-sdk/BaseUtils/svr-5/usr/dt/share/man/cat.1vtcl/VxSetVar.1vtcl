

VxSetVar(1vtcl)                                   VxSetVar(1vtcl)

SSyynnooppssiiss
       VxSetVar widgetName varName value

DDeessccrriippttiioonn
       Sets  the value of a variable and associates that variable
       with the passed-in widget. This is used to store  informa-
       tion  relevant to the widget (rather than setting a global
       variable).

   OOppttiioonnss
       Name of the widget that the variable is  associated  with.
       Name of the variable.  May be a scalar or array reference.
       The value assigned to the variable.

RReettuurrnn vvaalluueess
       If successful, VxSetVar returns value.

RReeffeerreenncceess

                                                                1

