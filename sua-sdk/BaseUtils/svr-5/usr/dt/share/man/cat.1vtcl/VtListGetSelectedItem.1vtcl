

VtListGetSelectedItem(1vtcl)         VtListGetSelectedItem(1vtcl)

SSyynnooppssiiss
       VtListGetSelectedItem list_widgetName [options]

DDeessccrriippttiioonn
       Gets  the list of selected items from a List.  By default,
       the items are returned by PositionList.  Only  one  option
       may be specifed at one time.

   OOppttiioonnss
       Specifies that a list of selected items is to be returned.
       For example: {{item 1}  {item  2}}  Sets  the  routine  to
       return items by List position; for example: {1 2}

RReettuurrnn vvaalluueess
       If VtListGetSelectedItem fails, it returns one of the fol-
       lowing error messages: referenced widget is not a List too
       many or too few options specified See

                                                                1

