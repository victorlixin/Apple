

VtPushButton(1vtcl)                           VtPushButton(1vtcl)

SSyynnooppssiiss
       VtPushButton widgetName [options]

DDeessccrriippttiioonn
       Creates  a  PushButton.  (Label  class options are used to
       label the button.)  Returns the PushButton widget name.

   OOppttiioonnss
       Sets the callback to call when you press the button.

       Additional callback keys: Sets the pixmap file to use  for
       the  armed pixmap.  The armed pixmap is displayed when you
       press a button.  Number of multi-clicks pressed  inside  a
       button.   This  is  used  to detect double or triple click
       events inside a  pushbutton.   Contains  the  pushbutton's
       label  string.   Updates  the  interpreter variable string
       with the widget value. The update happens under  the  same
       conditions  that would invoke -callback, but before -call-
       back is called. Setting a value into the interpreter vari-
       able updates the ComboBox as if setting -value on the wid-
       get.

RReettuurrnn vvaalluueess
       If VtPushButton fails, it returns  one  of  the  following
       error messages: See

                                                                1

