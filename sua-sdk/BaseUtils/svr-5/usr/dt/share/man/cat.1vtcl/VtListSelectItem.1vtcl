

VtListSelectItem(1vtcl)                   VtListSelectItem(1vtcl)

SSyynnooppssiiss
       VtListSelectItem list_widgetName [options]

DDeessccrriippttiioonn
       Selects one or more items in a List.

   OOppttiioonnss
       Selects  the  entire  List.   Selects item string from the
       List.  Selects all items in  list.   Calls  the  -callback
       callback for the selected item.  Selects the item at posi-
       tion integer in list.  (The base position is 1.  To  indi-
       cate  the last item on the List, use 0.)  Specifies a list
       of positions to select.

RReettuurrnn vvaalluueess
       If VtListSelectItem fails, it returns one of the following
       error  messages:  the  position  value is outside the List
       range no option was specified couldn't find the referenced
       field  referenced widget is not a List too many or too few
       options specified number of records specifed exceeds  num-
       ber of available records See

                                                                1

