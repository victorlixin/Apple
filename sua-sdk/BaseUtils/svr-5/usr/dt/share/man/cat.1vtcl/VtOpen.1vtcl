

VtOpen(1vtcl)                                       VtOpen(1vtcl)

SSyynnooppssiiss
       VtOpen Class [options]

DDeessccrriippttiioonn
       Establishes  a  connection  to &VTcl;.  An optional second
       argument is the bookname for scohelp books.

       Class is used as the application ClassName and may be used
       by  the  server to specify or find configuration items for
       this applet.

       VtOpen returns the application name to be used as a parent
       for the top form.

   OOppttiioonnss
       Specifies the bookname to reference for scohelp calls.

RReettuurrnn vvaalluueess
       If  VtOpen  fails,  it  returns one of the following error
       messages:  attempted  to  open  connection  when  one  was
       already  open wrong number of arguments, argument not rec-
       ognized, or  argument  options  not  recognized  attempted
       specify an unsupported connection/transport method

                                                                1

