

VtDrawnListDeselectItem(1vtcl)     VtDrawnListDeselectItem(1vtcl)

SSyynnooppssiiss
       VtDrawnListDeselectItem drawnlist_widgetName [options]

DDeessccrriippttiioonn
       Deselects  an item from a DrawnList. It is an error if the
       position or the field options do not  match  something  in
       the list.

   OOppttiioonnss
       Deselects the entire list.  Deselects the item whose field
       column matches matchStr.  For example, if your  list  con-
       tains:  {0 1 "String one"} {0 1 "String two"} {0 1 "String
       three"} specifying -field 2 "String one" would match  item
       1.  Specifies the position of the item to deselect.  Dese-
       lects the items at the specified positions.

RReettuurrnn vvaalluueess
       If VtDrawnListDeselectItem fails, it returns  one  of  the
       following  error  messages:  the position value is outside
       the list range  couldn't  find  the  referenced  field  no
       options  were  specified referenced widget is not a Drawn-
       List too many or  too  few  options  specified  number  of
       records specifed exceeds number of available records

                                                                1

