

VtDrawnListSelectItem(1vtcl)         VtDrawnListSelectItem(1vtcl)

SSyynnooppssiiss
       VtDrawnListSelectItem drawnlist_widgetName [options]

DDeessccrriippttiioonn
       Selects the items in a DrawnList.

   OOppttiioonnss
       Select  the  entire  list.   Select all items that contain
       matchStr in column.  For example, if your  list  contains:
       {0 1 "String one"} {0 1 "String two"} {0 1 "String three"}
       specifying -field 2  "String  one"  would  match  item  1.
       Select  the next item.  Specifies the position of the item
       to be selected.  (The base position is 1. To indicate  the
       last  item on the list, use 0.)  Select a list of items by
       position.  Select the previous item.

RReettuurrnn vvaalluueess
       If VtDrawnListSelectItem fails, it returns one of the fol-
       lowing  error  messages: the position value is outside the
       list range couldn't find the referenced field  no  options
       were  specified  referenced  widget is not a DrawnList too
       many or  too  few  options  specified  number  of  records
       specifed exceeds number of available records

                                                                1

