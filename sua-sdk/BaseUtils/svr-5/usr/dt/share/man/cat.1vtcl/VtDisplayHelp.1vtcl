

VtDisplayHelp(1vtcl)                         VtDisplayHelp(1vtcl)

SSyynnooppssiiss
       VtDisplayHelp widgetName [ options ]

DDeessccrriippttiioonn
       Displays a hard coded topic for widgetName.

   OOppttiioonnss
       string  specifies  the  help book to search for the topic.
       string specifies the  help  topic  to  send  to  the  help
       server.  string specifies a widget tree that the help sys-
       tem uses to resolve a help topic.

RReettuurrnn vvaalluueess
       If VtDisplayHelp fails, it returns one  of  the  following
       error  messages:  no  widget  corresponding  to widgetName
       given must specify either -topic or -tree, not both

                                                                1

