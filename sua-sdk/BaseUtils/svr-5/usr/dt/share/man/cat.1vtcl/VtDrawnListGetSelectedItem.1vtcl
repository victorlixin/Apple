

VtDrawnListGetSelectedItem(1vtcl)VtDrawnListGetSelectedItem(1vtcl)

SSyynnooppssiiss
       VtDrawnListGetSelectedItem  drawnlist_widgetName [ options
       ]

DDeessccrriippttiioonn
       Returns the selected items in the DrawnList, returns byPo-
       sitionList if you do not specify any options.

   OOppttiioonnss
       Return  items  by  position list (for example, {1 2 3 4}).
       For the DrawnList, returns a list of lists of fields;  for
       example: { { 0 1 "field one" "field two"}
         { 1 1 "field one" "field two"}
         { 1 3 "field one" "field two"} }

RReettuurrnn vvaalluueess
       If VtDrawnListGetSelectedItem fails, it returns one of the
       following error  messages:  referenced  widget  is  not  a
       DrawnList too many or too few options specified

                                                                1

