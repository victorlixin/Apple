

VxWidgetVarRef(1vtcl)                       VxWidgetVarRef(1vtcl)

SSyynnooppssiiss
       VxWidgetVarRef widgetName varName

DDeessccrriippttiioonn
       Return  a  reference  for  the  per-widget variable frames
       variable.  This allows the variable to be passed by refer-
       ence  to  other  Tcl  commands.   For example: array names
       [VxWidgetVarRef my.widget.path data]

   OOppttiioonnss
       Name of widget that the variable is associated with.  Name
       of  the  variable.   May  be a scalar or array, but not an
       element of the array.

RReettuurrnn vvaalluueess
       If successful, VxWidgetVarRef returns a reference  to  the
       variable usable in the current scope.

                                                                1

