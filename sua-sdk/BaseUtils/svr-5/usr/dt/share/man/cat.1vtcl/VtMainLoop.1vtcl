

VtMainLoop(1vtcl)                               VtMainLoop(1vtcl)

SSyynnooppssiiss
       VtAddMainLoop [-break]

DDeessccrriippttiioonn
       Starts  interaction  between  the  script  and  the &VTcl;
       graphical user interface (GUI) rendering  engine.   Causes
       the  script application to become active and react to user
       and system actions.  Usually called as the last command of
       a script after the GUI hierarchy is displayed with

   OOppttiioonnss
       Forces  the  original VtMainLoop command to end.  This can
       only be executed after VtMainloop is executed, from inside
       a  callback.  This  option  leaves the interpreter and the
       server still connected.

RReettuurrnn vvaalluueess
       If VtMainLoop fails, it returns one of the following error
       messages:  wrong  number of arguments, argument not recog-
       nized, or argument options not recognized the MainLoop  is
       already running

                                                                1

