

VtDrawnListDeleteItem(1vtcl)         VtDrawnListDeleteItem(1vtcl)

SSyynnooppssiiss
       VtDrawnListDeleteItem drawnlist_widgetName [options]

DDeessccrriippttiioonn
       Deletes  an  item from a DrawnList.  It is an error if the
       position or the field options do not  match  something  in
       the  list.  Only  one of the options listed may be used at
       one time.

   OOppttiioonnss
       Specify the entire list.  Delete the item whose field col-
       umn  matches  the  string  matchStr.  For example, if your
       list contains: {0 1 "String one"} {0 1 "String two"} {0  1
       "String  three"}  specifying  -field  2 "String one" would
       match item 1.  Specify a list item by position.  (The base
       position  is 1. To indicate the last item on the list, use
       0.)  Specify a list of items to delete by position.

RReettuurrnn vvaalluueess
       If VtDrawnListDeleteItem fails, it returns one of the fol-
       lowing  error  messages: the position value is outside the
       list range couldn't find the referenced field  no  options
       were  specified  referenced  widget is not a DrawnList too
       many or  too  few  options  specified  number  of  records
       specifed exceeds number of available records

                                                                1

