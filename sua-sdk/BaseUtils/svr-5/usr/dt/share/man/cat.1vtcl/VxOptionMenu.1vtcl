

VxOptionMenu(1vtcl)                           VxOptionMenu(1vtcl)

SSyynnooppssiiss
       VxOptionMenu   widgetName   label  [optionList]  defaultCB
       selection

DDeessccrriippttiioonn
       Returns an option list that contains pushbuttons  for  the
       options specified.

   OOppttiioonnss
       Widget hierarchy of option menu.  Menu label (for example,
       filename:).  Tcl list of options (to be used as labels for
       the  pushbuttons).  Callback to be called when option menu
       changes.  Option which is initially selected (for example,
       ``one'').

                                                                1

