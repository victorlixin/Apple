

VtCheckBox(1vtcl)                               VtCheckBox(1vtcl)

SSyynnooppssiiss
       VtCheckBox widgetName [options]

DDeessccrriippttiioonn
       Creates a CheckBox widget. Returns the widget name.

   OOppttiioonnss
       Specifies  that  cmd is called when a toggle button in the
       CheckBox is selected (using , , or  mouse  button).   This
       overrides  any  individual  callback set for the contained
       toggle buttons.

       Additional callback keys: list of selected toggle buttons)
       list  of  selected  toggle buttons Sets the orientation to
       horizontal.  Sets the number of columns of  objects  in  a
       RowColumn,   CheckBox,  or  RadioBox.   Sets  the  spacing
       between objects inside a row column.  Useful for  a  tool-
       bar.   Turns on the passed-in toggle button (unsetting all
       the rest).  To set more than one  button  use  -valueList.
       Takes a list of toggle button widget names and turns those
       toggle buttons on (unsetting all the rest).  Sets the ori-
       entation to vertical.

RReeffeerreenncceess

                                                                1

