

VtInfo(1vtcl)                                       VtInfo(1vtcl)

SSyynnooppssiiss
       VtInfo [ options ]

DDeessccrriippttiioonn
       Returns  some  information  about  &VTcl;.  Only  a single
       option may be submitted at one time.

   OOppttiioonnss
       This is a flag; it returns 1 if running in character mode,
       otherwise  0.  This is a flag; it returns the maximum num-
       ber of colors that can be displayed on the X-Server.  This
       does  not  mean  that you have that many colors free.  The
       character mode environment always  returns  2  (foreground
       and  background).   Returns the height of the display.  In
       character mode it returns the number of rows for the  ter-
       minal;  in graphical mode it returns the number of pixels.
       Returns the width of the display.  In  character  mode  it
       returns the number of columns for the terminal; in graphi-
       cal mode it returns the number  of  pixels.   Returns  the
       &VTcl; version.

RReettuurrnn vvaalluueess
       If  VtInfo  fails,  it  returns one of the following error
       messages: no  option  was  specified  unrecognized  option
       option option is not valid for this widget

                                                                1

