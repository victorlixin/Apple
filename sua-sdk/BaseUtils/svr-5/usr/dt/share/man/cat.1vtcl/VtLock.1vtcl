

VtLock(1vtcl)                                       VtLock(1vtcl)

SSyynnooppssiiss
       VtLock

DDeessccrriippttiioonn
       Sets the cursor to the watch cursor and locks the applica-
       tion (that is, the application will no longer accept input
       from  the user).  New dialogs that are about to pop up are
       also automatically locked.  To unlock the application, see

       VtLock  can  be  called  multiple times.  Calling VtUnLock
       will free all of the current locks.  If you only  wish  to
       unlock  one layer of the nested locks, use VtUnLock -once.

RReettuurrnn vvaalluueess
       If VtLock fails, it returns one  of  the  following  error
       messages: too many or too few options specified

                                                                1

