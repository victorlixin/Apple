

VtListAddItem(1vtcl)                         VtListAddItem(1vtcl)

SSyynnooppssiiss
       VtListAddItem list_widgetName [options]

DDeessccrriippttiioonn
       Adds one or more items to a List.

   OOppttiioonnss
       Sets  an  item  for the List widget.  Sets a list of items
       for the List widget.  Specifies the list position  of  the
       new  item.   (The base position is 1. To indicate the last
       item on the List, use 0.)

RReettuurrnn vvaalluueess
       If VtListAddItem fails, it returns one  of  the  following
       error  messages:  cannot set both -item and -itemList need
       to set either -item or -itemList need to set either  -item
       or -position See

                                                                1

