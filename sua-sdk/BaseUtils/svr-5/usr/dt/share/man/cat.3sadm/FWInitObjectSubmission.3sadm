

FWInitObjectSubmission(3sadm)       FWInitObjectSubmission(3sadm)

SSyynnooppssiiss
       CC ... -lsysadm -lvtcl -ltclx -ltcl -lsocket -lgen -lm

       #include  <scoadmin/basicIncl.h>  #include <scoadmin/call-
       Back.h> #include <scoadmin/statusLst.h>  #include  <scoad-
       min/CAPI.h>

       bmipSubmission_cl   *   FWInitObjectSubmission(frameWorkC-
       ClientData_cl  *clientDataPtr,                     object-
       Class_pt           objectClass);

DDeessccrriippttiioonn
       This  function  allocates  an  object  class specific data
       structure which is utilized by the routines  described  in
       FWClassParameters(3sadm).  and FWClassExamine(3sadm).

   AArrgguummeennttss
       pointer  to  an internal data structure returned by a call
       to FWInitializeFrameWork 3sadm A string defining the  name
       of  the  object  class, usually in the format "vendor_name
       class_name".

EExxaammpplleess
       #include <scoadmin/CAPI.h>

       main() {
           frameWorkCClientData_cl   *clientDataPtr;
           bmipSubmission_cl         *bmipSubmissionPtr1,  *bmip-
       SubmissionPtr2;

           /* Initialize general admin data structure */
           clientDataPtr=FWInitializeFrameWork();

           /* Initialize class-specific admin data structures */

           /*  Initialize  communications  to  the  "sco  device"
       class. */
           bmipSubmissionPtr1=FWInitObjectSubmission(clientDat-
       aPtr, "sco device");

           /* Initialize communications to the "acme disk" object
       class. */
           bmipSubmissionPtr1=FWInitObjectSubmission(clientDat-
       aPtr, "acme disk");

           /*
       *****************************************************
            * Routines defined in FWClassParameters 3sadm,
            * FWClassOperations 3sadm, and FWClassExamine(3sadm)
            * go here...
            *******************************************************/
           ....

           /* Cleanup */

                                                                1

FWInitObjectSubmission(3sadm)       FWInitObjectSubmission(3sadm)

           FWShutdownObjectSubmission(bmipSubmissionPtr1);
           FWShutdownObjectSubmission(bmipSubmissionPtr2);
           FWShutdownFrameWork(clientDataPtr); }

RReeffeerreenncceess

                                                                2

