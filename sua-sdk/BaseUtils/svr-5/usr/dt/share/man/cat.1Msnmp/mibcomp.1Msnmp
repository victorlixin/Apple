

mibcomp(1Msnmp)                                   mibcomp(1Msnmp)

SSyynnooppssiiss
       /usr/sbin/mibcomp  [  -s  ]  -o defns_file module.my[,mod-
       ule.my] ...

DDeessccrriippttiioonn
       The script mibcomp: runs the compiler  mosy  over  one  or
       more  managed  objects modules (module.my) and appends the
       output of each to a temporary file prepends the  following
       object definitions to the temporary file: iso.3.6.1 inter-
       net.1  internet.2  internet.3  internet.4  private.1  runs
       post_mosy  over  the temporary file and puts the result in
       defns_file. If defns_file is -,  the  standard  output  is
       used.

   OOppttiioonnss
       Normally, mosy prints the name of each object, identifier,
       or type while it runs. The -s option disables this  behav-
       ior.

RReeffeerrnncceess
       RFC 1155 (STD 16), RFC 1212

                                                                1

