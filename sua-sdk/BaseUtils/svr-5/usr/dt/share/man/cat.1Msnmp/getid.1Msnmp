

getid(1Msnmp)                                       getid(1Msnmp)

SSyynnooppssiiss
       getid [-T timeout] entity_addr community_string

DDeessccrriippttiioonn
       The  getid  command  is an SNMP application that retrieves
       the variables sysDescr.0, sysObjectID.0,  and  sysUpTime.0
       from an SNMP entity.

       The  arguments  are the entity's address and the community
       string needed for access to the SNMP entity.

       The entity address can be either an IP address  or  entity
       name (if name-to-address resolution is enabled).

       The  community  string  used  must  be  valid on the given
       entity.  On  &gemini;,  community  strings  are  kept   in
       /etc/netmgt/snmpd.comm.

       The  primary  purpose of this application is to illustrate
       the use of the SNMP library routines.

   OOppttiioonnss
       By default, getid waits 20 seconds for a response from the
       entity specified by entity_addr. Use the -T timeout option
       to specify a different number of seconds for  the  command
       to wait for a response.

RReeffeerreenncceess
       RFC 1155, RFC 1157, RFC 1213

                                                                1

