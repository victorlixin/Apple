

VipDestroyPtag(3via)                         VipDestroyPtag(3via)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -via  [library] ...  #include
       <vipl.h>

       VIP_RETURN VipDestroyPtag(VIP_NIC_HANDLE NicHandle,
            VIP_PROTECTION_HANDLE Ptag);

DDeessccrriippttiioonn
       The VipDestroyPtag routine is used to destroy a protection
       tag.

       If  the  specified  tag  is  associated  with  either a VI
       instance or a registered memory region at the time of  the
       call, an error is returned.

   AArrgguummeennttss
       The  NIC  handle  associated with the protection tag.  The
       protection tag to be destroyed.

RReettuurrnn vvaalluueess
       The memory protection tag was successfully  destroyed.   A
       VI instance or a registered memory region is still associ-
       ated with the specified protection tag, or  the  operation
       failed  due to insufficient resources.  One of the parame-
       ters was invalid.

RReeffeerreenncceess

                                                                1

