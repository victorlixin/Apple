

VipPostRecv(3via)                               VipPostRecv(3via)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -via  [library] ...  #include
       <vipl.h>

       VIP_RETURN       VipPostRecv(VIP_VI_HANDLE       ViHandle,
       VIP_DESCRIPTOR *DescriptorPtr,
            VIP_MEM_HANDLE MemoryHandle);

DDeessccrriippttiioonn
       The  VipPostRecv routine adds a descriptor to the tail end
       of a VI's receive queue. It then notifies the NIC that the
       descriptor is available, and returns immediately.

   AArrgguummeennttss
       The  instance  of a VI.  The pointer to a descriptor to be
       posted on the receive queue.  The handle  for  the  memory
       region of the descriptor being posted.

RReettuurrnn vvaalluueess
       The  receive  descriptor  was successfully posted.  The VI
       handle was invalid.

RReeffeerreenncceess

                                                                1

