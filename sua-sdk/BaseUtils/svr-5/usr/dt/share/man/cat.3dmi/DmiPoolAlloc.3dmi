

DmiPoolAlloc(3dmi)                             DmiPoolAlloc(3dmi)

SSyynnooppssiiss
       #include <dmi2mem.h>

       Void  * DMI_API DmiPoolAlloc (      size_t size;      void
       * pool_handle;      Dmi_ErrorStatus_t * status;      );

DDeessccrriippttiioonn
       DmiPoolAlloc is used to allocate  size-byte  memory  in  a
       pool  of  memory previously created by When successful, it
       returns a pointer to the  allocated  block,  otherwise  it
       returns NULL.

   PPaarraammeetteerrss
       (input)  Memory  block size Pool handle created by Pointer
       to an address in memory where the DmiErrorStatus_t code is
       returned or NULL.

RReettuurrnn vvaalluueess
       This  function  returns the pointer to the allocated block
       or NULL in case of failure.

       The   status   returned   is   one   of:   DMIERR_NO_ERROR
       DMIERR_OUT_OF_MEMORY

       For a description of return values, see

                                                                1

