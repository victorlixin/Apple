

DmiNodeAlloc_t(3dmi)                         DmiNodeAlloc_t(3dmi)

SSyynnooppssiiss
       #include <dmi2mem.h>

       typedef  void * DMI_API DmiNodeAlloc_t (      size_t size,
            void * mgmtData,      DmiErrorStatus_t * status);

DDeessccrriippttiioonn
       DmiNodeAlloc_t is the placeholder for the  memory  alloca-
       tion function.  This function allocates a size-byte memory
       block and returns the pointer to this block.

   PPaarraammeetteerrss
       (Input) Memory block size in octets.  (Input)  Pointer  to
       the  memory  model  private  data.  (Output) Pointer to an
       address in  memory  where  the  DmiErrorStatus_t  code  is
       returned.

RReettuurrnn vvaalluueess
       This  function returns the pointer to the allocated block,
       or NULL in case of failure.

NNoottiicceess
       Portions of this page are derived from material for  which
       the  copyright owner is Intel Corporation, Inc.  The mate-
       rial is reprinted with permission.  See copyright page for
       a full statement of rights and permissions.

                                                                1

