

DmiGetNextPoolBlock(3dmi)               DmiGetNextPoolBlock(3dmi)

SSyynnooppssiiss
       #include <dmi2mem.h>

       DmiVoid_t * DMI_API DmiGetNextPoolBlock (      void * ptr;
            void * pool_handle;      void * status;      );

DDeessccrriippttiioonn
       DmiGetNextPoolBlock is used to get the  next  block  in  a
       pool of memory.

   PPaarraammeetteerrss
       A  pointer to a memory block in the pool.  The pool_handle
       returned by a call to Pointer  to  an  address  in  memory
       where  the  DmiErrorStatus_t code is returned or NULL.  If
       the status pointer  is  NULL,  the  error  status  is  not
       returned.

RReettuurrnn vvaalluueess
       This  function  returns  the  pointer to the next block or
       NULL in case of failure.

       Status  codes  returned  to  status  are:  DMIERR_NO_ERROR
       DMIERR_MEM_NO_BLOCK_IN_POOL

       For a description of return values, see

                                                                1

