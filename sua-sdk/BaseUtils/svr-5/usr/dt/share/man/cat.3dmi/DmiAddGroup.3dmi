

DmiAddGroup(3dmi)                               DmiAddGroup(3dmi)

SSyynnooppssiiss
       cc  [options] file -llibdmionc # remote calls cc [options]
       file -llibdmi2api # local calls #include <dmi2srv.h>

       DmiErrorStatus_t       DMI_API        DmiAddGroup        (
                 DmiHandle_t         handle,             DmiFile-
       DataList_t                                   *   fileData,
                 DmiId_t             compId,
                 DmiId_t             groupId,
                 DmiStringList_t **   errors );

DDeessccrriippttiioonn
       The  DmiAddGroup function is used to add a new group to an
       existing component in the database.  It takes the name  of
       a  file,  or  the  address  of memory block containing the
       group's schema  description  data,  checks  the  data  for
       adherence  to the schema description grammar (e.g. DMI MIF
       grammar), and installs the group schema description in the
       database.

   PPaarraammeetteerrss
       (Input)  An open session handle (Input) Schema description
       file data for the group definition  (Input)  Component  to
       access  (Output)  On completion, the SP-allocated group ID
       (Output) Installation error messages

RReettuurrnn vvaalluueess
       For a description of return values, see

NNoottiicceess
       Portions of this page are derived from material for  which
       the  copyright owner is the Desktop Management Task Force.
       The material is reprinted with permission.  See  copyright
       page for a full statement of rights and permissions.

                                                                1

