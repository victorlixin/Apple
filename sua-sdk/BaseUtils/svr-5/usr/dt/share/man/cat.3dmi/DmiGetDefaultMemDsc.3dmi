

DmiGetDefaultMemDsc(3dmi)               DmiGetDefaultMemDsc(3dmi)

SSyynnooppssiiss
       #include <dmi2mem.h>

       DmiMemDsc_t   DMI_API  DmiGetDefaultMemDsc  (       DmiEr-
       rorStatus_t * status );

DDeessccrriippttiioonn
       DmiGetDefaultMemDsc returns the last  set  default  memory
       management  descriptor.   The  descriptor returned by this
       function is a global per-process variable. Initially,  the
       standard system descriptor is set as the default.

   PPaarraammeetteerrss
       (Output)  NULL, or a pointer to an address in memory where
       the DmiErrorStatus_t code  is  returned.   If  the  status
       pointer  is  NULL,  the  error status is not returned. The
       possible error codes are:

       For a description of return values, see

RReettuurrnn vvaalluueess
       When successful, this function returns the default  memory
       model descriptor. Otherwise, it returns NULL.

NNoottiicceess
       Portions  of this page are derived from material for which
       the copyright owner is Intel Corporation, Inc.  The  mate-
       rial is reprinted with permission.  See copyright page for
       a full statement of rights and permissions.

                                                                1

