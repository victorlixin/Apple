

DmiAddLanguage(3dmi)                         DmiAddLanguage(3dmi)

SSyynnooppssiiss
       cc  [options] file -llibdmionc # remote calls cc [options]
       file -llibdmi2api # local calls #include <dmi2srv.h>

       DmiErrorStatus_t      DMI_API       DmiAddLanguage       (
                 DmiHandle_t         handle,             DmiFile-
       DataList_t                                   *   fileData,
                 DmiId_t             compId,
                 DmiStringList_t **   errors );

DDeessccrriippttiioonn
       The DmiAddLanguage function is used to add a new  language
       mapping  for  an  existing  component in the database.  It
       takes the name of a file, or the address of  memory  block
       containing  translated schema description data, checks the
       data for adherence to the schema description grammar (e.g.
       DMI  MIF  grammar),  and  installs  the  translated schema
       description in the database.

   PPaarraammeetteerrss
       (Input) An open session handle  (Input)  Language  mapping
       file  for  the component (Input) Component to access (Out-
       put) Installation error messages

RReettuurrnn vvaalluueess
       For a description of return values, see

NNoottiicceess
       Portions of this page are derived from material for  which
       the  copyright owner is the Desktop Management Task Force.
       The material is reprinted with permission.  See  copyright
       page for a full statement of rights and permissions.

                                                                1

