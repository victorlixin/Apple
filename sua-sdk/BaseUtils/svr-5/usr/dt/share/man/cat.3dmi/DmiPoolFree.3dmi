

DmiPoolFree(3dmi)                               DmiPoolFree(3dmi)

SSyynnooppssiiss
       #include <dmi2mem.h>

       void  *  DMI_API  DmiPoolFree (      void * block_pointer;
            void * pool_handle;      );

DDeessccrriippttiioonn
       DmiPoolFree is used to free an element of  memory  pointed
       to by memDsc in a pool of memory mgmtData.

   PPaarraammeetteerrss
       The memory allocated by a previous call to The memory pool
       allocated by a previous call to

RReettuurrnn vvaalluueess
       DMIERR_NO_ERROR

       For a description of return values, see

                                                                1

