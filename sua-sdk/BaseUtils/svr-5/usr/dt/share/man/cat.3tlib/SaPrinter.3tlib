

SaPrinter(3tlib)                                 SaPrinter(3tlib)

SSyynnooppssiiss
       SaPrintersGet  [host]  [type]  [listtypes] SaPrinterGetDe-
       fault  [host]   SaPrinterGetClasses   [host]   [listtypes]
       SaPrinterGetQueues [host] [type] [listtypes]

DDeessccrriippttiioonn
       These  functions  provide  a  front end to obtain commonly
       used information from the printer  OSA.   get  a  list  of
       printers  of  specified type(s) on specified host machine.
       When "listtypes" is true, SaPrintersGet returns a list  of
       lists where each list contains a pair: printer and printer
       type.  get the  default  printer  on  the  specified  host
       machine.  If no default printer is configured, returns {}.
       get a list  of  printer  classes  on  the  specified  host
       machine.   When  "listtypes"  is true, SaPrinterGetClasses
       returns a list of lists where each list contains  a  pair:
       class  name  and  the type "class".  get a list of printer
       queues (e.g. printers and classes) of specified type(s) on
       specified host machine. When "listtypes" is true, SaPrint-
       erGetQueues returns a list of lists where each  list  con-
       tains a pair: queue name and the type (see below).

   AArrgguummeennttss
       title  of  a  desktop icon representing an icon.  optional
       full pathname of a file to be printed.  If omitted, a file
       selection  box  is presented for the user to interactively
       select the file to be printed.

DDiiaaggnnoossttiiccss
       Errors will be thrown and should  be  caught  with  Error-
       Catch.

                                                                1

