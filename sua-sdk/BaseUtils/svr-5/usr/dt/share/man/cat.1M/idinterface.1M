

idinterface(1M)                                   idinterface(1M)

SSyynnooppssiiss
       /etc/conf/bin/idinterface [ -R rootdir ] { name.version }

DDeessccrriippttiioonn
       One  of  the ID/TP (Installable Driver/Tunable Parameters)
       kernel configuration tools, idinterface reports,  to  std-
       out, the list of valid symbol names for one or more inter-
       face versions.

       A symbol list is output  for  each  name.version  argument
       specified.   If no arguments are given, then all supported
       interfaces are shown.

       Each symbol list consists of one  symbol  name  per  line,
       with  an optional second white-space separated field show-
       ing a symbol name to which  the  first  symbol  would  get
       remapped.  This is the same syntax described in

   OOppttiioonnss
       idinterface takes the following options: Look for configu-
       ration information under rootdir instead of /etc/conf.

EExxiitt ccooddeess
       An exit value of zero indicates success.  If an  error  is
       encountered,  idinterface  will exit with a non-zero value
       and report an error message.

FFiilleess
       Interface definition files

RReeffeerreenncceess

                                                                1

