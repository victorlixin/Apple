

ev_getemask(3event)                           ev_getemask(3event)

SSyynnooppssiiss
       cc   .  .  .   -levent  #include   <sys/types.h>  #include
       <sys/param.h>   #include     <sys/sysmacros.h>    #include
       <sys/page.h> #include  <sys/event.h> #include  <mouse.h>

       int ev_getemask(emaskp) emask_t *emaskp;

DDeessccrriippttiioonn
       ev_getemask  returns  the  current  event  mask. This call
       takes a pointer to an event mask which is filled  in.  The
       program must have already opened an event queue.

       This  call  complements  ev_setemask.  The manual page for
       ev_setemask describes an event mask in detail.

RReettuurrnn vvaalluueess
       ev_getemask returns -1 if there is no  open  event  queue.
       Otherwise it returns 0.

RReeffeerreenncceess
SSttaannddaarrddss ccoonnffoorrmmaannccee
       ev_getmsk is not part of any currently supported standard;
       it is an extension of AT&T System V provided by the  Santa
       Cruz Operation.

                                                                1

