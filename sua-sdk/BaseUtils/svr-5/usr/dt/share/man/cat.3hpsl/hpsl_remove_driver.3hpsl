

hpsl_remove_driver(3hpsl)               hpsl_remove_driver(3hpsl)

SSyynnooppssiiss
       cc -lhpsl

       #include <hpsl.h>

       int hpsl_remove_driver(char *drvName, int suspId)

DDeessccrriippttiioonn
       /*(L<hpsl_remove_driver/*(L>  removes  a  driver  from the
       suspended list.

   IInnppuutt
       suspended driver instance name.  ID of the device on which
       the  I/O  was  previously  suspended (maintained by hpci).
       Refer to for more information.

   OOuuttppuutt
       None.

UUssaaggee
       In order to use the driver which was removed from the sus-
       pended list again, must be called.

RReettuurrnn vvaalluueess
       See

RReeffeerreenncceess
EExxaammppllee
       SuspendedDrvInfoPtr_t    suspList=NULL;          uint sus-
       pListLen=0; int  index; int  retval;

       if  ((retval=hpsl_get_suspended_drvlist(&suspList,   &sus-
       pListLen))!=HPSL_SUCCESS)       LocalErrorHandler(retval);
       else  {       index=GetIndexToRemove();        if   ((ret-
       val=hpsl_remove_driver(suspList[index-1].drvname,     sus-
       pList[index-1].suspId))!=HPSL_SUCCESS)                   {
                 LocalErrorHandler(retval);      } }

                                                                1

