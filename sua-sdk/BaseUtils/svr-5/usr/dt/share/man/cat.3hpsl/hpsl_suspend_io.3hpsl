

hpsl_suspend_io(3hpsl)                     hpsl_suspend_io(3hpsl)

SSyynnooppssiiss
       cc -lhpsl

       #include <hpsl.h>

       int hpsl_suspend_io(HpslSocketInfoPtr_t      socketInfo,
                           rm_key_t                 rmKey);

DDeessccrriippttiioonn
       /*(L<hpsl_suspend_io/*(L>  suspends  the  driver  instance
       attached to the device represented by the key, rmKey.  All
       I/O  requests are queued until a call is made to the func-
       tion on the same or a different device.

   IInnppuutt
       socket identifier.  resource manager key  that  identifies
       this device.  Refer to for more information.

   OOuuttppuutt
       None.

UUssaaggee
       /*(L<hpsl_suspend_io/*(L>  also  updates  the  driver  and
       socket state members of the socketInfo structure.

       This function activates the CFG_SUSPEND subfunction of the
       driver's entry point routine.

RReettuurrnn vvaalluueess
       See

RReeffeerreenncceess
EExxaammppllee
       extern HpslSocketInfo_t **hpslSocketInfo;

       rm_key_t   rmKey=getResmgrKey();   int   socketIx=getSock-
       etIndex();

       if(isSocketIndexValid(socketIx))  &&  isKeyConnected(sock-
       etIx, rmKey)) {
               if(hpsl_suspend_io(hpslSocketInfo[socketIx],
       rmKey)!=HPSL_SUCCESS)
               {
                       localErrorHandler();
               } }

                                                                1

