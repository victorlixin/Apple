

hpsl_cleanup(3hpsl)                           hpsl_cleanup(3hpsl)

SSyynnooppssiiss
       cc -lhpsl

       #include <hpsl.h>

       void hpsl_cleanup(void)

DDeessccrriippttiioonn
       /*(L<hpsl_cleanup/*(L>  performs  any  required  clean  up
       operations such as freeing up memory that was allocated by
       the HPSL.

   IInnppuutt
       None.

   OOuuttppuutt
       None.

UUssaaggee
       /*(L<hpsl_cleanup/*(L>  should  be  called by the hot plug
       application before exiting and before  reinitializing  the
       data structures.  Refer to for more information.

RReettuurrnn vvaalluueess
       See

RReeffeerreenncceess
EExxaammppllee
       extern  uint      hpslSocketCount; extern HpslSocketInfo_t
       **hpslSocketInfo;

       uint    evCount=0; int     socketIx=0;

       if (hpsl_check_async_events(&evCount)!=HPSL_SUCCESS) {
               localErrorHandler(): } if (evCount==EV_RESCAN) {
               hpsl_cleanup();
               localReset(); } else {
               for (socketIx=0;  socketIx<hpslSocketCount;  sock-
       etIx++)
               {
                       if        (hpslSocketInfo[socketIx]->sock-
       etEvent==NEW_EVENT)
                       {
                               localSocketRedisplay(socketIx);
                               hpslSocketInfo[socketIx]->sock-
       etEvent=NO_CHANGE;
                               evCount--;
                       }
               }
               if (evCount!=0)
               {
                       localDisplayHandler("Event    count   mis-
       matched");
                       localErrorHandler();

                                                                1

hpsl_cleanup(3hpsl)                           hpsl_cleanup(3hpsl)

               } }

                                                                2

