

hpsl_match_devdrv(3hpsl)                 hpsl_match_devdrv(3hpsl)

SSyynnooppssiiss
       cc -lhpsl

       #include <hpsl.h>

       int hpsl_match_devdrv(rm_key_t rmKey, char *drvname)

DDeessccrriippttiioonn
       /*(L<hpsl_match_devdrv/*(L>   checks   the   compatibility
       between a driver and a particular device.

   IInnppuutt
       resmgr key of the device.  name of driver.  Refer  to  for
       more information.

   OOuuttppuutt
       None.

RReettuurrnn vvaalluueess
       See

RReeffeerreenncceess
EExxaammppllee
       Resume_Menu_Handler() {

            int  index;             HpslSocketInfo_t**SocketInfo;
            uint suspDrvCount=0;
            SuspendedDrvInfoPtr_t    suspDrvInfo=NULL;

            ...                   Get_SelectedSocket(SocketInfo);
            index=Get_SocketIndex();

            if  (hpsl_get_suspended_drvlist(&suspDrvInfo,  &susp-
       DrvCount)!=HPSL_SUCCESS)       {            LocalErrorHan-
       dler();      }

            for(i=0;i<suspendDrvCount;i++)                      {
                 if(hpsl_match_devdrv(Socket-
       Info.devList[index].rmKey,     suspDrvInfo[i].drvInfo.drv-
       Name)==HPSL_SUCCESS)                                     {
                      Insert_To_List_Of_Avail-
       able_Drivers_To_Resume_On_This_Device();                 }
            }      ...  }

                                                                1

