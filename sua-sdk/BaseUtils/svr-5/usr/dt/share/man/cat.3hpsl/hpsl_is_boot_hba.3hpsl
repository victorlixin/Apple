

hpsl_is_boot_hba(3hpsl)                   hpsl_is_boot_hba(3hpsl)

SSyynnooppssiiss
       cc -lhpsl

       #include <hpsl.h>

       int hpsl_is_boot_hba(rm_key_t rmKey)

DDeessccrriippttiioonn
       /*(L<hpsl_is_boot_hba/*(L>  checks whether the device is a
       boot/root controller.

   IInnppuutt
       resmgr key associated with the device.  Refer to for  more
       information.

   OOuuttppuutt
       None.

RReettuurrnn vvaalluueess
       See

RReeffeerreenncceess
EExxaammppllee
       if      (hpsl_is_boot_hba(resmgrKey)==HPSL_SUCCESS)      {
            Print("Suspend request denied: resmgr key %d  respre-
       sents   a   boot   hba",   resmgrKey);   }  if  (hpsl_sus-
       pend_io(hpslSocketInfo[socketIx],    resmgrKey)!=HPSL_SUC-
       CESS) {      Print("Suspend IO"); }

                                                                1

