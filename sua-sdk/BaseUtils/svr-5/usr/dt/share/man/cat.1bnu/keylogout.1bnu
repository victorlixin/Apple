

keylogout(1bnu)                                   keylogout(1bnu)

SSyynnooppssiiss
       keylogout [-f]

DDeessccrriippttiioonn
       The  keylogout command unsets the user's secret key, which
       is stored in the database.

UUssaaggee
       If you are logged in as root, use keylogout  -f  to  unset
       your  (root)  secret key.  Note that using keylogout -f as
       root will break all servers that  use  the  secure  Remote
       Procedure Call (RPC).

RReeffeerreenncceess

                                                                1

