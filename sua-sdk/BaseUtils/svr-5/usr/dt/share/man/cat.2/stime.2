

stime(2)                                                 stime(2)

SSyynnooppssiiss
       #include <unistd.h>

       int stime(const time_t *tp);

DDeessccrriippttiioonn
       stime  sets  the  system's  idea of the time and date.  tp
       points to the value of time as measured  in  seconds  from
       00:00:00 UTC January 1, 1970.

   RReettuurrnn vvaalluueess
       On success, stime returns 0.  On failure, stime returns -1
       and sets errno to identify the error.

   EErrrroorrss
       In the following conditions, stime fails  and  sets  errno
       to:  The  calling  process  does  not have the appropriate
       privilege (P_SYSOPS).

RReeffeerreenncceess

                                                                1

