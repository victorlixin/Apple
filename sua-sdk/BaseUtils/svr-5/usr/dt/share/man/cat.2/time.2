

time(2)                                                   time(2)

SSyynnooppssiiss
       #include <sys/types.h> #include <time.h>

       time_t time(time_t *tloc);

DDeessccrriippttiioonn
       time  returns  the value of time in seconds since 00:00:00
       UTC, January 1, 1970.

       If tloc is non-zero, the return value is  also  stored  in
       the location to which tloc points.

   RReettuurrnn vvaalluueess
       On  success,  time  returns  the  number  of seconds since
       00:00:00 UTC, January 1, 1970.  On failure,  time  returns
       (time_t)-1 and sets errno to identify the error.

RReeffeerreenncceess
NNoottiicceess
       time fails and its actions are undefined if tloc points to
       an illegal address.

                                                                1

