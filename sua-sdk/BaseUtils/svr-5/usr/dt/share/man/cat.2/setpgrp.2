

setpgrp(2)                                             setpgrp(2)

SSyynnooppssiiss
       #include <sys/types.h> #include <unistd.h>

       pid_t setpgrp(void);

DDeessccrriippttiioonn
       If  the  calling  process is not already a session leader,
       setpgrp sets the process group ID and session  ID  of  the
       calling  process to the process ID of the calling process,
       and releases the calling process's controlling terminal.

   RReettuurrnn vvaalluueess
       On success, setpgrp returns the value of the  new  process
       group ID.

RReeffeerreenncceess
NNoottiicceess
       setpgrp will be phased out in favor of the function.

   CCoonnssiiddeerraattiioonnss ffoorr tthhrreeaaddss pprrooggrraammmmiinngg
       This  ID  number is an attribute of the containing process
       and is shared by sibling threads.

                                                                1

