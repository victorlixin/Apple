

_lwp_setprivate(2)                             _lwp_setprivate(2)

SSyynnooppssiiss
       #include <sys/lwp.h>

       void _lwp_setprivate(void *buffer);

   PPaarraammeetteerrss
       value of the LWP-specific pointer

DDeessccrriippttiioonn
       _lwp_setprivate  sets  the  lightweight process (LWP)-spe-
       cific descriptor pointer to buffer.

   RReettuurrnn vvaalluueess
       _lwp_setprivate  sets  the  value  of   the   LWP-specific
       pointer.

   EErrrroorrss
       None.   Errors  will  be  detected  only when subsequently
       using the value of buffer.

UUssaaggee
       The per-LWP pointer value is intended  to  be  used  as  a
       pointer to an implementation-defined datum.

RReeffeerreenncceess
NNoottiicceess
       Lightweight  processes  (LWPs) are internal interfaces and
       are subject to change.  Their use should be avoided.

                                                                1

