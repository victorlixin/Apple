

mkmswtab(1Mha)                                     mkmswtab(1Mha)

SSyynnooppssiiss
       /sbin/mkmswtab -vn -i msw-device-name interface-name ...

DDeessccrriippttiioonn
       The  mkmswtab command is provided to assist in the genera-
       tion of the mswtab file. This file contains  entries  that
       describe  the MAC switch driver virtual interfaces and the
       physical interfaces that comprise them.

       The names of the MSW virtual interface  and  its  physical
       interfaces  may be specified on the command line, as stan-
       dard input, or entered through interactive prompting.

OOppttiioonnss
       Specifies  the  next  parameter  as  the  msw-device-name.
       Enables verbose output.  Prints the new mswtab to standard
       output and exits without replacing the file /etc/mswtab.

EExxaammppllee
       mkmswtab -i /dev/msw_0 /dev/ee16_0 /dev/ee16_1

FFiilleess
       /etc/mswtab

RReeffeerreenncceess

                                                                1

