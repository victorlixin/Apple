

putpwent(3C)                                         putpwent(3C)

SSyynnooppssiiss
       #include <pwd.h>

       int putpwent (const struct passwd *p, FILE *f);

DDeessccrriippttiioonn
       putpwent  is  the  inverse  of Given a pointer to a passwd
       structure created by getpwent (or getpwuid  or  getpwnam),
       putpwent  writes a line on the stream f, which matches the
       format of /etc/passwd.

   RReettuurrnn vvaalluueess
       putpwent returns non-zero if an error was detected  during
       its operation; otherwise, it returns zero.

RReeffeerreenncceess

                                                                1

