

iconv_close(3C)                                   iconv_close(3C)

SSyynnooppssiiss
       #include <iconv.h>

       int iconv_close(iconv_t cd);

DDeessccrriippttiioonn
       iconv_close  deallocates the conversion descriptor cd, and
       all data contained within it.  If  a  file  descriptor  or
       similar  facility  is  used  within  the descriptor, it is
       closed and deallocated.

   RReettuurrnn vvaalluueess
       If iconv_close encounters  no  errors,  it  returns  zero.
       Otherwise -1 is returned, and errno is set.

   EErrrroorrss
       cd may be an invalid conversion descriptor.

RReeffeerreenncceess

                                                                1

