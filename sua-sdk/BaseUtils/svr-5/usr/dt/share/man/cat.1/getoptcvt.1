

getoptcvt(1)                                         getoptcvt(1)

SSyynnooppssiiss
       /usr/lib/getoptcvt [-b] file

DDeessccrriippttiioonn
       The command reads the shell script in file, converts it to
       use instead of and writes the results on the standard out-
       put.   Make  the  converted  script  portable  to  earlier
       releases of the
        system;  that is, modifies the shell script  in  file  so
       that  when  the  resulting  shell  script  is executed, it
       determines at run time whether to invoke or

       So all new commands will  adhere  to  the  command  syntax
       standard  described  in  they should use or to parse posi-
       tional parameters and check for options that are valid for
       that command.

RReeffeerreenncceess

                                                                1

