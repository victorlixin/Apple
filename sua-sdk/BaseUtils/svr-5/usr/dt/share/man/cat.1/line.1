

line(1)                                                   line(1)

SSyynnooppssiiss
       line

DDeessccrriippttiioonn
       line  copies  one line (up to a newline) from the standard
       input and writes it on the standard output.  It returns an
       exit  code  of  1 on EOF and always prints at least a new-
       line.  It is often used within shell files  to  read  from
       the user's terminal.

       The  LC_CTYPE  environment variable defines the processing
       of the codesets used in the input file.  (See LANG in

RReeffeerreenncceess

                                                                1

