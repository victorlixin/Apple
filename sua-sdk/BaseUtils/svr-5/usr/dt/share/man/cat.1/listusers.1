

listusers(1)                                         listusers(1)

SSyynnooppssiiss
       listusers [-g groups] [-l logins]

       listusers [-h] [-v]

DDeessccrriippttiioonn
       Executed  without any options, this command lists all user
       logins sorted by login.  The output shows the login ID and
       the  account  field value in /etc/passwd.  Print the valid
       security levels of the invoking user.  Valid only  if  the
       Enhanced  Security  Utilities  are  installed.   Print the
       default security level of the invoking user.   Valid  only
       if  the  Enhanced Security Utilities are installed.  Lists
       all user logins belonging to group, sorted by login.  Mul-
       tiple  groups  can be specified as a comma-separated list.
       Lists the user login or logins specified by logins, sorted
       by  login.   Multiple  logins can be specified as a comma-
       separated list.

NNoottiicceess
       A user login is defined as having a UID of 100 or greater.

       The  -l  and -g options can be combined.  User logins will
       only be listed once, even if they belong to more than  one
       of the selected groups.

                                                                1

