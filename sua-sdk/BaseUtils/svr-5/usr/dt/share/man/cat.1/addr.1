

addr(1)                                                         addr(1)

  aaddddrr

  NNAAMMEE

    addr - displays IP address in network and presentation formats

  SSYYNNOOPPSSIISS

    addr [[-4 || -6]] -n "hexstring" || -p address

  DDEESSCCRRIIPPTTIIOONN

    The aaddddrr(1) utility accepts an IPv4 or IPv6 network address in either
    network (hexadecimal) or presentation format and displays the address in
    both formats.

  OOPPTTIIOONNSS

    --44
        Indicates that the address is an IPv4 address. If this option and the
        --66 option are omitted, the address is assumed to be IPv4.

    --66
        Indicates that the address is an IPv6 address. If this option and the
        --44 option are omitted, the address is assumed to be IPv4.
    --nn "hexstring"
        Specifies a network address in network format. The address must be
        enclosed in quotation marks (").
    --pp address
        Specifies a network address in presentation format.

  DDIIAAGGNNOOSSTTIICCSS

    The aaddddrr(1) utility returns 0 for success and >0 if an error occurs.

  EEXXAAMMPPLLEESS

    $$ aaddddrr --44 --pp 112277..00..00..11
    Input: "127.0.0.1"
    Network: [af2 len4] 7f 00 00 01
    Presentation: "127.0.0.1"

    $$ aaddddrr --44 --nn ""77ff 0000 0000 0011""
    Input: "7f 00 00 01"
    Network: [af2 len4] 7f 00 00 01
    Presentation: "127.0.0.1"

