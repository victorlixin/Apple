

mkcatdecl(1)                                         mkcatdecl(1)

SSyynnooppssiiss
       mkcatdecl  [-i moduleIdfile] [-n] [-g] [-c] [-m modid ...]
       msgSpecFile.msg

DDeessccrriippttiioonn
       mkcatdecl program generates the message declaration  files
       and message catalogs from a message specification file.

   AArrgguummeennttss
       This flag specifies the path to module declaration file If
       this option is not  specified,  and  environment  variable
       MODULEIDDECL  is checked for the location of the file.  if
       specified, a message catalog source  file  (.gen)  is  not
       generated.  if specified, no message declaration files are
       generated, only a .gen file is generated.   if  specified,
       all  message  declaration  files  will be generated in the
       current directory, regardless of the path specified in the
       $moduleset directive.  If specified, only the message dec-
       laration files for the listed module  ids  are  generated.
       This  option may be specified multiple times.  This is the
       message specification file, it must end with a .msg exten-
       sion.

FFiilleess
       *.gen

RReeffeerreenncceess

                                                                1

