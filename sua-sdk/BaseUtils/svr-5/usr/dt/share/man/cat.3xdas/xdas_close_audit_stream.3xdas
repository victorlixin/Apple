

xdas_close_audit_stream(3xdas)     xdas_close_audit_stream(3xdas)

SSyynnooppssiiss
       cc  [flag  ...]  file  ...   -Kthread -leels [library] ...
       #include <sys/xdas.h>

       OM_uint32     xdas_close_audit_stream(           OM_uint32
       *minor_status,        const   xdas_audit_ref_t   *das_ref,
            const xdas_audit_stream_t *audit_stream_ref);

DDeessccrriippttiioonn
       The xdas_close_audit_stream(3xdas) function is a member of
       the Basic XDAS Conformance class.

       xdas_close_audit_stream(3xdas)  closes  an  audit  stream,
       previously  opened  for   reading,   identified   by   the
       audit_stream_ref handle.

       An  audit  stream  is  no longer valid for use in any XDAS
       function once it has been closed using  any  copy  of  the
       handle  that refers to it.  Any storage associated with an
       audit stream is reclaimed when it is closed.   The  caller
       must possess the XDAS_AUDIT_READ authority.

       On    success,    xdas_close_audit_stream(3xdas)   returns
       XDAS_S_COMPLETE.

   AArrgguummeennttss
       (Output) Provides further information  when  the  function
       returns  XDAS_S_FAILURE.   (Input)  A  handle to the audit
       service obtained from a previous call to (Input) A  handle
       to the audit stream reference to be closed.

RReettuurrnn vvaalluueess
       The  caller  does not possess the correct authority.  Suc-
       cessful completion.  An implementation-specific  error  or
       failure has occurred, such as missing required parameters,
       or a  malloc  failure.   The  specified  audit  stream  is
       invalid.  The handle to the audit service is invalid.

RReeffeerreenncceess

                                                                1

