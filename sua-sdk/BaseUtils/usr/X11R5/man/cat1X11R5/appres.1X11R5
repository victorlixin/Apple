appres(1X11R5)                                           appres(1X11R5)

  aapppprreess

  NNAAMMEE

    appres - list application resource database

  SSYYNNOOPPSSIISS

    appres [[class [[instance]]]] [[-1]] [[toolkitoptions]]

  DDEESSCCRRIIPPTTIIOONN

    The aapppprreess(1X11R5) program prints the resources seen by an application (or
    subhierarchy of an application) with the specified class and instance
    names. You can use it to determine which resources a particular program
    will load. For example:

    %% appres XTerm

    will list the resources that any _xx_tt_ee_rr_mm(1X11R5) program will load. If no
    application class is specified, the class --AAppppRReessTTeesstt-- is used.

    To match a particular instance name, specify an instance name explicitly
    after the class name, or use the normal Xt toolkit option. For example:

    %% appres XTerm myxterm

    or

    %% appres XTerm -name myxterm

    To list resources that match a subhierarchy of an application, specify
    hierarchical class and instance names. The number of class and instance
    components must be equal, and the instance name should not be specified
    with a toolkit option. For example:

    %% appres Xman.TopLevelShell.Form xman.topBox.form

    will list the resources of widgets of _xx_mm_aa_nn(1X11R5) topBox hierarchy. To
    list only the resources matching a specific level in the hierarchy, use
    the --11 (the number one) option. In the following example:

    %% appres XTerm.VT100 xterm.vt100 -1

    will list the resources matching the xxtteerrmm(1X11R5) vt100 widget.

  SSEEEE AALLSSOO

    _X

    _x_r_d_b

    _l_i_s_t_r_e_s

  CCOOPPYYRRIIGGHHTT

    Copyright 1989, Massachusetts Institute of Technology.
    See XX(5X11R5) for a full statement of rights and permissions.

  AAUUTTHHOORR

    Jim Fulton, MIT X Consortium

