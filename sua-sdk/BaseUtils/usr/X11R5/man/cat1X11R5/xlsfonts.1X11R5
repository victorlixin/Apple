xlsfonts(1X11R5)                                       xlsfonts(1X11R5)

  xxllssffoonnttss

  NNAAMMEE

    xlsfonts - server font list displayer for X

  SSYYNNOOPPSSIISS

    xlsfonts [[-options ...]] [[-fn pattern]]

  DDEESSCCRRIIPPTTIIOONN

    The xxllssffoonnttss(1X11R5) utility lists the fonts that match the given pattern.
    The wildcard character "*" can be used to match any sequence of characters
    (including none), and "?" to match any single character. If no pattern is
    given, "*" is assumed.

    The "*" and "?" characters must be quoted to prevent them from being
    expanded by the shell.

  OOPPTTIIOONNSS
    --ddiissppllaayy host:dpy

        This option specifies the X server to contact.
    --ll[ll[ll]]
        Generate medium, long, and very long listings, respectively, for each
        font.

    --mm
        Long listings should also print the minimum and maximum bounds of each
        font.

    --CC
        Use multiple columns. This is the same as --nn.

    --11
        Use a single column for listings. This is the same as --nn 11.
    --ww width
        Use width characters to determine how many columns to print. The
        default is 79.
    --nn columns
        Use columns columns when displaying the output. By default, it will
        attempt to fit as many columns of font names into the number of
        character specified by --ww width.

    --uu
        Leave the output unsorted.

    --oo
        Do an OOppeennFFoonntt (and QQuueerryyFFoonntt, if appropriate) rather than a
        LLiissttFFoonnttss. This is useful if either LLiissttFFoonnttss or LLiissttFFoonnttssWWiitthhIInnffoo
        fails to list a known font (as is the case with some scaled font
        systems).

  EENNVVIIRROONNMMEENNTT VVAARRIIAABBLLEESS

    DISPLAY
        Contains the default host and display to use.

  BBUUGGSS

    Doing xxllssffoonnttss --ll can tie up your server for a very long time. This is
    really a bug with single-threaded non-preemptable servers, not with this
    program.

  CCOOPPYYRRIIGGHHTT

    Copyright 1988, Massachusetts Institute of Technology.

    See _X(5X11R5) for a full statement of rights and permissions.

  AAUUTTHHOORR

    Mark Lillibridge, MIT Project Athena; Jim Fulton, MIT X Consortium; Phil
    Karlton, SGI

  SSEEEE AALLSSOO

    _X(5X11R5)

    _x_s_e_t(1X11R5)

    _x_f_d(1X11R5)

    _X_s_e_r_v_e_r

