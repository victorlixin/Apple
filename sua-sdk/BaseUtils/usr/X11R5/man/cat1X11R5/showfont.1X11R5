showfont(1X11R5)                                       showfont(1X11R5)

  sshhoowwffoonntt

  NNAAMMEE

    showfont - font dumper for X font server

  SSYYNNOOPPSSIISS

    showfont [[-options ...]] [[-fn pattern]]

  DDEESSCCRRIIPPTTIIOONN

    The sshhoowwffoonntt(1X11R5) utility displays data about the font that matches the
    given pattern. The wildcard character "*" can be used to match any
    sequence of characters (including none), and "?" to match any single
    character. If no pattern is given, "*" is assumed.

    The "*" and "?" characters must be quoted to prevent them from being
    expanded by the shell.

    The sshhoowwffoonntt(1X11R5) utility uses the following options:
    --sseerrvveerr host::port
        This option specifies the X font server to contact.

    --ll
        This option indicates that the bit order of the font is LSBFirst
        (least significant bit first).

    --mm
        This option indicates that the bit order of the font is MSBFirst (most
        significant bit first).

    --LL
        This option indicates that the byte order of the font is LSBFirst
        (least significant byte first).

    --MM
        This option indicates that the byte order of the font is MSBFirst
        (most significant byte first).

    --eexxtteennttss__oonnllyy
        This option indicates that only the font's extents should be
        displayed.
    --ssttaarrtt #
        This option indicates the start of the range of the characters to
        display (# is a number).
    --eenndd #
        This option indicates the end of the range of the characters to
        display, (# is a number).
    --uunniitt #
        This option specifies the scan-line unit of the font (1, 2, 4, or 8).
        (# is a number).
    --ppaadd #
    --uunniitt #
        This option specifies the scan-pad unit of the font (1, 2, 4, or 8).
        (# is a number).
    --bbiittmmaapp__ppaadd #
        This option specifies the bitmap padding unit of the font (0, 1, or 2,
        where 0 is ImageRectMin, 1 is ImageRectMaxWidth and 2 is
        ImageRectMaxWidth).

  EENNVVIIRROONNMMEENNTT VVAARRIIAABBLLEESS

    FONTSERVER
        To get the default host and port to use.

  SSEEEE AALLSSOO

    _x_l_s_f_o_n_t_s(1X11R5)

