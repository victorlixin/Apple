xmkmf(1X11R5)                                             xmkmf(1X11R5)

  xxmmkkmmff

  NNAAMMEE

    xmkmf - create a Makefile from an Imakefile

  SSYYNNOOPPSSIISS

    xmkmf [[-a]] [[topdir [[curdir]] ]]

  DDEESSCCRRIIPPTTIIOONN

    The xxmmkkmmff(1X11R5) command is the usual way to create a MMaakkeeffiillee from an
    IImmaakkeeffiillee shipped with third-party software.

    When invoked with no arguments in a directory containing an IImmaakkeeffiillee,, the
    iimmaakkee(1X11R5) program is run with arguments appropriate for your system
    (configured into xxmmkkmmff(1X11R5) when X was built) and generates a MMaakkeeffiillee.

    When invoked with the --aa option, xxmmkkmmff(1X11R5) builds the MMaakkeeffiillee in the
    current directory, and then automatically executes "make Makefiles" (in
    case there are subdirectories), "make includes", and "make depend" for
    you. This is the usual way to configure software that is outside the MIT X
    build tree.

    If working inside the MIT X build tree (unlikely unless you are an X
    developer, and even then this option is never really used), the topdir
    argument should be specified as the relative path name from the current
    directory to the top of the build tree. Optionally, curdir can be
    specified as a relative path name from the top of the build tree to the
    current directory. It is necessary to supply curdir if the current
    directory has subdirectories, or the MMaakkeeffiillee will not be able to build
    the subdirectories. If a topdir is given, xxmmkkmmff(1X11R5) assumes nothing is
    installed on your system and looks for files in the build tree instead of
    using the installed versions.

  SSEEEE AALLSSOO

    _i_m_a_k_e(1X11R5)

