inetd.conf(5)                                             inetd.conf(5)

  iinneettdd..ccoonnff

  NNAAMMEE

    inetd.conf - inetd configuration file

  DDEESSCCRRIIPPTTIIOONN

    The iinneettdd..ccoonnff file is the configuration file for the _i_n_e_t_d(1) daemon. The
    file contains the list of Internet-related services that iinneettdd(1) can
    invoke when it receives a request from an Internet client.

    Each line of the iinneettdd..ccoonnff file has configuration information for one
    service. The format is:

    [ccaammpp--]service_name   socket_type   protocol   {wwaaiitt|nnoo wwaaiitt}   user
    {daemon|iinntteerrnnaall}   arguments

    Pound signs (##) are used to mark comments.

    Below is a description of each field in the iinneettdd configuration file.
    [ccaammpp--]
        Disables the indicated service, but blocks access to the default port
        used by the service. Use this to prevent a trojan horse program from
        using the port.

    service_name
        The name of a service listed in the %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//
        SSeerrvviicceess file. Remote procedure call (RPC) service names are followed
        by a slash and the version number or range of version numbers.
        Internal service names must be the first entry from the %%wwiinnddiirr%%//
        ssyysstteemm3322//ddrriivveerrss//eettcc//SSeerrvviicceess file.

    socket_type
        One of the following: ssttrreeaamm, a stream socket; ddggrraamm, a datagram
        socket; rraaww, a raw socket; rrddmm, a reliably delivered message socket;
        or sseeqqppaacckkeett, a sequenced packet socket.

    protocol
        The name of a protocol listed in the %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//
        pprroottooccooll file. For RPC services, use RPC followed by slash (//) and the
        name of the protocol; for example, rrppcc//uuddpp.
    {wwaaiitt|nnoo wwaaiitt}
        Multi-threaded servers, datagram servers that connect to peers and
        free a socket for iinneettdd to receive more packets on the socket, should
        use nnoowwaaiitt. Single-threaded servers, datagram servers that do not
        release the socket until a time-out occurs, must be set to wwaaiitt.
        The ttffttppdd(1) daemon, a datagram server that establishes separate
        pseudo connections, must be set to wwaaiitt. Although it appears to need a
        nnoowwaaiitt entry, its forking behavior can lead to a race condition unless
        it is set to wwaaiitt.

    user
        The user under which the server program will run. This allows servers
        to run with less access than the root account.
    {daemon|iinntteerrnnaall}
        The path name to the server program that iinneettdd starts when asked to
        perform a requested service. Contains the value iinntteerrnnaall if iinneettdd
        provides the service internally.

    arguments
        The argument(or arguments) that should be passed to the server
        program. You must include the entire command line (the server program
        name is passed as aarrggvv[[00]]). If the server program is internal, you can
        leave this blank.

  EEXXAAMMPPLLEESS

    The following line is an example from the Subsystem for UNIX-based Applications iinneettdd..ccoonnff file.

    ftp   stream   tcp   nowait   NULL   /usr/sbin/in.ftpd   in.ftpd -l

    ftp
        Service name. Listed in %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//SSeerrvviicceess.

    stream
        Socket type.

    tcp
        Protocol. Listed in %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//pprroottooccooll.

    nowait
        Set to nnoowwaaiitt because ffttpp is a multi-threaded datagram server

    NULL
        The user associated with this server program.

    /usr/sbin/in.ftpd
        The path and file name of the daemon or server program.

    in.ftpd -l
        The server program arguments. The argument iinn..ffttppdd is argv[0] and --
        ll is argv[1].

  FFIILLEESS

    //eettcc//iinneettdd..ccoonnff
        The iinneettdd(1) configuration file.

    %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//SSeerrvviicceess
        Provides service name-to-port identification resolution for Windows
        Sockets applications. The list of valid services that can be used by
        iinneettdd.

    %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//pprroottooccooll
        Provides protocol name-to-protocol identification resolution for
        Windows Sockets applications. The list of valid protocols that can be
        used by iinneettdd.

  NNOOTTEESS

    To prevent tampering, if the permissions protecting iinneettdd..ccoonnff file allow
    the file to be written by anyone other than the file's owner and group,
    the file is ignored and an error is logged using ssyyssllooggdd.

  SSEEEE AALLSSOO

    _i_n_e_t_d(1)

