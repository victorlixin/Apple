services(5)                                                 services(5)

  sseerrvviicceess

  NNAAMMEE

    services - service name data base

  DDEESSCCRRIIPPTTIIOONN

    The sseerrvviicceess file contains information regarding the known services
    available in the Internet. For each service a single line should be
    present with the following information:

    official_service_name   port_number/protocol_name   [alias(es)]
    [#comment]

    Items are separated by any number of blanks, tab characters, or both. The
    port_number and protocol_name are considered a single item; a slash (//) is
    used to separate them. A pound symbol (##) indicates the beginning of a
    comment; subsequent characters up to the end of the line are not
    interpreted by the routines that search the file.

    Instead of the UNIX eettcc//sseerrvviicceess file, you have the Windows
    %%wwiinnddiirr%%\\ssyysstteemm3322\\ddrriivveerrss\\eettcc\\sseerrvviicceess file. The %windir% variable
    represents your Windows installation directory.

  FFIILLEESS

    %%wwiinnddiirr%%//ssyysstteemm3322//ddrriivveerrss//eettcc//sseerrvviicceess
        Provides service name-to-port ID resolution for Windows Sockets
        applications.

