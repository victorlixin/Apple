break(1)                                                       break(1)

  bbrreeaakk

  NNAAMMEE

    break - exit a loop

  SSYYNNOOPPSSIISS

  CC sshheellll

    break

  KKoorrnn sshheellll

    break [[level]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    The bbrreeaakk(1) command causes execution to resume after the eenndd of the
    nearest enclosing _ff_oo_rr_ee_aa_cc_hh(1) or _ww_hh_ii_ll_ee(1) loop. The remaining commands on
    the current line are executed. Multilevel breaks are thus possible by
    writing them all on one line.

  KKoorrnn sshheellll

    The bbrreeaakk(1) command exits the innermost _ff_oo_rr(1), _ss_ee_ll_ee_cc_tt(1), _uu_nn_tt_ii_ll(1), or
    _ww_hh_ii_ll_ee(1) loop specified by level. If level is not specified, the first
    (that is, the innermost) loop is the default.

