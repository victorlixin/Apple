head(1)                                                         head(1)

  hheeaadd

  NNAAMMEE

    head - display first num lines or bytes of a file

  SSYYNNOOPPSSIISS

    head [[-b num]] [[-c num]] [[-k num]] [[-n num]] [[file...]

  DDEESSCCRRIIPPTTIIOONN

    This filter displays the first num lines of each of the specified files,
    or of the standard input if no files are specified.

    If more than one option (--bb, --cc, --kk, or --nn) is specified, the hheeaadd(1)
    command ignores all options except the last one. If no option is
    specified, the hheeaadd(1) command uses the default option --nn 1100.

    If more than a single file is specified, each file is preceded by a header
    consisting of the string "==> XXX <==" where XXX is the name of the file.

  OOPPTTIIOONNSS
    --bb num
        Print out first num blocks (512 bytes).
    --cc num
        Print out first num bytes.
    --kk num
        Print out first num kilobytes (1024 bytes).
    --nn num
        Print the first num lines of each input file. The num option must be a
        positive integer. If num is omitted, it defaults to 10.

  DDIIAAGGNNOOSSTTIICCSS

    The hheeaadd(1) utility exits 0 on success, and >0 if an error occurs.

  SSEEEE AALLSSOO

    _t_a_i_l(1)

