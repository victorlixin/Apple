fg(1)                                                             fg(1)

  ffgg

  NNAAMMEE

    fg - run jobs in the foreground

  SSYYNNOOPPSSIISS

  CC sshheellll

    fg [[%job ......]]

  KKoorrnn sshheellll

    fg [[job ......]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    The ffgg(1) command brings the specified jobs (or, without arguments, the
    current job) into the foreground, continuing each if it is stopped. The
    job argument can be a number, a string, '', '%', '+' or '-' as described
    under the Jobs section in _tt_cc_ss_hh(1). See also the rruunn--ffgg--eeddiittoorr editor
    command in _tt_cc_ss_hh(1).

  KKoorrnn sshheellll

    The ffgg(1) command resumes the specified job(s) in the foreground. If no
    jobs are specified, %%++ is assumed. This command is only available on
    systems on which job control is enabled. See the Job Control section in
    _kk_ss_hh(1) for more information.

