infotocap(1)                                               infotocap(1)

  iinnffoottooccaapp

  NNAAMMEE

    infotocap - convert a terminfo description into a termcap description

  SSYYNNOOPPSSIISS

    infotocap [[-hc]] [[-v[[n]]]] [[-e names]] [[-CILNRTrsw1]] source-file

  DDEESSCCRRIIPPTTIIOONN

    The iinnffoottooccaapp(1) utility looks in file for _t_e_r_m_i_n_f_o(5) descriptions. For
    each one found, an equivalent _t_e_r_m_c_a_p(5) description is written to
    standard output. The _t_e_r_m_i_n_f_o(5) uussee capabilities are translated directly
    to _t_e_r_m_c_a_p(5) ttcc capabilities.

    --11
        Format translation output one capability per line

    --CC
        Translate entries to termcap source form

    --II
        Translate entries to terminfo source form

    --LL
        Translate entries to full terminfo source form

    --NN
        Disable smart defaults for source translation

    --RR
        Restrict translation to given terminfo/termcap version

    --TT
        Remove size restrictions on compiled description

    --cc
        Check only; validate input without compiling or translating
    --ee names
        Translate/compile only entries named by comma-separated list
    --oo dir
        Set output directory for compiled entry writes

    --rr
        Force resolution of all use entries in source translation

    --ss
        Print summary statistics
    --vv[n]
        Set verbosity level
    --ww[n]
        Set format width for translation output

    source-file
        File to translate or compile

  FFIILLEESS

    //uussrr//sshhaarree//tteerrmmiinnffoo
        Compiled terminal description database.

  NNOOTTEESS

    This utility is actually a link to _t_i_c(1), running in --CC mode. You can use
    other _t_i_c(1) options, such as --ff and --xx.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _t_i_c(1)

    _i_n_f_o_c_m_p(1)

    _t_e_r_m_i_n_f_o(5)

