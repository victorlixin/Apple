unlimit(1)                                                   unlimit(1)

  uunnlliimmiitt

  NNAAMMEE

    unlimit - remove limitations on resources

  SSYYNNOOPPSSIISS

    unlimit [[-h]] [[resource]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a C-shell built-in command.

    The uunnlliimmiitt(1) command removes the limitation on resource or, if no
    resource is specified, all resource limitations. With --hh, the
    corresponding hard limits are removed. Only a user with appropriate
    privileges can do this.

