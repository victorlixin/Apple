mkfifo(1)                                                     mkfifo(1)

  mmkkffiiffoo

  NNAAMMEE

    mkfifo - make fifos

  SSYYNNOOPPSSIISS

    mkfifo [[-m mode]] fifo_name ...

  DDEESSCCRRIIPPTTIIOONN

    The mmkkffiiffoo(1) utility creates the first in/first outs (FIFOs) requested in
    the order specified, using mode rrww--rrww--rrww-- (0666).

    The options are as follows:

    --mm
        Set the file permission bits of the created fifo to the specified
        mode. The argument can be in any of the formats specified to the
        cchhmmoodd(1) command. If a symbolic mode is specified, the operation
        characters plus (++) and minus (--) are interpreted relative to an
        initial mode of aa==rrww.

    The mmkkffiiffoo(1) utility requires write permission in the parent directory.

  DDIIAAGGNNOOSSTTIICCSS

    The mmkkffiiffoo(1) utility exits with 0 if successful, and >0 if an error
    occurred.

  SSEEEE AALLSSOO

    _m_k_d_i_r(1)

    _r_m(1)

