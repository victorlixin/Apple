unalias(1)                                                   unalias(1)

  uunnaalliiaass

  NNAAMMEE

    unalias - remove aliases

  SSYYNNOOPPSSIISS

  CC sshheellll

    unalias pattern

  KKoorrnn sshheellll

    unalias [[-adt]] [[name1 ......]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    The uunnaalliiaass(1) command removes all aliases whose names match pattern;
    'unalias *' thus removes all aliases. It is not an error if no aliases are
    removed.

  KKoorrnn sshheellll

    The aliases for the given names are removed. If the --aa option is used, all
    aliases are removed. If the --tt or --dd options are used, the indicated
    operations are carried out on tracked or directory aliases, respectively.

