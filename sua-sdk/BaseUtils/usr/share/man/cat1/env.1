env(1)                                                           env(1)

  eennvv

  NNAAMMEE

    env - set environment for command invocation

  SSYYNNOOPPSSIISS

    env [[-i||-l usr||-w user]] [[-u name]] ......
        [--]] [[-]] [[name=value]]... [[command]]

  DDEESSCCRRIIPPTTIIOONN

    The eennvv(1) utility invokes the command after first modifying the
    environment as specified by the name=value operands. If eennvv(1) is invoked
    without arguments, it writes the current environment to standard output.
    If it is invoked with out a command argument, it writes to standard output
    the environment set by the other operands.

  OOPPTTIIOONNSS

    --
        Ignore the inherited environment.
    --cc user
        Replace the current environment with the standard ccrroonn(1) login
        environment for the specified user.

    --ii
        Ignore the current environment and replace it with an empty
        environment.
    --ll user
        Replace the current environment with the standard SUA login
        environment for the specified user.
    --uu name
        Unset the specified variable.
    --ww user
        Replace the current environment with the standard Windows login
        environment for the specified user.

  DDIIAAGGNNOOSSTTIICCSS

    The eennvv(1) utility exits 0 on success, and >0 if an error occurs. It exits
    with 126 if the command could be found but not invoked, and 127 if command
    could not be found.

  SSEEEE AALLSSOO

    _k_s_h(1)

    _s_h(1)

