type(1)                                                         type(1)

  ttyyppee

  NNAAMMEE

    type - write a description of a command type

  SSYYNNOOPPSSIISS

    type name ...

  DDEESSCCRRIIPPTTIIOONN

    The ttyyppee(1) command reports on how each name would be interpreted if used
    as a command name. For example, a name can be reported as a shell built-
    in, a function, an alias, or a keyword; if name is a separate utility,
    ttyyppee(1) displays the path name.

  DDIIAAGGNNOOSSTTIICCSS

    The ttyyppee(1) utility exits with status 0 for success, and >0 if an error
    occurred.

  NNOOTTEESS

    This command is a shell built-in.

  SSEEEE AALLSSOO

    _k_s_h(1)

