tty(1)                                                           tty(1)

  ttttyy

  NNAAMMEE

    tty - return user's terminal name

  SSYYNNOOPPSSIISS

    tty [[-s]]

  DDEESSCCRRIIPPTTIIOONN

    The ttttyy(1) utility writes to standard output the name of the terminal
    attached to standard input. The name is the string returned by _t_t_y_n_a_m_e(2).
    If the standard input is not a terminal, the message not a tty is written.
    The options are as follows:

    --ss
        Do not write the terminal name; only the exit status is affected when
        this option is specified.

    The --ss option is deprecated in favor of:

    test -t 0

  DDIIAAGGNNOOSSTTIICCSS

    The ttttyy(1) utility exits 0 if the standard input is a terminal, 1 if the
    standard input is not a terminal, and >1 if an error occurs.

  SSEEEE AALLSSOO

    _t_s_e_t(1)

