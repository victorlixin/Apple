pod2html(1)                                                 pod2html(1)

  ppoodd22hhttmmll

  NNAAMMEE

    pod2html - convert .pod files to .html files

  SSYYNNOOPPSSIISS

    pod2html --help --htmlroot=name --infile=name --outfile=name
             --podpath=name:...:name --podroot=name
             --libpods=name:...:name --recurse --norecurse --verbose
             --index --noindex --title=name

  DDEESSCCRRIIPPTTIIOONN

    Converts files from pod format (see ppeerrllppoodd) to HTML format.

  AARRGGUUMMEENNTTSS

    The ppoodd22hhttmmll(1) utility takes the following arguments:

    ----hheellpp
        Displays the usage message.
    ----hhttmmllrroooott==name
        Sets the base URL for the HTML files. When cross references are made,
        the HTML root is prepended to the URL.
    ----iinnffiillee==name
        Specify the pod file to convert. Input is taken from STDIN if no
        infile is specified.
    ----oouuttffiillee==name
        Specify the HTML file to create. Output goes to STDOUT if no outfile
        is specified.
    ----ppooddrroooott==name
        Specify the base directory for finding library pods.
    ----ppooddppaatthh==name::......::name
        Specify the subdirectories in which the ppooddrroooott contains pod files
        whose HTML converted forms can be linked-to in cross references.
    ----lliibbppooddss==name::......::name
        List of page names (for example, ppeerrllffuunncc) which contain linkable
        items.

    ----nneettssccaappee
        Use Netscape HTML directives when applicable.

    ----nnoonneettssccaappee
        Do not use Netscape HTML directives (default).

    ----iinnddeexx
        Generate an index at the top of the HTML file (default behavior).

    ----nnooiinnddeexx
        Do not generate an index at the top of the HTML file.

    ----rreeccuurrssee
        Recurse into subdirectories specified in ppooddppaatthh (default behavior).

    ----nnoorreeccuurrssee
        Do not recurse into subdirectories specified in ppooddppaatthh.
    ----ttiittllee==name
        Specify the title of the resulting HTML file.

    ----vveerrbboossee
        Display progress messages.

  AAUUTTHHOORR

    Tom Christiansen

  BBUUGGSS

    See the PPoodd::::HHttmmll man page for a list of known bugs in the translator.

