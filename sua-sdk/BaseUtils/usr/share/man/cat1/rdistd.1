rdistd(1)                                                     rdistd(1)

  rrddiissttdd

  NNAAMMEE

    rdistd - remote file distribution server program

  SSYYNNOOPPSSIISS

    rdistd -S [[-D]]

    rdistd -V

  DDEESSCCRRIIPPTTIIOONN

    The rrddiissttdd(1) utility is the server program for the rrddiisstt(1) command. It
    is normally run by rrddiisstt(1) through rrsshh(1).

    The --SS argument must be specified. The option is required so that
    rrddiissttdd(1) is not accidentally started because it normally resides
    somewhere in a normal user's PATH.

    The following options are available:

    --DD
        Enable debugging messages. Messages are logged through _ss_yy_ss_ll_oo_gg(3).

    --VV
        Print version information and exit.

  SSEEEE AALLSSOO

    _r_d_i_s_t(1)

    _r_s_h(1)

    _r_c_m_d(3)

