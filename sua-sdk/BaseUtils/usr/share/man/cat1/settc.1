settc(1)                                                       settc(1)

  sseettttcc

  NNAAMMEE

    settc - set termcap for shell

  SSYYNNOOPPSSIISS

    settc cap value

  DDEESSCCRRIIPPTTIIOONN

    This command is a C-shell built-in command.

    The sseettttcc(1) command tells the shell to behave as though the terminal
    capability cap (as defined in _tt_ee_rr_mm_cc_aa_pp(5)) has the value value. It does not
    check for correctness.

