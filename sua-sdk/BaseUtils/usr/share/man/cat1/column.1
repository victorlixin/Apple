column(1)                                                     column(1)

  ccoolluummnn

  NNAAMMEE

    column - columnate lists

  SSYYNNOOPPSSIISS

    column [[-tx]] [[-c columns]] [[-s sep]] [[file ...]]

  DDEESSCCRRIIPPTTIIOONN

    The ccoolluummnn(1) utility formats its input into multiple columns. Rows are
    filled before columns. Input is taken from file operands, or by default,
    from the standard input. Empty lines are ignored.

  OOPPTTIIOONNSS
    --cc columns
        Output is formatted for a display columns wide.

    --ss sep
        Specify a set of characters (sep used to delimit columns for the --
        tt option.

    --tt
        Create a table. The ccoolluummnn(1) utility determines the number of columns
        from the input. By default, output columns are delimited by white
        space or with the string specified using the --ss option.

    --xx
        Fill columns before filling rows.

  DDIIAAGGNNOOSSTTIICCSS

    The ccoolluummnn(1) utility exits with status 0 for success, and >0 if an error
    occurred.

  EENNVVIIRROONNMMEENNTT VVAARRIIAABBLLEESS

    The ccoolluummnnss(1) utility makes use of the following environment variables:

    COLUMNS
        If no other information is available, ccoolluummnn(1) examines the content
        of COLUMNS to determine the width of the screen.

  EEXXAAMMPPLLEE

    The following command converts llss --ll output into a table:

    $$ (printf "PERM LINKS OWNER SIZE MONTH DAY HH:MM/YEAR NAME\n" ; \ls -o |
    sed 1d) | column -t

  SSEEEE AALLSSOO

    _l_s(1)

    _p_a_s_t_e(1)

    _s_o_r_t(1)

