stop(1)                                                         stop(1)

  ssttoopp

  NNAAMMEE

    stop - terminates background jobs or processes

  SSYYNNOOPPSSIISS

    stop %job||pid ......

  DDEESSCCRRIIPPTTIIOONN

    This command is a C-shell built-in command.

    The ssttoopp(1) command stops the specified jobs or processes that are
    executing in the background. The job argument can be a number, a string, a
    null string, %%, ++, or --, as described in the "Jobs" section of _tt_cc_ss_hh(1).
    There is no default job; using ssttoopp without an argument does not stop the
    current job.

