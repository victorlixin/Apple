pwd(1)                                                           pwd(1)

  ppwwdd

  NNAAMMEE

    pwd - return working directory name

  SSYYNNOOPPSSIISS

    pwd

  DDEESSCCRRIIPPTTIIOONN

    The ppwwdd(1) utility writes the absolute path name of the current working
    directory to the standard output.

    The ppwwdd(1) utility can handle path names longer than PATH_MAX, if the path
    names can be returned by _gg_ee_tt_cc_ww_dd(2).

  DDIIAAGGNNOOSSTTIICCSS

    The ppwwdd(1) utility exits 0 on success, and >0 if an error occurs. Known
    errors are:

    1
        Out of memory.

    2
        Error in the underlying _gg_ee_tt_cc_ww_dd(2) call.

  SSEEEE AALLSSOO

    _c_d(1)

    _g_e_t_c_w_d(2)

