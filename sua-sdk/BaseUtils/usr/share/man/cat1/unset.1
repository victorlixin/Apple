unset(1)                                                       unset(1)

  uunnsseett

  NNAAMMEE

    unset - remove variables/parameters

  SSYYNNOOPPSSIISS

  CC sshheellll

    unset pattern

  KKoorrnn sshheellll

    unset [[-fv]] parameter ......

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    The uunnsseett(1) command removes all variables whose names match pattern,
    unless they are read-only. Thus, 'unset *' removes all variables unless
    they are read-only; this is not recommended. It is not an error if no
    variables are removed.

  KKoorrnn sshheellll

    The uunnsseett(1) command unsets the named parameters (--vv, the default) or
    functions (--ff). The exit status is non-zero if any of the parameters were
    already unset; otherwise, it is zero.

