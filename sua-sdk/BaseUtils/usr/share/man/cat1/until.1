until(1)                                                       until(1)

  uunnttiill

  NNAAMMEE

    until - execute commands until exit status is zero

  SSYYNNOOPPSSIISS

    until list do list done

  DDEESSCCRRIIPPTTIIOONN

    This command is a Korn-shell built-in command.

    The uunnttiill(1) command works like _w_h_i_l_e(1), except that the body is executed
    only while the exit status of the first list is non-zero.

