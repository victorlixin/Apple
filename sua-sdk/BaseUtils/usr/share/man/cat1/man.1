man(1)                                                           man(1)

  mmaann

  NNAAMMEE

    man - display system documentation

  SSYYNNOOPPSSIISS

    man [[-M manpaths]] [[-s section]] name ...
    man section name ...
    man -k topic

  DDEESSCCRRIIPPTTIIOONN

    The mmaann(1) command displays information about the given command or
    function name.

    The mmaann(1) command displays the first matching reference page it finds.
    When set, the MANPATH environment variable determines the order in which
    different sets are searched. See the description of the --ss option for more
    information about search order.

    The options are:

    --MM manpath
        The mmaann(1) command searches the directories specified in manpath as
        well as the standard directories. Manpath is a colon-separated list of
        directories. The manpath is used instead of the contents of the
        MANPATH environment variable (if set).
    --ss section
        Look for the man pages in the subdirectory named ccaattsection.
        SSeeccttiioonn     DDeessccrriippttiioonn

        1           Interix utilities

        1w          Windows utilities

        2           Interix system calls

        3           Interix library calls

        4           Devices and drivers

        5           File formats and miscellaneous information

        1X11R5      X11R5 utilities

        3X11R5      X11R5 library calls

        5X11R5      X11R5 miscellaneious information

        1X11R6      X11R6 utilities

        3X11R6      X11R6 library calls
        The section numbers are used to distinguish between reference pages
        with the same names, such as _pp_rr_ii_nn_tt_ff(1) and _pp_rr_ii_nn_tt_ff(3). To see the
        reference page for the _pp_rr_ii_nn_tt_ff(1) utility, use the command:
        $$ man -s 1 printf
        and to see the reference page for the _pp_rr_ii_nn_tt_ff(3) function, use:
        $$ man -s 3 printf
        By default, //uussrr//XX1111RR66//mmaann appears in the MANPATH environment variable
        before //uussrr//XX1111RR55//mmaann. Consequently, X11R6 man pages for utilities and
        functions with the same name as their X11R5 counterparts are displayed
        unless the X11R5 section number is specified on the mmaann command line.
        To give priority to the X11R5 man pages, change the order of the
        MANPATH environment variable so that //uussrr//XX1111RR55//mmaann appears before //
        uussrr//XX1111RR66//mmaann.
    --kk topic
        Search the wwhhaattiiss..ddbb database for a reference-page summary line that
        contains the keyword topic. The name of the reference page appears in
        parentheses. The topic can be an extended regular expression; the
        search is case-insensitive. (Many people prefer to alias mmaann --kk to the
        command aapprrooppooss(1).)
        The wwhhaattiiss..ddbb database is the collection of utility and function
        descriptions found under the NAME headings of the reference pages.

  LLooccaallee ssuuppppoorrtt bbyy tthhee mmaann uuttiilliittyy

    The mmaann utility supports searching for localized man pages in locale
    subdirectories of each directory specified in the MANPATH environment
    variable.

    The locale name is taken from the first of three environment variables
    that contain a nonempty value, in this order:

    *     LC_ALL
    *     LC_CTYPE
    *     LANG

    If none of these variables specifies a valid locale name, then the mmaann
    utility displays the first matching man page found in the paths specified
    by the MANPATH environment variable. Otherwise, the mmaann utility displays
    the first matching man page found in the following directories in this
    order:

    *     lang_country.charset
    *     lang.charset
    *     en.charset
    *     lang

    where lang is the Interix extension for locales that do not contain all
    the components in their name.

    For example, for the de_DE.ISO8859-1 locale, mmaann searches in the following
    subdirectories of /usr/share/man:

    *     /usr/share/man/de_DE.ISO8859.1
    *     /usr/share/man/de.ISO8859.1
    *     /usr/share/man/en.ISO8859.1
    *     /usr/share/man/de

  EEXXAAMMPPLLEESS

    To find all programs for editing files:

    $$ man -k edit
     eedd -- lliinnee oorriieenntteedd tteexxtt eeddiittoorr
     ex, vi - Visual editor
     sed - stream editor

    To see the reference page for the _pp_rr_ii_nn_tt_ff(3) function:

    $$ man 3 printf

  DDIIAAGGNNOOSSTTIICCSS

    The mmaann(1) command exits with 0 for success and >0 for failure. It returns
    1 if the topic is not found in any "whatis" database.

  FFIILLEESS

    The mmaann(1) utility uses the following files and directories:
    //sshhaarree//mmaann//ccaattsection
        The directories containing the formatted reference pages.

    //sshhaarree//mmaann//wwhhaattiiss..ddbb
        The keyword database. This is a text file containing the command
        summary descriptions from the reference pages, one to a line. For
        reference pages that document more than one utility or function,
        parentheses surround the utility or function that designates the page.

  EENNVVIIRROONNMMEENNTT VVAARRIIAABBLLEESS

    The mmaann(1) utility makes use of the following environment variables, if
    set:

    MANPATH
        A colon-separated list of directories to search for man pages. Each
        directory typically contains a set of ccaattsection subdirectories and a
        wwhhaattiiss..ddbb file.

    PAGER
        A command to invoke the pager program that displays the reference
        page. The default is mmoorree

  SSEEEE AALLSSOO

    _g_r_e_p(1)

    _m_o_r_e(1)

