locale(1)                                                     locale(1)

  llooccaallee

  NNAAMMEE

    locale - get locale-specific information

  SSYYNNOOPPSSIISS

    locale [[-ck]] name ...
    locale [[-a]] [[-m]]

  DDEESSCCRRIIPPTTIIOONN

    The llooccaallee(1) utility writes information to standard-output about the
    current locale.

    The options are as follows:

    --aa
        Write the names of all public available locales. The Subsystem for 
        UNIX-based Applications supports the C and POSIX locales (which are 
        identical), ja_JP.EUC-JP and ja_JP.eucJP locales (which are identical),
        and win-ja_JP.windows-932 and ja_JP.SJIS locales (which are identical).

    --cc
        Write the name of the selected locale categories.

    --kk
        Write the names and values of selected locale category keywords.

    --mm
        Write the names of all available character maps.

    Refer to the POSIX.2 standard for a complete discussion of the locale
    categories and keywords.

  DDIIAAGGNNOOSSTTIICCSS

    The llooccaallee(1) utility exits 0 on success, and >0 if an error occurs.

  SSEEEE AALLSSOO

    _l_o_c_a_l_e_d_e_f(1)

