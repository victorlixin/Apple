bg(1)                                                             bg(1)

  bbgg

  NNAAMMEE

    bg, % - puts jobs into the background

  SSYYNNOOPPSSIISS

  CC sshheellll

    bg [[%job......]]
    %job&

  KKoorrnn sshheellll

    bg [[job......]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    The bbgg(1) command puts the specified jobs (or, if no job is specified, the
    current job) into the background, continuing each if it is stopped. The
    job argument can be a number, a string, %%, ++, --, or omitted. For more
    information, see "Jobs" in _cc_ss_hh(1).

  KKoorrnn sshheellll

    The bbgg(1) command resumes the specified stopped job or jobs in the
    background. If no jobs are specified, the current job (%%++) is assumed. For
    more information, see "Job Control" in _kk_ss_hh(1).

  SSEEEE AALLSSOO

    _f_g(1)

