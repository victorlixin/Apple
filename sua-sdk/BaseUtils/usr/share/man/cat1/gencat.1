gencat(1)                                                     gencat(1)

  ggeennccaatt

  NNAAMMEE

    gencat - natural language support (NLS) catalog compiler

  SSYYNNOOPPSSIISS

    gencat output-file input-file...

  DDEESSCCRRIIPPTTIIOONN

    The ggeennccaatt(1) utility compiles a text NLS input file into a catalog in a
    binary format used by the _cc_aa_tt_cc_ll_oo_ss_ee(3), _cc_aa_tt_gg_ee_tt_ss(3) and _cc_aa_tt_oo_pp_ee_nn(3)
    functions.
	
	The ggeennccaatt(1) utility merges the text NLS input files into a formatted 
	message catalog file, output-file. The output file is created if it does not already 
	exist. If an output file does exist, its messages are included in the new output-file. 
	If set and message numbers collide, the new message text defined in any of the input 
	files replaces old message text contained in the existing output file.

	The ggeennccaatt(1) utility returns 0 on success, and >0 if an error occurs.

  SSEEEE AALLSSOO

    _c_a_t_c_l_o_s_e(3)

    _c_a_t_g_e_t_s(3)

    _c_a_t_o_p_e_n(3)

