fold(1)                                                         fold(1)

  ffoolldd

  NNAAMMEE

    fold - fold long lines for finite width output device

  SSYYNNOOPPSSIISS

    fold [[-w width]] file ...

  DDEESSCCRRIIPPTTIIOONN

    The ffoolldd(1) utility is a filter that folds the contents of the specified
    files, or the standard input if no files are specified, breaking the lines
    at a maximum width of 80 characters.

  OOPPTTIIOONNSS
    --ww width
        Specifies a line width to use instead of the default 80 characters. If
        the input text includes tabs, width should be a multiple of 8.

  DDIIAAGGNNOOSSTTIICCSS

    The ffoolldd(1) utility exits 0 on success, and >0 if an error occurs.

  SSEEEE AALLSSOO

    _f_m_t(1)

