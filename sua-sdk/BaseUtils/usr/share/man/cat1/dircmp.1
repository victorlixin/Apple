dircmp(1)                                                     dircmp(1)

  ddiirrccmmpp

  NNAAMMEE

    dircmp - compare directory contents

  SSYYNNOOPPSSIISS

    dircmp [[-ds]] dir1 dir2

  DDEESSCCRRIIPPTTIIOONN

    The ddiirrccmmpp(1) utility compares the contents of the directory hierarchies
    specified by dir1 and dir2. The files are compared by name first and then
    by content.Files that have identical content but different names will
    never be compared; use ccmmpp(1) or ddiiffff(1) for that.

  OOPPTTIIOONNSS

    --dd
        Run ddiiffff(1) on files that have the same name in both directories.

    --ss
        Suppress messages about identical files.

    See the ddiiffff(1) reference page for more information on output formats.

  DDIIAAGGNNOOSSTTIICCSS

    The ddiirrccmmpp(1) utility exits with a value of 0 for success or >0 if an
    error occurred.

  NNOOTTEESS

    This utility is identical to ddiiffff(1) when used to compare directories.

  SSEEEE AALLSSOO

    _c_m_p(1)

    _d_i_f_f(1)

