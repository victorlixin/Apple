exit(1)                                                         exit(1)

  eexxiitt

  NNAAMMEE

    exit - terminates the shell

  SSYYNNOOPPSSIISS

  CC sshheellll

    exit [[expr]]

  KKoorrnn sshheellll

    exit [[status]]

  DDEESSCCRRIIPPTTIIOONN

    This command is a built-in command in the C shell and the Korn shell.

  CC sshheellll

    When the eexxiitt(1) command is called, the shell exits either with the value
    of the specified expr (an expression, as described in the _tt_cc_ss_hh(1) section
    on Expressions) or, without expr, with the value of the status variable.

  KKoorrnn sshheellll

    When the eexxiitt(1) command is called, the shell exits with the specified
    exit status. If status is not specified, the exit status is the current
    value of the ?? parameter.

