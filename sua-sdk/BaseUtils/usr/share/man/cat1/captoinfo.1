captoinfo(1)                                               captoinfo(1)

  ccaappttooiinnffoo

  NNAAMMEE

    captoinfo - convert a tteerrmmccaapp description into a tteerrmmiinnffoo description

  SSYYNNOOPPSSIISS

    captoinfo [[-hc]] [[-v[[n]]]] [[-e names]] [[-CILNRTrsw1]] source-file

  DDEESSCCRRIIPPTTIIOONN

    The ccaappttooiinnffoo utility looks in file for _tt_ee_rr_mm_cc_aa_pp(5) descriptions. For each
    one found, an equivalent _tt_ee_rr_mm_ii_nn_ff_oo(5) description is written to standard
    output. TTeerrmmccaapp ttcc capabilities are translated directly to tteerrmmiinnffoo uussee
    capabilities.

    If no file is given, then the environment variable TERMCAP is used for the
    file name or entry. If TERMCAP is a full path name to a file, only the
    terminal whose name is specified in the environment variable TERM is
    extracted from that file. If the environment variable TERMCAP is not set,
    then the file //uussrr//sshhaarree//tteerrmmiinnffoo is read.

  OOPPTTIIOONNSS

    --11
        format translation output one capability per line

    --CC
        translate entries to termcap source form

    --II
        translate entries to terminfo source form

    --LL
        translate entries to full terminfo source form

    --NN
        disable smart defaults for source translation

    --RR
        restrict translation to given terminfo/termcap version

    --TT
        remove size-restrictions on compiled description

    --cc
        check only, validate input without compiling or translating
    --ee names
        translate/compile only entries named by comma-separated list
    --oo dir
        set output directory for compiled entry writes

    --rr
        force resolution of all use entries in source translation

    --ss
        print summary statistics
    --vv[n]
        set verbosity level
    --ww[n]
        set format width for translation output

    source-file
        file to translate or compile

  FFIILLEESS

    //uussrr//sshhaarree//tteerrmmiinnffoo
        Compiled terminal description database.

  TTRRAANNSSLLAATTIIOONNSS FFRROOMM NNOONNSSTTAANNDDAARRDD CCAAPPAABBIILLIITTIIEESS

    Some obsolete nonstandard capabilities will automatically be translated
    into standard (SVr4/XSI Curses) tteerrmmiinnffoo capabilities by ccaappttooiinnffoo.
    Whenever one of these automatic translations is done, the program will
    issue a notification to stderr, inviting the user to check that it has not
    mistakenly translated a completely unknown and random capability, or
    syntax error, or both.

    NNoonnssttdd     SSttdd      FFrroomm      TTeerrmmiinnffoo
    nnaammee       nnaammee               ccaappaabbiilliittyy

    BO         mr       AT&T      enter_reverse_mode

    CI         vi       AT&T      cursor_invisible

    CV         ve       AT&T      cursor_normal

    DS         mh       AT&T      enter_dim_mode

    EE         me       AT&T      exit_attribute_mode

    FE         LF       AT&T      label_on

    FL         LO       AT&T      label_off

    XS         mk       AT&T      enter_secure_mode

    EN         @7       XENIX     key_end

    GE         ae       XENIX     exit_alt_charset_mode

    GS         as       XENIX     enter_alt_charset_mode

    HM         kh       XENIX     key_home

    LD         kL       XENIX     key_dl

    PD         kN       XENIX     key_npage

    PN         po       XENIX     prtr_off

    PS         pf       XENIX     prtr_on

    PU         kP       XENIX     key_ppage

    RT         @8       XENIX     kent

    UP         ku       XENIX     kcuu1

    KA         k;       Tek       key_f10

    KB         F1       Tek       key_f11

    KC         F2       Tek       key_f12

    KD         F3       Tek       key_f13

    KE         F4       Tek       key_f14

    KF         F5       Tek       key_f15

    BC         Sb       Tek       set_background

    FC         Sf       Tek       set_foreground

    HS         mh       Iris      enter_dim_mode

    XENIX termcap also used to have a set of extension capabilities for forms
    drawing, designed to take advantage of the IBM PC high-half graphics. They
    were as follows:

    CCaapp     GGrraapphhiicc

    G2      upper left

    G3      lower left

    G1      upper right

    G4      lower right

    GR      pointing right

    GL      pointing left

    GU      pointing up

    GD      pointing down

    GH      horizontal line

    GV      vertical line

    GC      intersection

    G6      upper left

    G7      lower left

    G5      upper right

    G8      lower right

    Gr      tee pointing right

    Gr      tee pointing left

    Gu      tee pointing up

    Gd      tee pointing down

    Gh      horizontal line

    Gv      vertical line

    Gc      intersection

    GG      acs magic cookie count

    If the single-line capabilities occur in an entry, they will automatically
    be composed into an alternate character-set character (acsc) string. The
    double-line capabilities and GGGG are discarded with a warning message.

    IBM's Advanced Interactive Executive (AIX) has a tteerrmmiinnffoo facility
    descended from SVr1 tteerrmmiinnffoo but incompatible with the SVr4 format. The
    following AIX extensions are automatically translated:

    IIBBMM       XXSSII

    ksel      kslt

    kbtab     kcbt

    font0     s0ds

    font1     s1ds

    font2     s2ds

    font3     s3ds

    Additionally, the AIX box1 capability will be automatically translated to
    an acsc string.

    Hewlett-Packard's terminfo library supports two nonstandard tteerrmmiinnffoo
    capabilities: mmeemmll (memory lock) and mmeemmuu (memory unlock). These will be
    discarded with a warning message.

  NNOOTTEESS

    This utility is actually a link to _tt_ii_cc(1), running in --II mode.

    The ttrraaccee option is not identical to the same option under SVr4. Under
    SVr4, instead of following the --vv with a trace level n, you repeat it n
    times.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _i_n_f_o_c_m_p(1)

    _t_e_r_m_i_n_f_o(5)

  AAUUTTHHOORR

    Eric S. Raymond

