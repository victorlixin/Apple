vi(1w)                                                           vi(1w)

  vvii

  NNAAMMEE

    vi - Windows command-line utility text editor

  SSYYNNOOPPSSIISS

    vi [[-eFlRrv]] [[-c cmd]] [[-t tag]] [[-w size]] [[file ...]]

  DDEESSCCRRIIPPTTIIOONN

    The vvii(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications vvii(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax, and the files
    should be in Windows text format (that is, with lines ending with
    carriage-return/line-feed characters). When run as a Windows command-line
    utility, vvii saves files in Windows text format.

    For more information, see _v_i(1).

