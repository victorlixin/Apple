ln(1w)                                                           ln(1w)

  llnn

  NNAAMMEE

    ln - Windows command-line utility to link files

  SSYYNNOOPPSSIISS

    ln [[-fs]] source_file target_file
    ln [[-fs]] source_file... target_dir

  DDEESSCCRRIIPPTTIIOONN

    The llnn(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications llnn(1) command that you can run at a Windows command prompt. The
    source_file, target_file, and target_dir arguments must use Windows file-
    name and path syntax. For more information, see _l_n(1).

