wc(1w)                                                           wc(1w)

  wwcc

  NNAAMMEE

    wc - Windows command-line utility to provide word, line, and byte count

  SSYYNNOOPPSSIISS

    wc [[-c||-m [[-lsw]]]] [[ file...]]

  DDEESSCCRRIIPPTTIIOONN

    The wwcc(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications wwcc(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _w_c(1).

