chgrp(1w)                                                     chgrp(1w)

  cchhggrrpp

  NNAAMMEE

    chgrp - Windows command-line utility to change group

  SSYYNNOOPPSSIISS

    chgrp [[-fhR]] group file...

  DDEESSCCRRIIPPTTIIOONN

    The cchhggrrpp(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications cchhggrrpp(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _c_h_g_r_p(1).

