uniq(1w)                                                       uniq(1w)

  uunniiqq

  NNAAMMEE

    uniq - Windows command-line utility to report or filter out repeated lines
    in a file

  SSYYNNOOPPSSIISS

    uniq [[-c||-d||-u]] [[-f fields]] [[-s chars]]
         [[input_file[[output_file]]]]

  DDEESSCCRRIIPPTTIIOONN

    The uunniiqq(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications uunniiqq(1) command that you can run at a Windows command prompt. The
    input_file and output_file arguments must use Windows file-name and path
    syntax. For more information, see _u_n_i_q(1).

