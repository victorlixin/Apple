more(1w)                                                       more(1w)

  mmoorree

  NNAAMMEE

    more - Windows command-line utility to display file on crt terminal

  SSYYNNOOPPSSIISS

    more [[-ceisu]] [[-p command]] [[-P str]] [[-n number]] [[-t tag]] [[-x tabs]] [[-/
    pattern]] [[-#]] [[file... ]]

  DDEESSCCRRIIPPTTIIOONN

    The mmoorree(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications mmoorree(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _m_o_r_e(1).

