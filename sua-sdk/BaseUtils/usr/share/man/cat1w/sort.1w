sort(1w)                                                       sort(1w)

  ssoorrtt

  NNAAMMEE

    sort - Windows command-line utility to sort or merge text files

  SSYYNNOOPPSSIISS

    sort [[-cmubdfinr]] [[-t char]] [[-T char]]
         [[-k field1[[,field2]]]]... [[ -o output]] [[file]]...

  DDEESSCCRRIIPPTTIIOONN

    The ssoorrtt(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications ssoorrtt(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _s_o_r_t(1).

