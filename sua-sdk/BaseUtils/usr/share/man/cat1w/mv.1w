mv(1w)                                                           mv(1w)

  mmvv

  NNAAMMEE

    mv - Windows command-line utility to move files

  SSYYNNOOPPSSIISS

    mv [[-fi]] source target
    mv [[-fi]] source ... directory

  DDEESSCCRRIIPPTTIIOONN

    The mmvv(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications mmvv(1) command that you can run at a Windows command prompt. The
    source, target, and directory arguments must use Windows file-name and
    path syntax. For more information, see _m_v(1).

