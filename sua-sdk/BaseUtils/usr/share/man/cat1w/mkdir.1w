mkdir(1w)                                                     mkdir(1w)

  mmkkddiirr

  NNAAMMEE

    mkdir - Windows command-line utility to make directories

  SSYYNNOOPPSSIISS

    mkdir [[-p]] [[-m mode]] directory_name...

  DDEESSCCRRIIPPTTIIOONN

    The mmkkddiirr(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications mmkkddiirr(1) command that you can run at a Windows command prompt. The
    directory_name argument must use Windows file-name and path syntax. For
    more information, see _m_k_d_i_r(1).

