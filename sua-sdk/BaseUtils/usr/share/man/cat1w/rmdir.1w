rmdir(1w)                                                     rmdir(1w)

  rrmmddiirr

  NNAAMMEE

    rmdir - Windows command-line utility to remove directories

  SSYYNNOOPPSSIISS

    rmdir directory...

  DDEESSCCRRIIPPTTIIOONN

    The rrmmddiirr(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications rrmmddiirr(1) command that you can run at a Windows command prompt. The
    directory argument must use Windows file-name and path syntax. For more
    information, see _r_m_d_i_r(1).

