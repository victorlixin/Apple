cat(1w)                                                         cat(1w)

  ccaatt

  NNAAMMEE

    cat - Windows command-line utility to concatenate and print files

  SSYYNNOOPPSSIISS

    cat [[-benstuv]] [[file ...]]

  DDEESSCCRRIIPPTTIIOONN

    The ccaatt(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications ccaatt(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _c_a_t(1).

