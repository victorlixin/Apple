find(1w)                                                       find(1w)

  ffiinndd

  NNAAMMEE

    find - Windows command-line utility to walk a file hierarchy

  SSYYNNOOPPSSIISS

    find [[-L|-P]] [[-DdHhiXx]] [[-f file]] ffiillee  [[file...]] [[expression]]

  DDEESSCCRRIIPPTTIIOONN

    The ffiinndd(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications ffiinndd(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _f_i_n_d(1).

