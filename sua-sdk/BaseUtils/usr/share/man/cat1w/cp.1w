cp(1w)                                                           cp(1w)

  ccpp

  NNAAMMEE

    cp - Windows command-line utility to copy files

  SSYYNNOOPPSSIISS

    cp [[-Rr]] [[-fip]] src target
    cp [[-Rr]] [[-fip]] src1 ...... srcN directory

  DDEESSCCRRIIPPTTIIOONN

    The ccpp(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications ccpp(1) command that you can run at a Windows command prompt. The
    source_file, target_file, and target_directory arguments must use Windows
    file-name and path syntax. For more information, see _c_p(1).

