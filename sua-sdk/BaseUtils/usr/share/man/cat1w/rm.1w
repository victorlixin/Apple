rm(1w)                                                           rm(1w)

  rrmm

  NNAAMMEE

    rm - Windows command-line utility to remove directory entries

  SSYYNNOOPPSSIISS

    rm [[-dfiPRr]] file...

  DDEESSCCRRIIPPTTIIOONN

    The rrmm(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications rrmm(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _r_m(1).

