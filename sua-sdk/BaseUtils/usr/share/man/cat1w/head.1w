head(1w)                                                       head(1w)

  hheeaadd

  NNAAMMEE

    head - Windows command-line utility to display first lines of a file

  SSYYNNOOPPSSIISS

    head [[-b || -c || -k || -n num]] [[ file...]]

  DDEESSCCRRIIPPTTIIOONN

    The hheeaadd(1w) Windows command-line utility is an implementation of the
    Subsystem for UNIX-based Applications hheeaadd(1) command that you can run at a Windows command prompt. The
    file argument must use Windows file-name and path syntax. For more
    information, see _h_e_a_d(1).

