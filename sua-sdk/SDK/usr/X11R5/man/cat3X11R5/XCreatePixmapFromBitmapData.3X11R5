XCreatePixmapFromBitmapData(3X11R5)      XCreatePixmapFromBitmapData(3X11R5)

  XXRReeaaddBBiittmmaappFFiillee(())

  NNAAMMEE

    XReadBitmapFile(), XWriteBitmapFile(), XCreatePixmapFromBitmapData(),
    XCreateBitmapFromData() - manipulate bitmaps

  SSYYNNOOPPSSIISS

    int XReadBitmapFile (Display *display, Drawable d,
                         char *filename, unsigned int *width_return,
                         unsigned int *height_return,
                         Pixmap *bitmap_return, int *x_hot_return,
                         int *y_hot_return)
    int XWriteBitmapFile (Display *display, char *filename,
                          Pixmap bitmap, unsigned int width,
                          unsigned int height, int x_hot,
                          int y_hot)
    Pixmap XCreatePixmapFromBitmapData (Display *display, Drawable d,
                                        char *data,
                                        unsigned int width,
                                        unsigned int height,
                                        unsigned long fg,
                                        unsigned long bg,
                                        unsigned int depth)
    Pixmap XCreateBitmapFromData (Display *display, Drawable d,
                                  char *data, unsigned int width,
                                  unsigned int height)

  AARRGGUUMMEENNTTSS

    bitmap
        Specifies the bitmap.

    bitmap_return
        Returns the bitmap that is created.

    d
        Specifies the drawable that indicates the screen.

    data
        Specifies the data in bitmap format.

    data
        Specifies the location of the bitmap data.

    depth
        Specifies the depth of the pixmap.

    display
        Specifies the connection to the X server.

    fg

    bg
        Specify the foreground and background pixel values to use.

    filename
        Specifies the file name to use. The format of the file name is
        operating-system dependent.

    width

    height
        Specify the width and height.

    width_return

    height_return
        Return the width and height values of the read in bitmap file.

    x_hot

    y_hot
        Specify where to place the hotspot coordinates (or -1,-1 if none are
        present) in the file.

    x_hot_return

    y_hot_return
        Return the hotspot coordinates.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_RR_ee_aa_dd_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) function reads in a file containing a bitmap.
    The file is parsed in the encoding of the current locale. The ability to
    read other than the standard format is implementation dependent. If the
    file cannot be opened, _XX_RR_ee_aa_dd_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) returns BitmapOpenFailed.
    If the file can be opened but does not contain valid bitmap data, it
    returns BitmapFileInvalid. If insufficient working storage is allocated,
    it returns BitmapNoMemory. If the file is readable and valid, it returns
    BitmapSuccess.

    _XX_RR_ee_aa_dd_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) returns the bitmap's height and width, as read
    from the file, to width_return and height_return. It then creates a pixmap
    of the appropriate size, reads the bitmap data from the file into the
    pixmap, and assigns the pixmap to the caller's variable bitmap. The caller
    must free the bitmap using _XX_FF_rr_ee_ee_PP_ii_xx_mm_aa_pp(3X11R5) when finished. If
    <name>_x_hot and <name>_y_hot exist, _XX_RR_ee_aa_dd_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) returns them
    to x_hot_return and y_hot_return; otherwise, it returns -1,-1.

    _XX_RR_ee_aa_dd_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) can generate BadAlloc and BadDrawable errors.

    The _XX_WW_rr_ii_tt_ee_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) function writes a bitmap out to a file in the
    X version 11 format. The file is written in the encoding of the current
    locale. If the file cannot be opened for writing, it returns
    BitmapOpenFailed. If insufficient memory is allocated,
    _XX_WW_rr_ii_tt_ee_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) returns BitmapNoMemory; otherwise, on no error,
    it returns BitmapSuccess. If x_hot and y_hot are not -1, -1,
    _XX_WW_rr_ii_tt_ee_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) writes them out as the hotspot coordinates for
    the bitmap.

    _XX_WW_rr_ii_tt_ee_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) can generate BadDrawable and BadMatch errors.

    The _XX_CC_rr_ee_aa_tt_ee_PP_ii_xx_mm_aa_pp_FF_rr_oo_mm_BB_ii_tt_mm_aa_pp_DD_aa_tt_aa(3X11R5) function creates a pixmap of the
    given depth and then does a bitmap-format _XX_PP_uu_tt_II_mm_aa_gg_ee(3X11R5) of the data
    into it. The depth must be supported by the screen of the specified
    drawable, or a BadMatch error results.

    _XX_CC_rr_ee_aa_tt_ee_PP_ii_xx_mm_aa_pp_FF_rr_oo_mm_BB_ii_tt_mm_aa_pp_DD_aa_tt_aa(3X11R5) can generate BadAlloc and BadMatch
    errors.

    The _XX_CC_rr_ee_aa_tt_ee_BB_ii_tt_mm_aa_pp_FF_rr_oo_mm_DD_aa_tt_aa(3X11R5) function allows you to include in your C
    program (using #include) a bitmap file that was written out by
    _XX_WW_rr_ii_tt_ee_BB_ii_tt_mm_aa_pp_FF_ii_ll_ee(3X11R5) (X version 11 format only) without reading in the
    bitmap file. The following example creates a gray bitmap:

    #include "gray.bitmap"
    Pixmap bitmap;
    bitmap = XCreateBitmapFromData(display, window,
                    gray_bits, gray_width, gray_height);

    If insufficient working storage was allocated,
    _XX_CC_rr_ee_aa_tt_ee_BB_ii_tt_mm_aa_pp_FF_rr_oo_mm_DD_aa_tt_aa(3X11R5) returns None. It is your responsibility to
    free the bitmap using _XX_FF_rr_ee_ee_PP_ii_xx_mm_aa_pp(3X11R5) when finished.

    _XX_CC_rr_ee_aa_tt_ee_BB_ii_tt_mm_aa_pp_FF_rr_oo_mm_DD_aa_tt_aa(3X11R5) can generate a BadAlloc error.

  DDIIAAGGNNOOSSTTIICCSS

    BadAlloc
        The server failed to allocate the requested resource or server memory.

    BadDrawable
        A value for a Drawable argument does not name a defined Window or
        Pixmap.

    BadMatch
        An InputOnly window is used as a Drawable.

  SSEEEE AALLSSOO

    _X_C_r_e_a_t_e_P_i_x_m_a_p()

    _X_P_u_t_I_m_a_g_e()

    Xlib

