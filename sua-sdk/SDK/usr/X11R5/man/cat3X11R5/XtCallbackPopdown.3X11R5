XtCallbackPopdown(3X11R5)                     XtCallbackPopdown(3X11R5)

  XXttPPooppddoowwnn(())

  NNAAMMEE

    XtPopdown(), XtCallbackPopdown(), MenuPopdown() - unmap a pop-up

  SSYYNNOOPPSSIISS

    void XtPopdown (Widget popup_shell)
    void XtCallbackPopdown (Widget w, XtPointer client_data,
                            XtPointer call_data)
    void MenuPopdown (String shell_name)

  AARRGGUUMMEENNTTSS

    call_data
        Specifies the callback data, which is not used by this procedure.

    client_data
        Specifies a pointer to the XXttPPooppddoowwnnIIDD structure.

    popup_shell
        Specifies the widget shell to pop down.

    shell_name
        Specifies the name of the widget shell to pop down.

    w
        Specifies the widget.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_tt_PP_oo_pp_dd_oo_ww_nn(3X11R5) function performs the following:
    *     Calls _XX_tt_CC_hh_ee_cc_kk_SS_uu_bb_cc_ll_aa_ss_ss(3X11R5) to ensure popup_shell is a subclass of
          Shell.
    *     Checks that popup_shell is currently popped_up; otherwise, it
          generates an error.
    *     Unmaps popup_shell's window.
    *     If popup_shell's grab_kind is either XtGrabNonexclusive or
          XtGrabExclusive, it calls _XX_tt_RR_ee_mm_oo_vv_ee_GG_rr_aa_bb(3X11R5).
    *     Sets pop-up shell's popped_up field to False.
    *     Calls the callback procedures on the shell's popdown_callback list.

    The _XX_tt_CC_aa_ll_ll_bb_aa_cc_kk_PP_oo_pp_dd_oo_ww_nn(3X11R5) function casts the client data parameter to
    an XXttPPooppddoowwnnIIDD pointer:

    typedef struct {
         Widget shell_widget;
         Widget enable_widget;
    } XtPopdownIDRec, *XtPopdownID;

    The shell_widget is the pop-up shell to pop down, and the enable_widget is
    the widget that was used to pop it up.

    _XX_tt_CC_aa_ll_ll_bb_aa_cc_kk_PP_oo_pp_dd_oo_ww_nn(3X11R5) calls _XX_tt_PP_oo_pp_dd_oo_ww_nn(3X11R5) with the specified
    shell_widget and then calls _XX_tt_SS_ee_tt_SS_ee_nn_ss_ii_tt_ii_vv_ee(3X11R5) to resensitize the
    enable_widget.

    If a shell name is not given, _MM_ee_nn_uu_PP_oo_pp_dd_oo_ww_nn(3X11R5) calls _XX_tt_PP_oo_pp_dd_oo_ww_nn(3X11R5)
    with the widget for which the translation is specified. If a shell_name is
    specified in the translation table, _MM_ee_nn_uu_PP_oo_pp_dd_oo_ww_nn(3X11R5) tries to find the
    shell by looking up the widget tree starting at the parent of the widget
    in which it is invoked. If it finds a shell with the specified name in the
    pop-up children of that parent, it pops down the shell; otherwise, it
    moves up the parent chain as needed. If _MM_ee_nn_uu_PP_oo_pp_dd_oo_ww_nn(3X11R5) gets to the
    application top-level shell widget and cannot find a matching shell, it
    generates an error.

  SSEEEE AALLSSOO

    _X_t_C_r_e_a_t_e_P_o_p_u_p_S_h_e_l_l()

    _X_t_P_o_p_u_p()

    X Toolkit Intrinsics - C Language Interface

    Xlib - C Language X Interface

