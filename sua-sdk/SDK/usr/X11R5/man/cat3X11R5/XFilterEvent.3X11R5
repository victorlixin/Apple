XFilterEvent(3X11R5)                               XFilterEvent(3X11R5)

  XXFFiilltteerrEEvveenntt(())

  NNAAMMEE

    XFilterEvent() - filter X events for an input method

  SSYYNNOOPPSSIISS

    Bool XFilterEvent (XEvent *event, Window w)

  AARRGGUUMMEENNTTSS

    event
        Specifies the event to filter.

    w
        Specifies the window for which the filter is to be applied.

  DDEESSCCRRIIPPTTIIOONN

    If the window argument is None, XFilterEvent applies the filter to the
    window specified in the XXEEvveenntt structure. The window argument is provided
    so that layers above Xlib that do event redirection can indicate to which
    window an event has been redirected.

    If _XX_FF_ii_ll_tt_ee_rr_EE_vv_ee_nn_tt(3X11R5) returns True, then some input method has filtered
    the event, and the client should discard the event. If
    _XX_FF_ii_ll_tt_ee_rr_EE_vv_ee_nn_tt(3X11R5) returns False, then the client should continue
    processing the event.

    If a grab has occurred in the client, and _XX_FF_ii_ll_tt_ee_rr_EE_vv_ee_nn_tt(3X11R5) returns
    True , the client should ungrab the keyboard.

  SSEEEE AALLSSOO

    _X_N_e_x_t_E_v_e_n_t()

    Xlib

