XPutBackEvent(3X11R5)                             XPutBackEvent(3X11R5)

  XXPPuuttBBaacckkEEvveenntt(())

  NNAAMMEE

    XPutBackEvent() - put events back on the queue

  SSYYNNOOPPSSIISS

    XPutBackEvent (Display *display, XEvent *event)

  AARRGGUUMMEENNTTSS

    display
        Specifies the connection to the X server.

    event
        Specifies the event.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_PP_uu_tt_BB_aa_cc_kk_EE_vv_ee_nn_tt(3X11R5) function pushes an event back onto the head of
    the display's event queue by copying the event into the queue. This can be
    useful if you read an event and then decide that you would rather deal
    with it later. There is no limit to the number of times in succession that
    you can call _XX_PP_uu_tt_BB_aa_cc_kk_EE_vv_ee_nn_tt(3X11R5).

  SSEEEE AALLSSOO

    _X_A_n_y_E_v_e_n_t()

    _X_I_f_E_v_e_n_t()

    _X_N_e_x_t_E_v_e_n_t()

    _X_S_e_n_d_E_v_e_n_t()

    Xlib

