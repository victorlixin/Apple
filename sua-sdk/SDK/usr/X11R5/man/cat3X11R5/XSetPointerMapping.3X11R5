XSetPointerMapping(3X11R5)                   XSetPointerMapping(3X11R5)

  XXSSeettPPooiinntteerrMMaappppiinngg(())

  NNAAMMEE

    XSetPointerMapping(), XGetPointerMapping() - manipulate pointer settings

  SSYYNNOOPPSSIISS

    int XSetPointerMapping (Display *display, unsigned char map[],
                            int nmap)
    int XGetPointerMapping (Display *display,
                            unsigned char map_return[],
                            int nmap)

  AARRGGUUMMEENNTTSS

    display
        Specifies the connection to the X server.

    map
        Specifies the mapping list.

    map_return
        Returns the mapping list.

    nmap
        Specifies the number of items in the mapping list.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_SS_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) function sets the mapping of the pointer.
    If it succeeds, the X server generates a MappingNotify event, and
    _XX_SS_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) returns MappingSuccess. Element map[i] defines
    the logical button number for the physical button i+1. The length of the
    list must be the same as _XX_GG_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) would return, or a
    BadValue error results. A zero element disables a button, and elements are
    not restricted in value by the number of physical buttons. However, no two
    elements can have the same nonzero value, or a BadValue error results. If
    any of the buttons to be altered are logically in the down state,
    _XX_SS_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) returns MappingBusy, and the mapping is not
    changed.

    _XX_SS_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) can generate a BadValue error.

    The _XX_GG_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) function returns the current mapping of the
    pointer. Pointer buttons are numbered starting from one.
    _XX_GG_ee_tt_PP_oo_ii_nn_tt_ee_rr_MM_aa_pp_pp_ii_nn_gg(3X11R5) returns the number of physical buttons actually
    on the pointer. The nominal mapping for a pointer is map[i]=i+1. The nmap
    argument specifies the length of the array where the pointer mapping is
    returned, and only the first nmap elements are returned in map_return.

  DDIIAAGGNNOOSSTTIICCSS

    BadValue
        Some numeric value falls outside the range of values accepted by the
        request. Unless a specific range is specified for an argument, the
        full range defined by the argument's type is accepted. Any argument
        defined as a set of alternatives can generate this error.

  SSEEEE AALLSSOO

    _X_C_h_a_n_g_e_K_e_y_b_o_a_r_d_C_o_n_t_r_o_l()

    _X_C_h_a_n_g_e_K_e_y_b_o_a_r_d_M_a_p_p_i_n_g()

    Xlib

