XDrawImageString16(3X11R5)                   XDrawImageString16(3X11R5)

  XXDDrraawwIImmaaggeeSSttrriinngg(())

  NNAAMMEE

    XDrawImageString(), XDrawImageString16() - draw image text

  SSYYNNOOPPSSIISS

    XDrawImageString (Display *display, Drawable d, GC gc,
                      int x, int y, char *string, int length)
    XDrawImageString16 (Display *display, Drawable d, GC gc,
                        int x, int y, XChar2b *string, int length)

  AARRGGUUMMEENNTTSS

    d
        Specifies the drawable.

    display
        Specifies the connection to the X server.

    gc
        Specifies the GC.

    length
        Specifies the number of characters in the string argument.

    string
        Specifies the character string.

    x

    y
        Specify the x and y coordinates, which are relative to the origin of
        the specified drawable and define the origin of the first character.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_DD_rr_aa_ww_II_mm_aa_gg_ee_SS_tt_rr_ii_nn_gg_11_66(3X11R5) function is similar to
    _XX_DD_rr_aa_ww_II_mm_aa_gg_ee_SS_tt_rr_ii_nn_gg(3X11R5) except that it uses 2-byte or 16-bit characters.
    Both functions also use both the foreground and background pixels of the
    GC in the destination.

    The effect is first to fill a destination rectangle with the background
    pixel defined in the GC and then to paint the text with the foreground
    pixel. The upper-left corner of the filled rectangle is at:

    [x, y - font-ascent]

    The width is:

    overall-width

    The height is:

    font-ascent + font-descent

    The overall-width, font-ascent, and font-descent are as would be returned
    by _XX_QQ_uu_ee_rr_yy_TT_ee_xx_tt_EE_xx_tt_ee_nn_tt_ss(3X11R5) using gc and string. The function and fill-
    style defined in the GC are ignored for these functions. The effective
    function is GGXXccooppyy(), and the effective fill-style is FillSolid.

    For fonts defined with 2-byte matrix indexing and used with
    _XX_DD_rr_aa_ww_II_mm_aa_gg_ee_SS_tt_rr_ii_nn_gg(3X11R5), each byte is used as a byte2 with a byte1 of
    zero.

    Both functions use these GC components: plane-mask, foreground,
    background, font, subwindow-mode, clip-x-origin, clip-y-origin, and clip-
    mask.

    _XX_DD_rr_aa_ww_II_mm_aa_gg_ee_SS_tt_rr_ii_nn_gg(3X11R5) and _XX_DD_rr_aa_ww_II_mm_aa_gg_ee_SS_tt_rr_ii_nn_gg_11_66(3X11R5) can generate
    BadDrawable , BadGC , and BadMatch errors.

  DDIIAAGGNNOOSSTTIICCSS

    BadDrawable
        A value for a Drawable argument does not name a defined Window or
        Pixmap.

    BadGC
        A value for a GContext argument does not name a defined GContext.

    BadMatch
        An InputOnly window is used as a Drawable.

    BadMatch
        Some argument or pair of arguments has the correct type and range but
        fails to match in some other way required by the request.

  SSEEEE AALLSSOO

    _X_D_r_a_w_S_t_r_i_n_g()

    _X_D_r_a_w_T_e_x_t()

    _X_L_o_a_d_F_o_n_t()

    _X_T_e_x_t_E_x_t_e_n_t_s()

    Xlib

