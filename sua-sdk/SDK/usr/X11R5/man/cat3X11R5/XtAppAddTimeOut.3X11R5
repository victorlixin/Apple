XtAppAddTimeOut(3X11R5)                         XtAppAddTimeOut(3X11R5)

  XXttAAppppAAddddTTiimmeeOOuutt(())

  NNAAMMEE

    XtAppAddTimeOut(), XtRemoveTimeOut() - register and remove time-outs

  SSYYNNOOPPSSIISS

    XtIntervalId XtAppAddTimeOut (XtAppContext app_context,
                                  unsigned long interval,
                                  XtTimerCallbackProc proc,
                                  XtPointer client_data)
    void XtRemoveTimeOut (XtIntervalId timer)

  AARRGGUUMMEENNTTSS

    app_context
        Specifies the application context for which the timer is to be set.

    client_data
        Specifies the argument that is to be passed to the specified procedure
        when called.

    interval
        Specifies the time interval in milliseconds.

    proc
        Specifies the procedure that is to be called when time expires.

    timer
        Specifies the ID for the time-out request to be destroyed.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_tt_AA_pp_pp_AA_dd_dd_TT_ii_mm_ee_OO_uu_tt(3X11R5) function creates a time-out and returns an
    identifier for it. The time-out value is set to interval. The callback
    procedure is called when the time interval elapses, and then the time-out
    is removed.

    The _XX_tt_RR_ee_mm_oo_vv_ee_TT_ii_mm_ee_OO_uu_tt(3X11R5) function removes the time-out. Note that time-
    outs are automatically removed once they trigger.

  SSEEEE AALLSSOO

    _X_t_A_p_p_A_d_d_I_n_p_u_t()

    X Toolkit Intrinsics - C Language Interface

    Xlib - C Language X Interface

