XQueryTree(3X11R5)                                   XQueryTree(3X11R5)

  XXQQuueerryyTTrreeee(())

  NNAAMMEE

    XQueryTree() - query window tree information

  SSYYNNOOPPSSIISS

    Status XQueryTree (Display *display,
                       Window w, Window *root_return,
                       Window *parent_return,
                       Window **children_return,
                       unsigned int *nchildren_return)

  AARRGGUUMMEENNTTSS

    children_return
        Returns the list of children.

    display
        Specifies the connection to the X server.

    nchildren_return
        Returns the number of children.

    parent_return
        Returns the parent window.

    root_return
        Returns the root window.

    w
        Specifies the window whose list of children, root, parent, and number
        of children you want to obtain.

  DDEESSCCRRIIPPTTIIOONN

    The _XX_QQ_uu_ee_rr_yy_TT_rr_ee_ee(3X11R5) function returns the root ID, the parent window ID,
    a pointer to the list of children windows, and the number of children in
    the list for the specified window. The children are listed in current
    stacking order, from bottommost (first) to topmost (last).
    _XX_QQ_uu_ee_rr_yy_TT_rr_ee_ee(3X11R5) returns zero if it fails and nonzero if it succeeds. To
    free this list when it is no longer needed, use _XX_FF_rr_ee_ee(3X11R5).

  BBUUGGSS

    This really should return a ssccrreeeenn **, not a root window ID.

  SSEEEE AALLSSOO

    _X_F_r_e_e()

    _X_G_e_t_W_i_n_d_o_w_A_t_t_r_i_b_u_t_e_s()

    _X_Q_u_e_r_y_P_o_i_n_t_e_r()

    Xlib

