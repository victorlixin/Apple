XtCreateWindow(3X11R5)                           XtCreateWindow(3X11R5)

  XXttCCrreeaatteeWWiinnddooww(())

  NNAAMMEE

    XtCreateWindow() - window creation convenience function

  SSYYNNOOPPSSIISS

    void XtCreateWindow (Widget w, unsigned int window_class,
                         Visual *visual, XtValueMask value_mask,
                         XSetWindowAttributes *attributes)

  AARRGGUUMMEENNTTSS

    attributes
        Specifies the window attributes to use in the _XX_CC_rr_ee_aa_tt_ee_WW_ii_nn_dd_oo_ww(3X11R5)
        call.

    value_mask
        Specifies which attribute fields to use.

    visual
        Specifies the visual type (usually CopyFromParent ).

    w
        Specifies the widget that is used to set the x,y coordinates and so
        on.

    window_class
        Specifies the Xlib window class (for example, InputOutput, InputOnly,
        or CopyFromParent).

  DDEESSCCRRIIPPTTIIOONN

    The _XX_tt_CC_rr_ee_aa_tt_ee_WW_ii_nn_dd_oo_ww(3X11R5) function calls the Xlib _XX_CC_rr_ee_aa_tt_ee_WW_ii_nn_dd_oo_ww(3X11R5)
    function with values from the widget structure and the passed parameters.
    Then, it assigns the created window to the widget's window field.

    _XX_tt_CC_rr_ee_aa_tt_ee_WW_ii_nn_dd_oo_ww(3X11R5) evaluates the following fields of the Core widget
    structure:
    *     depth
    *     screen
    *     parent->core.window
    *     x
    *     y
    *     width
    *     height
    *     border_width

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface

    Xlib - C Language X Interface

