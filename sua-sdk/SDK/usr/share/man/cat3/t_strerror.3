t_strerror(3)                                             t_strerror(3)

  tt__ssttrreerrrroorr(())

  NNAAMMEE

    t_strerror - produce an error message string

  SSYYNNOOPPSSIISS

    #include <xti.h>

    const char *t_strerror(
        int errnum)

  DDEESSCCRRIIPPTTIIOONN

    The tt__ssttrreerrrroorr(3) function has only one parameter.

    PPaarraammeetteerrss     BBeeffoorree ccaallll     AAfftteerr ccaallll

    errnum         x               /

    The tt__ssttrreerrrroorr(3) function maps the error number in errnum that
    corresponds to an XTI error to a language-dependent error message string
    and returns a pointer to the string. The string pointed to will not be
    modified by the program, but may be overwritten by a subsequent call to
    the tt__ssttrreerrrroorr(3) function. The string is not terminated by a newline
    character. The language for error message strings written by tt__ssttrreerrrroorr(3)
    is that of the current locale. If it is English, the error message string
    describing the value in t_errno may be derived from the comments following
    the t_errno codes defined in <<xxttii..hh>>. If an error code is unknown, and the
    language is English, tt__ssttrreerrrroorr(3) returns the string:

    "<error>: error unknown"

    where <error> is the error number supplied as input. In other languages,
    an equivalent text is provided.

  VVAALLIIDD SSTTAATTEESS

    ALL - apart from T_UNINIT

  RREETTUURRNN VVAALLUUEE

    The function tt__ssttrreerrrroorr(3) returns a pointer to the generated message
    string.

  SSEEEE AALLSSOO

    _tt____ee_rr_rr_oo_rr(3)

  UUSSAAGGEE NNOOTTEESS

    The tt__ssttrreerrrroorr function is not thread safe.

    The tt__ssttrreerrrroorr function is not async-signal safe.

