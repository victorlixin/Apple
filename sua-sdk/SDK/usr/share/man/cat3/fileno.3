fileno(3)                                                     fileno(3)

  cclleeaarreerrrr(())

  NNAAMMEE

    clearerr(), feof(), ferror(), fileno() - check and reset stream status

  SSYYNNOOPPSSIISS

    #include <stdio.h>

    void clearerr (FILE *stream)
    int feof (FILE *stream)
    int ferror (FILE *stream)
    int fileno (FILE *stream)

  DDEESSCCRRIIPPTTIIOONN

    The function _cc_ll_ee_aa_rr_ee_rr_rr(3) clears the end-of-file and error indicators for
    the stream pointed to by stream.

    The function _ff_ee_oo_ff(3) tests the end-of-file indicator for the stream
    pointed to by stream, returning non-zero if it is set. The end-of-file
    indicator can only be cleared by the function _cc_ll_ee_aa_rr_ee_rr_rr(3).

    The function _ff_ee_rr_rr_oo_rr(3) tests the error indicator for the stream pointed to
    by stream, returning non-zero if it is set. The error indicator can only
    be reset by the _cc_ll_ee_aa_rr_ee_rr_rr(3) function.

    The function _ff_ii_ll_ee_nn_oo(3) examines the argument stream and returns its
    integer descriptor.

  EERRRROORRSS

    These functions should not fail and do not set the external variable errno

  SSEEEE AALLSSOO

    _o_p_e_n(2)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

