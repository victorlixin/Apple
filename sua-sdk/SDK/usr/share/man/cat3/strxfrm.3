strxfrm(3)                                                   strxfrm(3)

  ssttrrxxffrrmm(())

  NNAAMMEE

    strxfrm() - transform a string under locale

  SSYYNNOOPPSSIISS

    #include <string.h>

    size_t strxfrm (char *dst, const char *src, size_t n)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_xx_ff_rr_mm(3) function transforms a string, src, to the current locale
    and stores at most n bytes of it in dst. The transformed string will still
    compare properly

    This function is a subset of _ss_tt_rr_cc_oo_ll_ll(3)'s functionality, and is primarily
    a tool for efficient comparisons. When many comparisons must be made,
    using _ss_tt_rr_xx_ff_rr_mm(3) and _ss_tt_rr_cc_mm_pp(3) can be faster than using _ss_tt_rr_cc_oo_ll_ll(3).

    In the C and POSIX locales, it is nearly identical to _ss_tt_rr_nn_cc_mm_pp(3), but the
    return value is different.

  RREETTUURRNN VVAALLUUEE

    _ss_tt_rr_xx_ff_rr_mm(3) returns the length of the transformed string.

  SSEEEE AALLSSOO

    _m_e_m_c_m_p(3)

    _s_t_r_c_a_s_e_c_m_p(3)

    _s_t_r_c_m_p(3)

    _s_t_r_c_o_l_l(3)

  UUSSAAGGEE NNOOTTEESS

    The ssttrrxxffrrmm function is thread safe.

    The ssttrrxxffrrmm function is not async-signal safe.

