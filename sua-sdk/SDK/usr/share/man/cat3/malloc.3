malloc(3)                                                     malloc(3)

  mmaalllloocc(())

  NNAAMMEE

    malloc() - general memory allocation function

  SSYYNNOOPPSSIISS

    #include <stdlib.h>

    void * malloc (size_t size)

  DDEESSCCRRIIPPTTIIOONN

    The _mm_aa_ll_ll_oo_cc(3) function allocates uninitialized space for an object whose
    size is specified by size. The _mm_aa_ll_ll_oo_cc(3) function maintains multiple lists
    of free blocks according to size, allocating space from the appropriate
    list.

    In Subsystem for UNIX-based Applications, each call to mmaalllloocc(0) returns a different value.

  RREETTUURRNN VVAALLUUEESS

    Upon successful completion with size not equal to 0, mmaalllloocc() returns a
    pointer to the allocated space. If size is 0, either a null pointer or a
    unique pointer that can be successfully passed to _ff_rr_ee_ee(3) will be
    returned. Otherwise, it returns a null pointer and sets errno to indicate
    the error.

  EERRRROORRSS

    The mmaalllloocc() function will fail if:

    [ENOMEM]
        Insufficient storage space is available.

  SSEEEE AALLSSOO

    _c_a_l_l_o_c(3)

    _f_r_e_e(3)

    _r_e_a_l_l_o_c(3)

  UUSSAAGGEE NNOOTTEESS

    The mmaalllloocc function is thread safe.

    The mmaalllloocc function is not async-signal safe.

