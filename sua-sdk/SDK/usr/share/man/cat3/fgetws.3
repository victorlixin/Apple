fgetws(3)                                                     fgetws(3)

  ffggeettwwss(())

  NNAAMMEE

    fgetws() - get a wide-character line of from a stream

  SSYYNNOOPPSSIISS

    #include <stdio.h>
    #include <wchar.h>

    wchar_t * fgetws (wchar_t *str, size_t size, FILE *stream)

  DDEESSCCRRIIPPTTIIOONN

    The _ff_gg_ee_tt_ww_ss(3) function reads at most one less than the number of
    characters specified by size from the given stream and stores them in the
    wide-character string str. Reading stops when a newline character is
    found, at end-of-file or error. The newline, if any, is retained. In any
    case a \0 wide-character code is appended to end the string.

  RREETTUURRNN VVAALLUUEESS

    Upon successful completion, _ff_gg_ee_tt_ww_ss(3) returns a pointer to the wide-
    character string. If end-of-file or an error occurs before any characters
    are read, they return NULL. The _ff_gg_ee_tt_ww_ss(3) function does not distinguish
    between end-of-file and error, and callers must use _ff_ee_oo_ff(3) and _ff_ee_rr_rr_oo_rr(3)
    to determine which occurred.

  EERRRROORRSS

    [EBADF]
        The given stream is not a readable stream.

    The function _ff_gg_ee_tt_ww_ss(3) may also fail and set errno for any of the errors
    specified for the routines _ff_ff_ll_uu_ss_hh(3), _ff_ss_tt_aa_tt(2), _rr_ee_aa_dd(2) or _mm_aa_ll_ll_oo_cc(3).

    The function _ff_gg_ee_tt_ww_ss(3) may also fail and set errno for any of the errors
    specified for the routine _gg_ee_tt_ww_cc_hh_aa_rr(3).

  SSEEEE AALLSSOO

    _f_e_o_f(3)

    _f_e_r_r_o_r(3)

  UUSSAAGGEE NNOOTTEESS

    The ffggeettwwss function is thread safe.

    The ffggeettwwss function is not async-signal safe.

