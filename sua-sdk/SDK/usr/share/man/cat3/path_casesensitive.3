path_casesensitive(3)                             path_casesensitive(3)

  ppaatthh__ccaasseesseennssiittiivvee(())

  NNAAMMEE

    path_casesensitive() - ensure correct case of pathname

  SSYYNNOOPPSSIISS

    #include <interix/interix.h>

    char *
    path_casesensitive(char *buf)

  DDEESSCCRRIIPPTTIIOONN

    The _pp_aa_tt_hh____cc_aa_ss_ee_ss_ee_nn_ss_ii_tt_ii_vv_ee(3) function attempts to ensure that the Subsystem for UNIX-based Applications
    pathname passed in buf has the correct case. If the case is not correct
    (for example, the value of buf is /dev/fs/C/temp/123 and the directory is
    actually /dev/fs/C/TEMP/123), then the function converts the pathname in
    place. If buf could match either of two other pathnames, all three
    differing only by case, it returns the first one it can match.

  RREETTUURRNN VVAALLUUEESS

    The _pp_aa_tt_hh____cc_aa_ss_ee_ss_ee_nn_ss_ii_tt_ii_vv_ee(3) utility exits with status 0 for success, and >0
    if an error occurred.

  SSEEEE AALLSSOO

    _c_h_g_p_a_t_h(1)

    _u_n_i_x_p_a_t_h_2_w_i_n(2)

  UUSSAAGGEE NNOOTTEESS

    The ppaatthh__ccaasseesseennssiittiivvee function is thread safe.

    The ppaatthh__ccaasseesseennssiittiivvee function is not async-signal safe.

