ffs(3)                                                           ffs(3)

  ffffss(())

  NNAAMMEE

    ffs() - find first bit set in a bit string

  SSYYNNOOPPSSIISS

    #include <strings.h>

    int ffs (int value)

  DDEESSCCRRIIPPTTIIOONN

    The _ff_ff_ss(3) function finds the first bit set in value and returns the index
    of that bit. Bits are numbered starting from 1, starting at the right-most
    bit.

  RREETTUURRNN VVAALLUUEESS

    _FF_ff_ss(3) returns the index to the first bit set; the right-most bit is
    numbered 1. A return value of 0 means that the argument was zero.

  UUSSAAGGEE NNOOTTEESS

    The ffffss function is thread safe.

    The ffffss function is not async-signal safe.

