form_new_page(3)                                       form_new_page(3)

  ffoorrmm__nneeww__ppaaggee(())

  NNAAMMEE

    set_new_page(), new_page() - form pagination functions

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_new_page(FIELD *field, int justification);
    int new_page(FIELD *field);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____nn_ee_ww____pp_aa_gg_ee(3) marks the given field as the beginning of a
    new page on its form.

    The function _nn_ee_ww____pp_aa_gg_ee(3) is a predicate which tests if a given field marks
    a page beginning on its form.

  RREETTUURRNN VVAALLUUEE

    The function _nn_ee_ww____pp_aa_gg_ee(3) returns TRUE or FALSE.

    The function _ss_ee_tt____nn_ee_ww____pp_aa_gg_ee(3) return one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_CONNECTED
        The given field is already connected to a form.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3) and 3X pages whose names begin form_ for detailed descriptions
    of the entry points.

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

