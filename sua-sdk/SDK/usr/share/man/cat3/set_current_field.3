set_current_field(3)                               set_current_field(3)

  ffoorrmm__ppaaggee(())

  NNAAMMEE

    set_current_field(), current_field(), set_form_page() - set and get form
    page number

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_current_field(FORM *form, int page);
    FIELD *current_field(FORM *);
    int set_form_page(FORM *form, int n);
    int form_page(FORM *form);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____cc_uu_rr_rr_ee_nn_tt____ff_ii_ee_ll_dd(3) sets the current field of the given
    form; _cc_uu_rr_rr_ee_nn_tt____ff_ii_ee_ll_dd(3) returns the current field of the given form.

    The function _ss_ee_tt____ff_oo_rr_mm____pp_aa_gg_ee(3) sets the form's page number (goes to page n
    of the form).

    The function _ff_oo_rr_mm____pp_aa_gg_ee(3) returns the form's current page number.

  RREETTUURRNN VVAALLUUEE

    Except for _ff_oo_rr_mm____pp_aa_gg_ee(3), each routine returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_BAD_STATE
        Routine was called from an initialization or termination function.

    E_INVALID_FIELD
        Contents of a field are not valid.

    E_REQUEST_DENIED
        The form driver could not process the request.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _f_o_r_m_s(3)

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

