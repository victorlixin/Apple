strpbrk(3)                                                   strpbrk(3)

  ssttrrppbbrrkk(())

  NNAAMMEE

    strpbrk() - locate multiple characters in string

  SSYYNNOOPPSSIISS

    #include <string.h>

    char * strpbrk (const char *s, const char *charset)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_pp_bb_rr_kk(3) function locates in the NUL-terminated string s the first
    occurrence of any character in the string charset and returns a pointer to
    this character. If no characters from charset occur anywhere in s,
    _ss_tt_rr_pp_bb_rr_kk(3) returns NULL.

  RREETTUURRNN CCOODDEESS

    On success, _ss_tt_rr_pp_bb_rr_kk(3) returns a pointer to a character. On failure, it
    returns NULL.

  SSEEEE AALLSSOO

    _m_e_m_c_h_r(3)

    _s_t_r_c_h_r(3)

    _s_t_r_c_s_p_n(3)

    _s_t_r_r_c_h_r(3)

    _s_t_r_s_e_p(3)

    _s_t_r_s_p_n(3)

    _s_t_r_s_t_r(3)

    _s_t_r_t_o_k(3)

  UUSSAAGGEE NNOOTTEESS

    The ssttrrppbbrrkk function is thread safe.

    The ssttrrppbbrrkk function is not async-signal safe.

