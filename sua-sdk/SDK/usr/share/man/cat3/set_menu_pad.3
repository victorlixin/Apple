set_menu_pad(3)                                         set_menu_pad(3)

  mmeennuu__aattttrriibbss(())

  NNAAMMEE

    menu_fore(), set_menu_fore(), menu_back(), set_menu_back(), menu_grey(),
    set_menu_grey(), menu_pad() set_menu_pad() - color and attribute control
    for menus

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_menu_fore(MENU *menu, chtype attr);
    chtype menu_fore(MENU *menu);
    int set_menu_back(MENU *menu, chtype attr);
    chtype menu_back(MENU *menu);
    int set_menu_grey(MENU *menu, chtype attr);
    chtype menu_grey(MENU *menu);
    int set_menu_pad(MENU *menu, int pad);
    chtype menu_pad(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____mm_ee_nn_uu____ff_oo_rr_ee(3) sets the foreground attribute of menu. This
    is the highlight used for selected menu items. _mm_ee_nn_uu____ff_oo_rr_ee(3) returns the
    foreground attribute. The default is A_STANDOUT.

    The function _ss_ee_tt____mm_ee_nn_uu____bb_aa_cc_kk(3) sets the background attribute of menu. This
    is the highlight used for selectable (but not currently selected) menu
    items. The function _mm_ee_nn_uu____bb_aa_cc_kk(3) returns the background attribute. The
    default is A_NORMAL.

    The function _ss_ee_tt____mm_ee_nn_uu____gg_rr_ee_yy(3) sets the grey attribute of menu. This is the
    highlight used for un-selectable menu items in menus that permit more than
    one selection. The function _mm_ee_nn_uu____gg_rr_ee_yy(3) returns the grey attribute. The
    default is A_UNDERLINE.

    The function _ss_ee_tt____mm_ee_nn_uu____pp_aa_dd(3) sets the character used to fill the space
    between the name and description parts of a menu item. _mm_ee_nn_uu____pp_aa_dd(3) returns
    the given menu's pad character. The default is a blank.

  RREETTUURRNN VVAALLUUEE

    These routines return one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3) and reference pages whose names begin menu_ for detailed
    descriptions of the entry points.

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

