getnetent(3)                                               getnetent(3)

  ggeettnneetteenntt(())

  NNAAMMEE

    getnetent(), getnetbyaddr(), getnetbyname(), setnetent(), endnetent() -
    get network entry

  SSYYNNOOPPSSIISS

    #include <netdb.h>

    struct netent * getnetent (void)
    struct netent * getnetbyname (char *name)
    struct netent * getnetbyaddr (long net, int type)
    int setnetent (int stayopen)
    int endnetent (void)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_nn_ee_tt_ee_nn_tt(3), _gg_ee_tt_nn_ee_tt_bb_yy_nn_aa_mm_ee(3), and _gg_ee_tt_nn_ee_tt_bb_yy_aa_dd_dd_rr(3) functions each
    return a pointer to an object with the following structure containing the
    broken-out fields of a line in the network data base:
    %%WWIINNDDIIRR%%//ssyysstteemm3322//ddrriivveerrss//eettcc//nneettwwoorrkkss

    struct    netent {
         char      *n_name;  /* official name of net */
         char      **n_aliases;   /* alias list */
         int       n_addrtype;    /* net number type */
         unsigned long  n_net;         /* net number */
    };

    The members of this structure are:

    nn__nnaammee
        The official name of the network.

    nn__aalliiaasseess
        A zero terminated list of alternate names for the network.

    nn__aaddddrrttyyppee
        The type of the network number returned; currently only AF_INET.

    nn__nneett
        The network number. Network numbers are returned in machine byte
        order.

    The _gg_ee_tt_nn_ee_tt_ee_nn_tt(3) function reads the next line of the file, opening the
    file if necessary.

    The _ss_ee_tt_nn_ee_tt_ee_nn_tt(3) function opens and rewinds the file. If the stayopen flag
    is non-zero, the net data base will not be closed after each call to
    _gg_ee_tt_nn_ee_tt_bb_yy_nn_aa_mm_ee(3) or _gg_ee_tt_nn_ee_tt_bb_yy_aa_dd_dd_rr(3).

    The _ee_nn_dd_nn_ee_tt_ee_nn_tt(3) function closes the file.

    The _gg_ee_tt_nn_ee_tt_bb_yy_nn_aa_mm_ee(3) function and _gg_ee_tt_nn_ee_tt_bb_yy_aa_dd_dd_rr(3) search sequentially from
    the beginning of the file until a matching net name or net address and
    type is found, or until the end of file (EOF) is encountered. Network
    numbers are supplied in host order.

  FFIILLEESS

    %%WWIINNDDIIRR%%ssyysstteemm3322//ddrriivveerrss//eettcc//nneettwwoorrkkss
        The network database; the value of %%WWIINNDDIIRR%% is the installation
        directory for Windows, typically //ddeevv//ffss//CC//WWIINNNNTT.

  RREETTUURRNN VVAALLUUEESS

    Null pointer (0) returned on EOF or error.

  NNOOTTEESS

    The data space used by these functions is static; if future use requires
    the data, it should be copied before any subsequent calls to these
    functions overwrite it. Only Internet network numbers are currently
    understood. Expecting network numbers to fit in no more than 32 bits is
    probably unrealistic.

  UUSSAAGGEE NNOOTTEESS

    None of these functions are thread safe.

    None of these functions are async-signal safe.

