sigaddset(3)                                               sigaddset(3)

  ssiiggsseettooppss(())

  NNAAMMEE

    sigemptyset(), sigfillset(), sigaddset(), sigdelset(), sigismember() -
    manipulate signal sets

  SSYYNNOOPPSSIISS

    #include <signal.h>

    int sigemptyset (sigset_t *set)
    int sigfillset (sigset_t *set)
    int sigaddset (sigset_t *set, int signo)
    int sigdelset (sigset_t *set, int signo)
    int sigismember (sigset_t *set, int signo)

  DDEESSCCRRIIPPTTIIOONN

    These functions manipulate signal sets stored in a ssiiggsseett__tt. You must call
    either _ss_ii_gg_ee_mm_pp_tt_yy_ss_ee_tt(3) or _ss_ii_gg_ff_ii_ll_ll_ss_ee_tt(3) for every object of type sigset_t
    before any other use of the object.

    The _ss_ii_gg_ee_mm_pp_tt_yy_ss_ee_tt(3) function initializes a signal set to be empty of the
    POSIX signals.

    The _ss_ii_gg_ff_ii_ll_ll_ss_ee_tt(3) function initializes a signal set to contain all of the
    POSIX signals.

    The _ss_ii_gg_aa_dd_dd_ss_ee_tt(3) function adds the specified signal signo to the signal
    set.

    The _ss_ii_gg_dd_ee_ll_ss_ee_tt(3) function deletes the specified signal signo from the
    signal set.

    The _ss_ii_gg_ii_ss_mm_ee_mm_bb_ee_rr(3) function returns whether a specified signal signo is
    contained in the signal set.

    These functions are provided as macros in the include file <<ssiiggnnaall..hh>>.
    Actual functions are available if their names are undefined (with ##uunnddeeff
    name

  RREETTUURRNN VVAALLUUEESS

    The _ss_ii_gg_ii_ss_mm_ee_mm_bb_ee_rr(3) function returns 1 if the signal is a member of the
    set, 0 otherwise. The other functions return 0 upon successful completion;
    otherwise, they return -1 and set errno to indicate the error.

  EERRRROORRSS

    _ss_ii_gg_aa_dd_dd_ss_ee_tt(3), _ss_ii_gg_dd_ee_ll_ss_ee_tt(3), and _ss_ii_gg_ii_ss_mm_ee_mm_bb_ee_rr(3) (that is, all execpt
    _ss_ii_gg_ee_mm_pp_tt_yy_ss_ee_tt(3) and _ss_ii_gg_ff_ii_ll_ll_ss_ee_tt(3)) may fail because:

    [EINVAL]
        The specified signo is invalid.

  SSEEEE AALLSSOO

    _k_i_l_l(2)

    _s_i_g_a_c_t_i_o_n(2)

    _s_i_g_p_r_o_c_m_a_s_k(2)

    _s_i_g_s_u_s_p_e_n_d(2)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    The following functions are async-signal safe: ssiiggaaddddsseett,, ssiiggddeellsseett,,
    ssiiggeemmppttyysseett,, ssiiggffiillllsseett,, ssiiggiissmmeemmbbeerr. The ssiiggsseettooppss function is not async-
    signal safe.

