ATAN(3) 	                                        	       ATAN(3)

NAME
     atan, atanf -- arc tangent functions of one variable

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     atan(double x);

     float
     atanf(float x);

DESCRIPTION
     The aattaann() and the aattaannff() functions compute the principal value of the
     arc tangent of x.	For a discussion of error due to roundoff, see
     math(3).

RETURN VALUES
     The aattaann() and the aattaannff() function returns the arc tangent in the range
     [-pi/2, +pi/2] radians.

SEE ALSO
     aaccooss(3), aassiinn(3), aattaann22(3), ccooss(3), ccoosshh(3), math(3), ssiinn(3), ssiinnhh(3),
     ttaann(3), ttaannhh(3)

STANDARDS
     The aattaann() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The aattaann function is thread safe.
     The aattaann function is nnoott async-signal safe.

