TANH(3) 	                                        	       TANH(3)

NAME
     tanh, tanhf -- hyperbolic tangent functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     tanh(double x);

     float
     tanhf(float x);

DESCRIPTION
     The ttaannhh() and the ttaannhhff() functions compute the hyperbolic tangent of x.
     For a discussion of error due to roundoff, see math(3).

RETURN VALUES
     The ttaannhh() and the ttaannhhff() functions return the hyperbolic tangent value.

SEE ALSO
     aaccooss(3), aassiinn(3), aattaann(3), aattaann22(3), ccooss(3), ccoosshh(3), math(3), ssiinn(3),
     ssiinnhh(3), ttaann(3)

STANDARDS
     The ttaannhh() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The ttaannhhff function is thread safe.
     The ttaannhhff function is nnoott async-signal safe.

