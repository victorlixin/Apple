strdup(3)                                                     strdup(3)

  ssttrrdduupp(())

  NNAAMMEE

    strdup() - save a copy of a string

  SSYYNNOOPPSSIISS

    #include <string.h>

    char * strdup (const char *str)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_dd_uu_pp(3) function allocates sufficient memory for a copy of the
    string str, does the copy, and returns a pointer to it. The pointer may
    subsequently be used as an argument to the function _ff_rr_ee_ee(3).

    If insufficient memory is available, NULL is returned.

  SSEEEE AALLSSOO

    _m_a_l_l_o_c(3)

    _f_r_e_e(3)

  UUSSAAGGEE NNOOTTEESS

    The ssttrrdduupp function is thread safe.

    The ssttrrdduupp function is not async-signal safe.

