memccpy(3)                                                   memccpy(3)

  mmeemmccccppyy(())

  NNAAMMEE

    memccpy() - copy string until character found

  SSYYNNOOPPSSIISS

    #include <string.h>

    void * memccpy (void *dst, const void *src, int c, size_t len)

  DDEESSCCRRIIPPTTIIOONN

    The _mm_ee_mm_cc_cc_pp_yy(3) function copies bytes from string src to string dst. If the
    character c (as converted to an uunnssiiggnneedd cchhaarr) occurs in the string src,
    the copy stops and a pointer to the byte after the copy of c in the string
    dst is returned. Otherwise, len bytes are copied, and a NULL pointer is
    returned.

  SSEEEE AALLSSOO

    _b_c_o_p_y(3)

    _m_e_m_c_p_y(3)

    _m_e_m_m_o_v_e(3)

    _s_t_r_c_p_y(3)

  UUSSAAGGEE NNOOTTEESS

    The mmeemmccccppyy function is thread safe.

    The mmeemmccccppyy function is not async-signal safe.

