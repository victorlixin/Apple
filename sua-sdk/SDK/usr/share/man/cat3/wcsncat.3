wcsncat(3)                                                   wcsncat(3)

  wwccssccaatt(())

  NNAAMMEE

    wcscat(), wcslcat(), wcsncat() - concatenate wide-character strings

  SSYYNNOOPPSSIISS

    #include <wchar.h>

    wchar_t * wcscat (wchar_t *ws, const wchar_t *append)
    size_t wcslcat (wchar_t *ws, const wchar_t *append, size_t count)
    wchar_t * wcsncat (wchar_t *ws, const wchar_t *append, size_t count)

  DDEESSCCRRIIPPTTIIOONN

    The _ww_cc_ss_cc_aa_tt(3) and _ww_cc_ss_nn_cc_aa_tt(3) functions append a copy of the null-
    terminated wide-character string append to the end of the null-terminated
    wide-character string ws, then add a terminating wide-character \0. The
    string ws must have sufficient space to hold the result.

    The _ww_cc_ss_nn_cc_aa_tt(3) function appends not more than count wide-characters.

    The _ww_cc_ss_ll_cc_aa_tt(3) function is designed to be a safer, more consistent, and
    less error prone replacement for the _ww_cc_ss_nn_cc_aa_tt(3) function. Unlike the
    _ww_cc_ss_nn_cc_aa_tt(3) function, _ww_cc_ss_ll_cc_aa_tt(3) takes the full size of the buffer (not
    just the length) and guaranteees to NUL-terminate the result (as long as
    count is at larger than 0 or as long as there is at least one wide-
    character column free in ws. Note that _ww_cc_ss_ll_cc_aa_tt(3) only operates on true
    wide-character strings. This means that both ws and append must be NUL-
    terminated.

  RREETTUURRNN VVAALLUUEESS

    The _ww_cc_ss_cc_aa_tt(3) and _ww_cc_ss_nn_cc_aa_tt(3) functions return the pointer ws. The
    _ww_cc_ss_ll_cc_aa_tt(3) function returns the total number of wide-characters contained
    in ws resulting from the call, not including the NUL terminator.

  SSEEEE AALLSSOO

    _b_c_o_p_y(3)

    _m_e_m_c_p_y(3)

    _m_e_m_m_o_v_e(3)

    _w_c_s_c_p_y(3)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

