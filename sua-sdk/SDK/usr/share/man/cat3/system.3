system(3)                                                     system(3)

  ssyysstteemm(())

  NNAAMMEE

    system() - pass a command to the shell

  SSYYNNOOPPSSIISS

    #include <stdlib.h>

    int system (const char *string)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_yy_ss_tt_ee_mm(3) function hands the argument string to the command
    interpreter sshh(1). The calling process waits for the shell to finish
    executing the command, ignoring SIGINT and SIGQUIT, and blocking SIGCHLD.

    If string is a NULL pointer, _ss_yy_ss_tt_ee_mm(3) will return non-zero if the command
    interpreter sshh(1) is available, and zero if it is not.

  RREETTUURRNN VVAALLUUEESS

    The _ss_yy_ss_tt_ee_mm(3) function returns the exit status of the shell, or -1 if the
    _ww_aa_ii_tt(2) for the shell failed. A return value of 127 means the execution of
    the shell failed.

  SSEEEE AALLSSOO

    _s_h(1)

    _e_x_e_c(2)

    _w_a_i_t(2)

    _p_o_p_e_n(3)

  UUSSAAGGEE NNOOTTEESS

    The ssyysstteemm function is not thread safe.

    The ssyysstteemm function is not async-signal safe.

