strsigname(3)                                             strsigname(3)

  ssttrrssiiggnnaall(())

  NNAAMMEE

    strsignal(), strsigname() - return string or name describing signal

  SSYYNNOOPPSSIISS

    #include <string.h>

    extern const char * const sys_siglist[]

    char * strsignal(int signal)
    char * strsigname(int signal)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_ss_ii_gg_nn_aa_ll(3) function returns a pointer to a string that describes the
    signal passed in the argument signal. The _ss_tt_rr_ss_ii_gg_nn_aa_mm_ee(3) function returns a
    pointer to a string that names the signal passed in the argument signal.
    The pointer refers to a static buffer that is overwritten at the next call
    to _ss_tt_rr_ss_ii_gg_nn_aa_ll(3).

  RREETTUURRNN VVAALLUUEESS

    The _ss_tt_rr_ss_ii_gg_nn_aa_ll(3) function returns a pointer to a string describing the
    signal, or a pointer to the string "Unknown signal: " and the signal
    number.

    The _ss_tt_rr_ss_ii_gg_nn_aa_mm_ee(3) function returns a pointer to a string naming the
    signal, or a pointer to the string "Unknown signal: " and the signal
    number.

  SSEEEE AALLSSOO

    _s_t_r_s_i_g_n_a_l(1)

    _s_t_r_e_r_r_o_r(3)

  UUSSAAGGEE NNOOTTEESS

    None of these functions are thread safe.

    None of these functions are async-signal safe.

