set_menu_opts(3)                                       set_menu_opts(3)

  mmeennuu__ooppttss(())

  NNAAMMEE

    set_menu_opts(), menu_opts_on(), menu_opts_off(), menu_opts() - set and
    get menu options

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_menu_opts(MENU *menu, OPTIONS opts);
    int menu_opts_on(MENU *menu, OPTIONS opts);
    int menu_opts_off(MENU *menu, OPTIONS opts);
    OPTIONS menu_opts(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____mm_ee_nn_uu____oo_pp_tt_ss(3) sets all the given menu's option bits (menu
    option bits may be logically-ored together).

    The function _mm_ee_nn_uu____oo_pp_tt_ss____oo_nn(3) turns on the given option bits, and leaves
    others alone.

    The function _mm_ee_nn_uu____oo_pp_tt_ss____oo_ff_ff(3) turns off the given option bits, and leaves
    others alone.

    The function _mm_ee_nn_uu____oo_pp_tt_ss(3) returns the menu's current option bits.

    The following options are defined (all are on by default):
    O_ONEVALUE       Only one item can be selected for this menu.

    O_SHOWDESC       Display the item descriptions when the menu is
                     posted.

    O_ROWMAJOR       Display the menu in row-major order.

    O_IGNORECASE     Ignore the case when pattern-matching.

    O_SHOWMATCH      Move the cursor to within the item name while
                     pattern-matching.

    O_NONCYCLIC      On next-item and previous-item requests, don't
                     wrap around to the other end of the menu.

  RREETTUURRNN VVAALLUUEE

    Except for _mm_ee_nn_uu____oo_pp_tt_ss(3), each routine returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_POSTED
        The menu is already posted.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3)

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

