getdtablesize(3)                                       getdtablesize(3)

  ggeettddttaabblleessiizzee(())

  NNAAMMEE

    getdtablesize() - return file descriptor table size

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    int getdtablesize(void);

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_dd_tt_aa_bb_ll_ee_ss_ii_zz_ee(3) call returns the current limit on the number of file
    descriptors.

    This is equivalent to the call _gg_ee_tt_rr_ll_ii_mm_ii_tt(2) with the option RLIMIT_NOFILE.

  RREETTUURRNN VVAALLUUEE

    The _gg_ee_tt_dd_tt_aa_bb_ll_ee_ss_ii_zz_ee(3) function returns the current soft limit on the number
    of files open by a process.

  NNOOTTEESS

    In previous releases of Subsystem for UNIX-based Applications, this was a macro that returned
    {OPEN_MAX}. It's equivalent to:

    sysconf(_SC_OPEN_MAX);

  SSEEEE AALLSSOO

    _s_e_l_e_c_t(2)

  UUSSAAGGEE NNOOTTEESS

    The ggeettddttaabblleessiizzee function is thread safe.

    The ggeettddttaabblleessiizzee function is not async-signal safe.

