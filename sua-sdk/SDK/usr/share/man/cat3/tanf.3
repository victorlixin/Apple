TAN(3)		                                        		TAN(3)

NAME
     tan, tanf -- tangent functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     tan(double x);

     float
     tanf(float x);

DESCRIPTION
     The ttaann() and the ttaannff() functions compute the tangent of x (measured in
     radians).	A large magnitude argument may yield a result with little or
     no significance.  For a discussion of error due to roundoff, see math(3).

RETURN VALUES
     The ttaann() function returns the tangent value.

SEE ALSO
     aaccooss(3), aassiinn(3), aattaann(3), aattaann22(3), ccooss(3), ccoosshh(3), math(3), ssiinn(3),
     ssiinnhh(3), ttaannhh(3)

STANDARDS
     The ttaann() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The ttaannff function is thread safe.
     The ttaannff function is nnoott async-signal safe.

