iconv_close(3)                                           iconv_close(3)

  iiccoonnvv__cclloossee(())

  NNAAMMEE

    iconv_close() - codeset conversion deallocation function

  SSYYNNOOPPSSIISS

    #include <iconv.h>

    int iconv_close (iconv_t cd)

  DDEESSCCRRIIPPTTIIOONN

    The iiccoonnvv__cclloossee(3) function deallocates the conversion descriptor cd and
    all other associated resources allocated by _ii_cc_oo_nn_vv____oo_pp_ee_nn(3).

    If a file descriptor is used to implement the type iconv_t, that file
    descriptor will be closed.

    The arguments are:

    cd
        Conversion descriptor returned by a call to _ii_cc_oo_nn_vv____oo_pp_ee_nn(3).

  RREETTUURRNN VVAALLUUEESS

    If the iiccoonnvv__cclloossee(3) call is successful 0 is returned. Otherwise, -1 is
    returned and errno is set to indicate the error.

  EERRRROORRSS

    The iiccoonnvv__cclloossee(3) call may fail if:

    [EBADF]
        The conversion descriptor is invalid.

  SSEEEE AALLSSOO

    _i_c_o_n_v___o_p_e_n(3)

    _i_c_o_n_v(3)

  UUSSAAGGEE NNOOTTEESS

    The iiccoonnvv__cclloossee function is thread safe.

    The iiccoonnvv__cclloossee function is not async-signal safe.

