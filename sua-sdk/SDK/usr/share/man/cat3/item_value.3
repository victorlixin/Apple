item_value(3)                                             item_value(3)

  mmiitteemm__vvaalluuee(())

  NNAAMMEE

    set_item_value(), item_value() - set and get menu item values

  SSYYNNOOPPSSIISS

    #include <menu.h>

    char *set_item_value(ITEM *item, int bool);
    char *item_value(ITEM *item);

  DDEESSCCRRIIPPTTIIOONN

    If you turn off the menu option O_ONEVALUE (e.g., with _ss_ee_tt____mm_ee_nn_uu____oo_pp_tt_ss(3) or
    _mm_ee_nn_uu____oo_pp_tt_ss____oo_ff_ff(3)), the menu becomes multi-valued; that is, more than one
    item may simultaneously be selected.

    In a multi_valued menu, you can used to select the given menu item (second
    argument TRUE) or deselect it (second argument FALSE).

  RREETTUURRNN VVAALLUUEE

    The function _ss_ee_tt____ii_tt_ee_mm____vv_aa_ll_uu_ee(3) returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_REQUEST_DENIED
        The menu driver could not process the request.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3)

    _m_e_n_u___o_p_t_s(3)

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

