wcstok(3)                                                     wcstok(3)

  wwccssttookk(())

  NNAAMMEE

    wcstok() - wide-character string tokens

  SSYYNNOOPPSSIISS

    #include <wchar.h>

    wchar_t * wcstok (wchar_t *wstr, const wchar_t *wsep)

  DDEESSCCRRIIPPTTIIOONN

    The _ww_cc_ss_tt_oo_kk(3) function is used to isolate sequential tokens in a wide-
    character string, str. These tokens are separated in the string by at
    least one of the wide-characters in wsep. The first time that _ww_cc_ss_tt_oo_kk(3) is
    called, wstr should be specified; subsequent calls, wishing to obtain
    further tokens from the same string, should pass a null pointer instead.
    The separator string, wsep, must be supplied each time, and may change
    between calls.

    The _ww_cc_ss_tt_oo_kk(3) function returns a wide-character pointer to the beginning
    of each subsequent token in the string, after replacing the token itself
    with a wide-character NUL code. When no more tokens remain, a null pointer
    is returned.

  SSEEEE AALLSSOO

    _m_e_m_c_h_r(3)

    _w_c_s_c_h_r(3)

    _w_c_s_c_s_p_n(3)

    _w_c_s_p_b_r_k(3)

    _w_c_s_r_c_h_r(3)

    _w_c_s_s_e_p(3)

    _w_c_s_s_p_n(3)

    _w_c_s_s_t_r(3)

  UUSSAAGGEE NNOOTTEESS

    The wwccssttookk function is thread safe.

    The wwccssttookk function is not async-signal safe.

