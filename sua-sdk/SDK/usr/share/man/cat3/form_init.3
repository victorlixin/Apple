form_init(3)                                               form_init(3)

  ffoorrmm__hhooookk(())

  NNAAMMEE

    set_field_init(), set_field_term(), field_term(), form_init(),
    set_form_init(), set_form_term(), form_term() - set hooks for automatic
    invocation by applications

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_field_init(FORM *form, void (*func)(FORM *));
    void (*)(FORM *) field_init(MWNU *form);
    int set_field_term(FORM *form, void (*func)(FORM *));
    void (*)(FORM *) field_term(MWNU *form);
    int set_form_init(FORM *form, void (*func)(FORM *));
    void (*)(FORM *) form_init(MWNU *form);
    int set_form_term(FORM *form, void (*func)(FORM *));
    void (*)(FORM *) form_term(MWNU *form);

  DDEESSCCRRIIPPTTIIOONN

    These functions make it possible to set hook functions to be called at
    various points in the automatic processing of input event codes by
    _ff_oo_rr_mm____dd_rr_ii_vv_ee_rr(3).

    The function _ss_ee_tt____ff_ii_ee_ll_dd____ii_nn_ii_tt(3) sets a hook to be called at form-post time
    and each time the selected field changes (after the change). _ff_ii_ee_ll_dd____ii_nn_ii_tt(3)
    returns the current field init hook, if any (NULL if there is no such
    hook).

    The function _ss_ee_tt____ff_ii_ee_ll_dd____tt_ee_rr_mm(3) sets a hook to be called at form-unpost
    time and each time the selected field changes (before the change).
    _ff_ii_ee_ll_dd____tt_ee_rr_mm(3) returns the current field term hook, if any (NULL if there
    is no such hook).

    The function _ss_ee_tt____ff_oo_rr_mm____ii_nn_ii_tt(3) sets a hook to be called at form-post time
    and just after a page change once it is posted. _ff_oo_rr_mm____ii_nn_ii_tt(3) returns the
    current form init hook, if any (NULL if there is no such hook).

    The function _ss_ee_tt____ff_oo_rr_mm____tt_ee_rr_mm(3) sets a hook to be called at form-unpost time
    and just before a page change change once it is posted. _ff_oo_rr_mm____ii_nn_ii_tt(3)
    returns the current form term hook, if any (NULL if there is no such
    hook).

  RREETTUURRNN VVAALLUUEE

    Routines that return pointers return NULL on error. Other routines return
    one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _f_o_r_m_s(3)

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

