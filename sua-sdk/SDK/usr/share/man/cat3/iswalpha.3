iswalpha(3)                                                 iswalpha(3)

  iisswwaallpphhaa(())

  NNAAMMEE

    iswalpha() - alphabetic wide-character test

  SSYYNNOOPPSSIISS

    #include <wctype.h>

    int iswalpha (wint_t wc)

  DDEESSCCRRIIPPTTIIOONN

    The _ii_ss_ww_aa_ll_pp_hh_aa(3) function tests whether wc is a wide-character code of
    class aallpphhaa in the program's current locale.

    In the POSIX locale, the character class aallpphhaa includes all characters in
    the classes uuppppeerr and lloowweerr. The character class aallpphhaa excludes all
    characters in the classes ccnnttrrll, ddiiggiitt, ppuunncctt, and ssppaaccee.

  RREETTUURRNN VVAALLUUEESS

    The _ii_ss_ww_aa_ll_pp_hh_aa(3) function returns zero if the wide-character code tests
    false and returns non-zero if the wide-character code tests true.

  SSEEEE AALLSSOO

    _i_s_w_a_l_n_u_m(3)

    _i_s_w_b_l_a_n_k(3)

    _i_s_w_c_n_t_r_l(3)

    _i_s_w_d_i_g_i_t(3)

    _i_s_w_g_r_a_p_h(3)

    _i_s_w_l_o_w_e_r(3)

    _i_s_w_p_r_i_n_t(3)

    _i_s_w_p_u_n_c_t(3)

    _i_s_w_s_p_a_c_e(3)

    _i_s_w_u_p_p_e_r(3)

    _i_s_w_x_d_i_g_i_t(3)

    _t_o_w_l_o_w_e_r(3)

    _t_o_w_u_p_p_e_r(3)

  UUSSAAGGEE NNOOTTEESS

    The iisswwaallpphhaa function is thread safe.

    The iisswwaallpphhaa function is not async-signal safe.

