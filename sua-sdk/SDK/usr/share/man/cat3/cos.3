COS(3)		                                        		COS(3)

NAME
     cos, cosf -- cosine functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     cos(double x);

     float
     cosf(float x);

DESCRIPTION
     The ccooss() and the ccoossff() functions compute the cosine of x (measured in
     radians).	A large magnitude argument may yield a result with little or
     no significance.  For a discussion of error due to roundoff, see math(3).

RETURN VALUES
     The ccooss() and the ccoossff() functions return the cosine value.

SEE ALSO
     aaccooss(3), aassiinn(3), aattaann(3), aattaann22(3), ccoosshh(3), math(3), ssiinn(3), ssiinnhh(3),
     ttaann(3), ttaannhh(3)

STANDARDS
     The ccooss() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The ccooss function is thread safe.
     The ccooss function is nnoott async-signal safe.

