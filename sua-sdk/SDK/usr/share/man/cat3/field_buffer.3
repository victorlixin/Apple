field_buffer(3)                                         field_buffer(3)

  ffoorrmm__ffiieelldd__bbuuffffeerr(())

  NNAAMMEE

    set_field_buffer(), field_buffer(), set_field_status(), field_status(),
    set_max_field() - field buffer control

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_field_buffer(FIELD *field, int buf, char *value);
    char *field_buffer(FIELD *field, int buffer);
    int set_field_status(FIELD *field, int status);
    int field_status(FIELD *field);
    int set_max_field(FIELD *field, int max);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____ff_ii_ee_ll_dd____bb_uu_ff_ff_ee_rr(3) sets the numbered buffer of the given
    field to contain a given string. Buffer 0 is the displayed value of the
    field; other numbered buffers may be allocated by applications through the
    nbuffers argument of _nn_ee_ww____ff_ii_ee_ll_dd(3) but are not manipulated by the forms
    library. The function _ff_ii_ee_ll_dd____bb_uu_ff_ff_ee_rr(3) returns the foreground attribute.

    The function _ss_ee_tt____ff_ii_ee_ll_dd____ss_tt_aa_tt_uu_ss(3) sets the associated status flag of field;
    _ff_ii_ee_ll_dd____ss_tt_aa_tt_uu_ss(3) gets the current value. The status flag is set to a
    nonzero value whenever the field changes.

    The function _ss_ee_tt____mm_aa_xx____ff_ii_ee_ll_dd(3) sets the maximum size for a dynamic field.
    An argument of 0 turns off any maximum size threshold for that field.

  RREETTUURRNN VVAALLUUEE

    The _ff_ii_ee_ll_dd____bb_uu_ff_ff_ee_rr(3) function returns NULL on error.

    The _ff_ii_ee_ll_dd____ss_tt_aa_tt_uu_ss(3) function returns TRUE or FALSE.

    The remaining routines return one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        errno )

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _n_e_w___f_i_e_l_d(3)

    _f_o_r_m_s(3) and 3X pages whose names begin form_ for detailed descriptions of
    the entry points.

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

