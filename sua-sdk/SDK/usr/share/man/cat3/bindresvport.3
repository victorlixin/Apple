bindresvport(3)                                         bindresvport(3)

  bbiinnddrreessvvppoorrtt(())

  NNAAMMEE

    bindresvport() - bind a socket to a privileged IP port

  SSYYNNOOPPSSIISS

    #include <sys/types.h>
    #include <netinet/in.h>

    int bindresvport(int sd, struct socaddr_in *sin)

  DDEESSCCRRIIPPTTIIOONN

    _bb_ii_nn_dd_rr_ee_ss_vv_pp_oo_rr_tt(3) is used to bind a socket descriptor to a privileged IP
    port, that is, a port number in the range 0-1023. The routine returns 0 if
    it is successful, otherwise -1 is returned and eerrrrnnoo set to reflect the
    cause of the error.

    Only a privileged user can bind to a privileged port; this call will fail
    for any other users.

  UUSSAAGGEE NNOOTTEESS

    The bbiinnddrreessvvppoorrtt function is not thread safe.

    The bbiinnddrreessvvppoorrtt function is not async-signal safe.

