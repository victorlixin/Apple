minor(3)                                                       minor(3)

  mmkkddeevv(())

  NNAAMMEE

    mkdev(), major(), minor() - create a special device number

  SSYYNNOOPPSSIISS

    #include <sys/types.h>
    #include <sys/mkdev.h>

    dev_t mkdev (dev_t maj, dev_t min)
    dev_t major (dev_t device)
    dev_t minor (dev_t device)

  DDEESSCCRRIIPPTTIIOONN

    The mmkkddeevv(3) macro returns a formatted device number using the major and
    minor device number components specified by the maj and min arguments,
    respectively. These values are obtained by calling mmaajjoorr(3) and mmiinnoorr(3)
    for the device specified by the device argument.

  RREETTUURRNN VVAALLUUEESS

    The mmkkddeevv(3) macro returns a formatted device number.

    The mmaajjoorr(3) macro returns a major device number component for a device.

    The mmiinnoorr(3) macro returns a minor device number component for a device.

  EERRRROORRSS

    None.

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

