SQRT(3) 	                                        	       SQRT(3)

NAME
     cbrt, cbrtf, sqrt, sqrtf -- cube root and square root functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     cbrt(double x);

     float
     cbrtf(float x);

     double
     sqrt(double x);

     float
     sqrtf(float x);

DESCRIPTION
     The ccbbrrtt() and the ccbbrrttff() functions compute the cube root of x.

     The ssqqrrtt() and the ssqqrrttff() functions compute the non-negative square root
     of x.

RETURN VALUES
     The ccbbrrtt() and the ccbbrrttff() functions return the requested cube root.  The
     ssqqrrtt() and the ssqqrrttff() functions return the requested square root unless
     an error occurs.  An attempt to take the ssqqrrtt() of negative x raises an
     invalid exception and causes an NaN to be returned.

SEE ALSO
     fenv(3), math(3)

STANDARDS
     The ssqqrrtt() function conforms to ISO/IEC 9899:1990 (``ISO C90'').

HISTORY
     The ccbbrrtt() function appeared in 4.3BSD.


USAGE NOTES

     The ssqqrrtt function is thread safe.
     The ssqqrrtt function is nnoott async-signal safe.

