mitem_current(3)                                       mitem_current(3)

  mmiitteemm__ccuurrrreenntt(())

  NNAAMMEE

    set_current_item(), current_item(), set_top_row(), top_row(), item_index()
    - set and get current_menu_item

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_current_item(MENU *menu, ITEM *item);
    ITEM *current_item(MENU *menu);
    int set_top_row(MENU *menu, int row);
    int top_row(MENU *menu);
    int item_index(ITEM *item);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____cc_uu_rr_rr_ee_nn_tt____ii_tt_ee_mm(3) sets the current item (the item on which
    the menu cursor is positioned). _cc_uu_rr_rr_ee_nn_tt____ii_tt_ee_mm(3) returns a pointer to the
    current item in the given menu.

    The function _ss_ee_tt____tt_oo_pp____rr_oo_ww(3) sets the top row of the menu to show the given
    row (the top row is initially 0, and is reset to this value whenever the
    O_ROWMAJOR option is toggled). The item leftmost on the given row becomes
    current. The function _tt_oo_pp____rr_oo_ww(3) returns the number of the top menu row
    being displayed.

    The function _ii_tt_ee_mm____ii_nn_dd_ee_xx(3) returns the (zero-origin) index of item in the
    menu's item pointer list.

  RREETTUURRNN VVAALLUUEE

    _cc_uu_rr_rr_ee_nn_tt____ii_tt_ee_mm(3) returns NULL on error.

    _tt_oo_pp____rr_oo_ww(3) and _ii_tt_ee_mm____ii_nn_dd_ee_xx(3) return ERR (the general ccuurrsseess error value)
    on error.

    _ss_ee_tt____cc_uu_rr_rr_ee_nn_tt____ii_tt_ee_mm(3) and _ss_ee_tt____tt_oo_pp____rr_oo_ww(3) return one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_BAD_STATE
        Routine was called from an initialization or termination function.

    E_NOT_CONNECTED
        No items are connected to the menu.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3)

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

    The SVr4 menu library documentation specifies the _tt_oo_pp____rr_oo_ww(3) and
    _ii_tt_ee_mm____ii_nn_dd_ee_xx(3) error value as -1 (which is the value of ERR).

