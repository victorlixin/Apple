set_menu_items(3)                                     set_menu_items(3)

  mmeennuu__iitteemmss(())

  NNAAMMEE

    set_menu_items(), menu_items(), item_count() - make and break connections
    between items and menus

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_menu_items(MENU *menu, ITEM **items);
    ITEM **menu_items(MENU *menu);
    int item_count(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____mm_ee_nn_uu____ii_tt_ee_mm_ss(3) changes the item pointer array of the given
    menu. The array must be terminated by a NULL.

    The function _mm_ee_nn_uu____ii_tt_ee_mm_ss(3) returns the item array of the given menu.

    The function _ii_tt_ee_mm____cc_oo_uu_nn_tt(3) returns the count of items in menu.

  RREETTUURRNN VVAALLUUEESS

    The function _mm_ee_nn_uu____ii_tt_ee_mm_ss(3) returns NULL on error.

    The function _ii_tt_ee_mm____cc_oo_uu_nn_tt(3) returns ERR (the general ccuurrsseess error return
    value) on error.

    The function _ss_ee_tt____mm_ee_nn_uu____ii_tt_ee_mm_ss(3) returns one of the following codes on
    error:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_POSTED
        The menu is already posted.

    E_NOT_CONNECTED
        No items are connected to the menu.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3).

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

    The SVr4 menu library documentation specifies the _ii_tt_ee_mm____cc_oo_uu_nn_tt(3) error
    value as -1 (which is the value of ERR).

