raise(3)                                                       raise(3)

  rraaiissee(())

  NNAAMMEE

    raise() - send a signal to the current thread or process

  SSYYNNOOPPSSIISS

    #include <signal.h>

    int raise (int signum)

  DDEESSCCRRIIPPTTIIOONN

    The _rr_aa_ii_ss_ee(3) function sends the signal signum to the current thread or
    process.

    This is equivalent to

    pthread_kill(pthread_self(), signum)

  RREETTUURRNN VVAALLUUEESS

    Upon successful completion, a value of 0 is returned. Otherwise, a value
    of -1 is returned and the global variable errno is set to indicate the
    error.

  EERRRROORRSS

    The _rr_aa_ii_ss_ee(3) function may fail and set errno for any of the errors
    specified for the library functions _pp_tt_hh_rr_ee_aa_dd____ss_ee_ll_ff(2) and _pp_tt_hh_rr_ee_aa_dd____kk_ii_ll_ll(2).

  SSEEEE AALLSSOO

    _k_i_l_l(2)

  UUSSAAGGEE NNOOTTEESS

    The rraaiissee function is thread safe.

    The rraaiissee function is async-signal safe.

