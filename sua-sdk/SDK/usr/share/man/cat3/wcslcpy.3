wcslcpy(3)                                                   wcslcpy(3)

  wwccssccppyy(())

  NNAAMMEE

    wcscpy(), wcslcpy(), wcsncpy() - copy wide-character strings

  SSYYNNOOPPSSIISS

    #include <wchar.h>

    wchar_t * wcscpy (wchar_t *dst, const wchar_t *src)
    size_t wcslcpy (wchar_t *dst, const wchar_t *src, size_t len)
    wchar_t * wcsncpy (wchar_t *dst, const wchar_t *src, size_t len)

  DDEESSCCRRIIPPTTIIOONN

    The _ww_cc_ss_cc_pp_yy(3) and _ww_cc_ss_nn_cc_pp_yy(3) functions copy the wide-character string src
    to dst (including the terminating \0 wide-character code).

    The _ww_cc_ss_nn_cc_pp_yy(3) copies no more than len characters into dst, appending \0
    wide-character codes if src is less than len characters long, and not
    terminating dst if src is more than len characters long.

    The _ww_cc_ss_ll_cc_pp_yy(3) function is designed to be a safer, more consistent, and
    less error prone replacement for the _ww_cc_ss_nn_cc_pp_yy(3) function. Unlike the
    _ww_cc_ss_nn_cc_pp_yy(3) function, _ww_cc_ss_ll_cc_pp_yy(3) takes the full size of the buffer (not
    just the length) and guaranteees to NUL-terminate the result (as long as
    len is at larger than 0 or as long as there is at least one wide-character
    column free in dst. Note that _ww_cc_ss_ll_cc_pp_yy(3) only operates on true wide-
    character strings. This means that src must be NUL-terminated.

  RREETTUURRNN VVAALLUUEESS

    The _ww_cc_ss_cc_pp_yy(3) and _ww_cc_ss_nn_cc_pp_yy(3) functions return dst. The _ww_cc_ss_ll_cc_pp_yy(3) function
    returns the number of wide-characters copied to dst, not including the NUL
    terminator.

  EEXXAAMMPPLLEESS

    The following sets chararray to abc\0\0\0:

    (void)wcsncpy(chararray, "abc", 6).

    The following sets chararray to abcdef:

    (void)wcsncpy(chararray, "abcdefgh", 6);

  SSEEEE AALLSSOO

    _b_c_o_p_y(3)

    _m_e_m_c_p_y(3)

    _m_e_m_m_o_v_e(3)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

