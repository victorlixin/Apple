has_il(3)                                                     has_il(3)

  ccuurrss__tteerrmmaattttrrss(())

  NNAAMMEE

    baudrate(), erasechar(), has_ic(), has_il(), killchar(), longname(),
    termattrs(), termname() - curses environment query routines

  SSYYNNOOPPSSIISS

    #include <curses.h>

    int baudrate(void);
    char erasechar(void);
    int has_ic(void);
    int has_il(void);
    char killchar(void);
    char *longname(void);
    attr_t termattrs(void);
    char *termname(void);

  DDEESSCCRRIIPPTTIIOONN

    The _bb_aa_uu_dd_rr_aa_tt_ee(3) routine returns the output speed of the terminal. The
    number returned is in bits per second, for example 9600, and is an
    integer.

    The _ee_rr_aa_ss_ee_cc_hh_aa_rr(3) routine returns the user's current erase character.

    The _hh_aa_ss____ii_cc(3) routine is true if the terminal has insert- and delete-
    character capabilities.

    The _hh_aa_ss____ii_ll(3) routine is true if the terminal has insert- and delete-line
    capabilities, or can simulate them using scrolling regions. This might be
    used to determine if it would be appropriate to turn on physical scrolling
    using _ss_cc_rr_oo_ll_ll_oo_kk(3).

    The _kk_ii_ll_ll_cc_hh_aa_rr(3) routine returns the user's current line kill character.

    The _ll_oo_nn_gg_nn_aa_mm_ee(3) routine returns a pointer to a static area containing a
    verbose description of the current terminal. The maximum length of a
    verbose description is 128 characters. It is defined only after the call
    to _ii_nn_ii_tt_ss_cc_rr(3) or _nn_ee_ww_tt_ee_rr_mm(3). The area is overwritten by each call to
    _nn_ee_ww_tt_ee_rr_mm(3) and is not restored by _ss_ee_tt____tt_ee_rr_mm(3), so the value should be
    saved between calls to _nn_ee_ww_tt_ee_rr_mm(3) if _ll_oo_nn_gg_nn_aa_mm_ee(3) is going to be used with
    multiple terminals.

    If a given terminal doesn't support a video attribute that an application
    program is trying to use, _cc_uu_rr_ss_ee_ss(3) may substitute a different video
    attribute for it. The _tt_ee_rr_mm_aa_tt_tt_rr_ss(3) function returns a logical OR of all
    video attributes supported by the terminal. This information is useful
    when a _cc_uu_rr_ss_ee_ss(3) program needs complete control over the appearance of the
    screen.

    The _tt_ee_rr_mm_nn_aa_mm_ee(3) routine returns the value of the environment variable TERM
    (truncated to 14 characters).

  RREETTUURRNN VVAALLUUEE

    _ll_oo_nn_gg_nn_aa_mm_ee(3) and _tt_ee_rr_mm_nn_aa_mm_ee(3) return NULL on error.

    Routines that return an integer return ERR upon failure and OK (SVr4 only
    specifies "an integer value other than ERR") upon successful completion.

  NNOOTTEESS

    Note that _tt_ee_rr_mm_aa_tt_tt_rr_ss(3) may be a macro.

  PPOORRTTAABBIILLIITTYY

    The XSI Curses standard, Issue 4 describes these functions. It changes the
    return type of _tt_ee_rr_mm_aa_tt_tt_rr_ss(3) to the new type aattttrr__tt.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _c_u_r_s___i_n_i_t_s_c_r(3)

    _c_u_r_s___o_u_t_o_p_t_s(3)

