field_userptr(3)                                       field_userptr(3)

  ffoorrmm__ffiieelldd__uusseerrppttrr(())

  NNAAMMEE

    set_field_userptr(), field_userptr() - associate application data with a
    form field

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_field_userptr(FIELD *field, char *userptr);
    char field_userptr(FIELD *field);

  DDEESSCCRRIIPPTTIIOONN

    Every form field has a field that can be used to hold application-specific
    data (that is, the form-driver code leaves it alone). These functions get
    and set that field.

  RREETTUURRNN VVAALLUUEE

    The function _ff_ii_ee_ll_dd____uu_ss_ee_rr_pp_tt_rr(3) returns NULL on error. The function
    _ss_ee_tt____ff_ii_ee_ll_dd____uu_ss_ee_rr_pp_tt_rr(3) returns(3) one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno)

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _f_o_r_m_s(3)

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

    The user pointer should be a void pointer. We leave it as a char pointer
    for SVr4 compatibility.

