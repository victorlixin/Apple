menu_cursor(3)                                           menu_cursor(3)

  mmeennuu__ccuurrssoorr(())

  NNAAMMEE

    pos_menu_cursor() - position a menu's cursor

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int pos_menu_cursor(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    The function _pp_oo_ss____mm_ee_nn_uu____cc_uu_rr_ss_oo_rr(3) restores the cursor to the current
    position associated with the menu's selected item. This is useful after
    ccuurrsseess routines have been called to do screen-painting in response to a
    menu select.

  RREETTUURRNN VVAALLUUEE

    This routine returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_NOT_POSTED
        The menu has not been posted.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3).

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

