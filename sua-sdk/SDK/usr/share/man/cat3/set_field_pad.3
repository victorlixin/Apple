set_field_pad(3)                                       set_field_pad(3)

  ffoorrmm__ffiieelldd__aattttrriibbuutteess(())

  NNAAMMEE

    set_field_fore(), field_fore(), set_field_back(), field_back(),
    set_field_pad(), field_pad() - color and attribute control for form fields

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_field_fore(FIELD *field, chtype attr);
    chtype field_fore(FIELD *field);
    int set_field_back(FIELD *field, chtype attr);
    chtype field_back(FIELD *field);
    int set_field_pad(FIELD *field, int pad);
    chtype field_pad(FIELD *field);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____ff_ii_ee_ll_dd____ff_oo_rr_ee(3) sets the foreground attribute of field.
    This is the highlight used to display the field contents. The function
    _ff_ii_ee_ll_dd____ff_oo_rr_ee(3) returns the foreground attribute. The default is A_STANDOUT.

    The function _ss_ee_tt____ff_ii_ee_ll_dd____bb_aa_cc_kk(3) sets the background attribute of form. This
    is the highlight used to display the extent fields in the form. The
    function _ff_ii_ee_ll_dd____bb_aa_cc_kk(3) returns the background attribute. The default is
    A_NORMAL.

    The function _ss_ee_tt____ff_ii_ee_ll_dd____pp_aa_dd(3) sets the character used to fill the field.
    The function _ff_ii_ee_ll_dd____pp_aa_dd(3) returns the given form's pad character. The
    default is a blank.

  RREETTUURRNN VVAALLUUEE

    These routines return one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3) and 3X pages whose names begin form_ for detailed descriptions
    of the entry points.

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

