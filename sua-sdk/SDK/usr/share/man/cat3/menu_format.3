menu_format(3)                                           menu_format(3)

  mmeennuu__ffoorrmmaatt(())

  NNAAMMEE

    set_menu_format(), menu_format() - set and get menu sizes

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_menu_format(MENU *menu, int rows, int cols);
    int menu_format(MENU *menu, int *rows, int *cols);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____mm_ee_nn_uu____ff_oo_rr_mm_aa_tt(3) sets the maximum display size of the given
    window. If this size is too small to display all menu items, the menu will
    be made scrollable.

    The default format is 16 rows, 1 column. Calling _ss_ee_tt____mm_ee_nn_uu____ff_oo_rr_mm_aa_tt(3) with a
    null menu pointer will change this default. A zero row or column argument
    to _ss_ee_tt____mm_ee_nn_uu____ff_oo_rr_mm_aa_tt(3) is interpreted as a request not to change the
    current value.

    The function _mm_ee_nn_uu____ff_oo_rr_mm_aa_tt(3) returns the maximum-size constraints for the
    given menu into the storage addressed by rows and cols.

  RREETTUURRNN VVAALLUUEE

    These routines returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_POSTED
        The menu is already posted.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3).

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

