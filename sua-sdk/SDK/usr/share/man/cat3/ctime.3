ctime(3)                                                       ctime(3)

  ccttiimmee(())

  NNAAMMEE

    ctime(), ctime_r() - transform binary date and time value to ASCII

  SSYYNNOOPPSSIISS

    #include <sys/types.h>
    #include <time.h>

    char * ctime (const time_t *clock)
    char * ctime_r (const time_t *clock, char *buf)

  DDEESSCCRRIIPPTTIIOONN

    The function _cc_tt_ii_mm_ee(3) function takes as an argument a time value
    representing the time in seconds since the Epoch (00:00:00 UTC, January 1,
    1970; see _tt_ii_mm_ee(2)).

    The _cc_tt_ii_mm_ee(3) function adjusts the time value for the current time zone and
    returns a pointer to a 26-character string of the form:

    Thu Nov 24 18:22:48 1986\n\0

    All the fields have constant width.

    The reentrant ccttiimmee__rr(3) function converts the calendar time referenced by
    clock to local time in exactly the same form as ccttiimmee(), puts the string
    into the array identified by buf (which must be at least 26 bytes in
    size), and returns buf.

  SSEEEE AALLSSOO

    _d_a_t_e(1)

    _a_s_c_t_i_m_e(3)

    _d_i_f_f_t_i_m_e(3)

    _g_e_t_e_n_v(3)

    _g_m_t_i_m_e(3)

    _l_o_c_a_l_t_i_m_e(3)

    _m_k_t_i_m_e(3)

    _t_i_m_e(2)

  UUSSAAGGEE NNOOTTEESS

    The ccttiimmee__rr function is thread safe. The ccttiimmee function is not thread
    safe.

    None of these functions are async-signal safe.

