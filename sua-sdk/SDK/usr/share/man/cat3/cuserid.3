cuserid(3)                                                   cuserid(3)

  ccuusseerriidd(())

  NNAAMMEE

    cuserid() - return user ID (login name) as a string

  SSYYNNOOPPSSIISS

    #include <stdio.h>

    char * cuserid (char *s)

  DDEESSCCRRIIPPTTIIOONN

    The _cc_uu_ss_ee_rr_ii_dd(3) function returns the login name of the real owner of the
    process.

    If s is NULL, then the function returns a pointer to a static buffer,
    which may be overwritten by subsequent calls to ccuusseerriidd(3). Consequently,
    this function is thread safe only if the s argument is not NULL.

    If s is not NULL, it's assumed to be a buffer that is at least {L_cuserid}
    bytes long; {L_cuserid} is defined in <<ssttddiioo..hh>>.

  RREETTUURRNNSS

    If s is not NULL, _cc_uu_ss_ee_rr_ii_dd(3) returns s; if s is NULL, _cc_uu_ss_ee_rr_ii_dd(3) returns a
    pointer to an internal buffer.

    If the login name was found, it will be in the appropriate buffer. If the
    login name could not be found, the buffer contains a null byte `\0'.

  SSEEEE AALLSSOO

    _g_e_t_l_o_g_i_n(2)

    _g_e_t_p_w_n_a_m(2)

    _g_e_t_p_w_u_i_d(2)

    _g_e_t_u_i_d(2)

    _g_e_t_e_u_i_d(2)

  UUSSAAGGEE NNOOTTEESS

    The ccuusseerriidd function is thread safe.

    The ccuusseerriidd function is not async-signal safe.

