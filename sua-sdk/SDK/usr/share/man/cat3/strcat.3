strcat(3)                                                     strcat(3)

  ssttrrccaatt(())

  NNAAMMEE

    strcat(), strncat() - concatenate strings

  SSYYNNOOPPSSIISS

    #include <string.h>

    char * strcat (char *s, const char *append)
    char * strncat (char *s, const char *append, size_t count)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_cc_aa_tt(3) and _ss_tt_rr_nn_cc_aa_tt(3) functions append a copy of the null-
    terminated string append to the end of the null-terminated string s, then
    add a terminating \0. The string s must have sufficient space to hold the
    result.

    The _ss_tt_rr_nn_cc_aa_tt(3) function appends not more than count characters.

  RREETTUURRNN VVAALLUUEESS

    The _ss_tt_rr_cc_aa_tt(3) and _ss_tt_rr_nn_cc_aa_tt(3) functions return the pointer s.

  SSEEEE AALLSSOO

    _b_c_o_p_y(3)

    _m_e_m_c_p_y(3)

    _m_e_m_m_o_v_e(3)

    _s_t_r_c_p_y(3)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

