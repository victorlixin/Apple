set_menu_mark(3)                                       set_menu_mark(3)

  mmeennuu__mmaarrkk(())

  NNAAMMEE

    menu_mark(), set_menu_mark() - get and set the menu mark string

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_menu_mark(MENU *menu, char *mark);
    char *menu_mark(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    In order to make menu selections visible on older terminals without
    highlighting or color capability, the menu library marks selected items in
    a menu with a prefix string.

    The function _ss_ee_tt____mm_ee_nn_uu____mm_aa_rr_kk(3) sets the mark string for the given menu.
    Calling _ss_ee_tt____mm_ee_nn_uu____mm_aa_rr_kk(3) with a null menu item will abolish the mark
    string. Note that changing the length of the mark string for a menu while
    the menu is posted is likely to produce unhelpful behavior.

    The default string is "-" (a dash). Calling _ss_ee_tt____mm_ee_nn_uu____mm_aa_rr_kk(3) wiith a NULL
    menu argument will change this default.

    The function _mm_ee_nn_uu____mm_aa_rr_kk(3) returns the menu's mark string (or NULL if there
    is none).

  RREETTUURRNN VVAALLUUEE

    The function _mm_ee_nn_uu____mm_aa_rr_kk(3) returns NULL on error. The function
    _ss_ee_tt____mm_ee_nn_uu____mm_aa_rr_kk(3) may return the following error codes:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3)

