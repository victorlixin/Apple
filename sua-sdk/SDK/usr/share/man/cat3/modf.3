MODF(3) 	                                        	       MODF(3)

NAME
     modf -- extract signed integral and fractional values from floating-point
     number

LIBRARY
     Standard C Library (libc, -lc)

SYNOPSIS
     #include <math.h>

     double
     modf(double value, double *iptr);

DESCRIPTION
     The mmooddff() function breaks the argument value into integral and frac-
     tional parts, each of which has the same sign as the argument.  It stores
     the integral part as a double in the object pointed to by iptr.

RETURN VALUES
     The mmooddff() function returns the signed fractional part of value.

SEE ALSO
     ffrreexxpp(3), llddeexxpp(3), math(3)

STANDARDS
     The mmooddff() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The mmooddff function is thread safe.
     The mmooddff function is nnoott async-signal safe.

