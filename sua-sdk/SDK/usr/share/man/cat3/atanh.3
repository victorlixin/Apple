ATANH(3)	                                        	      ATANH(3)

NAME
     atanh, atanhf -- inverse hyperbolic tangent functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     atanh(double x);

     float
     atanhf(float x);

DESCRIPTION
     The aattaannhh() and the aattaannhhff() functions compute the inverse hyperbolic
     tangent of the real argument x.  For a discussion of error due to round-
     off, see math(3).

RETURN VALUES
     The aattaannhh() and the aattaannhhff() functions return the inverse hyperbolic tan-
     gent of x if successful.  If the argument has absolute value 1, a divide-
     by-zero exception is raised and an infinity is returned.  If |x| > 1, an
     invalid exception is raised and an NaN is returned.

SEE ALSO
     aaccoosshh(3), aassiinnhh(3), eexxpp(3), fenv(3), math(3)

HISTORY
     The aattaannhh() function appeared in 4.3BSD.


USAGE NOTES

     The aattaannhh function is thread safe.
     The aattaannhh function is nnoott async-signal safe.

