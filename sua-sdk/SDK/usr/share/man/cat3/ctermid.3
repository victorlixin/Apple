ctermid(3)                                                   ctermid(3)

  cctteerrmmiidd(())

  NNAAMMEE

    ctermid() - generate terminal pathname

  SSYYNNOOPPSSIISS

    #include <stdio.h>

    char * ctermid (char *buf)

  DDEESSCCRRIIPPTTIIOONN

    The _cc_tt_ee_rr_mm_ii_dd(3) function generates a string that, when used as a pathname,
    refers to the current controlling terminal of the calling process.

  RREETTUURRNN VVAALLUUEESS

    If buf is the NULL pointer, the function returns a pointer to a static
    area, which may be overwritten by subsequent calls to cctteerrmmiidd(3).
    Consequently, this function is thread safe only if the buf argument is not
    NULL. If buf is not NULL, the pathname is copied into the memory
    referenced by buf. The argument buf is assumed to be at least {L_ctermid}
    (as defined in the include file <<ssttddiioo..hh>>) bytes long.

  NNOOTTEESS

    This implementation of the _cc_tt_ee_rr_mm_ii_dd(3) function always returns //ddeevv//ttttyy.

  SSEEEE AALLSSOO

    _t_t_y_n_a_m_e(2)

  UUSSAAGGEE NNOOTTEESS

    The cctteerrmmiidd function is thread safe.

    The cctteerrmmiidd function is not async-signal safe.

