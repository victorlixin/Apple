tmpnam(3)                                                     tmpnam(3)

  ttmmppffiillee(())

  NNAAMMEE

    tmpfile(), tmpnam() - temporary file routines

  SSYYNNOOPPSSIISS

    #include <stdio.h>

    FILE * tmpfile (void)
    char * tmpnam (char *str)

  DDEESSCCRRIIPPTTIIOONN

    The _tt_mm_pp_ff_ii_ll_ee(3) function returns a pointer to a stream associated with a
    file descriptor returned by the routine _mm_kk_ss_tt_ee_mm_pp(3). The created file is
    unlinked before _tt_mm_pp_ff_ii_ll_ee(3) returns, causing the file to be automatically
    deleted when the last reference to it is closed. The file is opened with
    the access value w+.

    The _tt_mm_pp_nn_aa_mm(3) function returns a pointer to a file name which did not
    reference an existing file at some indeterminate point in the past. If the
    argument s is non-NULL, the file name is copied to the buffer it
    references. Otherwise, the file name is copied to a static buffer. In
    either case, _tt_mm_pp_nn_aa_mm(3) returns a pointer to the file name.

    The buffer referenced by s is expected to be at least {L_tmpnam} bytes in
    length. {L_tmpnam} is defined in the include file <<ssttddiioo..hh>>.

    The environment variable TMPDIR (if set), the argument tmpdir (if non-
    NULL), and the directory //ttmmpp are tried, in that order, as directories in
    which to store the temporary file.

  RREETTUURRNN VVAALLUUEESS

    The _tt_mm_pp_ff_ii_ll_ee(3) function returns a pointer to an open file stream on
    success, and a NULL pointer on error.

    The _tt_mm_pp_nn_aa_mm(3) returns a pointer to a file name on success, and a NULL
    pointer on error.

  EERRRROORRSS

    The _tt_mm_pp_ff_ii_ll_ee(3) function may fail and set the global variable errno for any
    of the errors specified for the library functions _ff_dd_oo_pp_ee_nn(3) or _mm_kk_ss_tt_ee_mm_pp(3).

    The _tt_mm_pp_nn_aa_mm(3) function may fail and set errno for any of the errors
    specified for the library function _mm_kk_tt_ee_mm_pp(3).

    The _tt_ee_mm_pp_nn_aa_mm(3) function may fail and set errno for any of the errors
    specified for the library functions _mm_aa_ll_ll_oo_cc(3) or _mm_kk_tt_ee_mm_pp(3).

  SSEEEE AALLSSOO

    _m_k_t_e_m_p(3)

    _t_e_m_p_n_a_m(3)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

