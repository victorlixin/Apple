ualarm(3)                                                     ualarm(3)

  uuaallaarrmm(())

  NNAAMMEE

    ualarm() - schedule signal after specified time

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    useconds_t
    ualarm(useconds_t microseconds, useconds_t interval)

  DDEESSCCRRIIPPTTIIOONN

    The _uu_aa_ll_aa_rr_mm(3) function causes the system to generate a SIGALRM signal for
    the calling process after microseconds of real time have elapsed. If the
    interval is not zero, the signal occurs again every interval microseconds.

    If the SIGALRM signal is not caught or ignored, the calling process is
    terminated.

    The _uu_aa_ll_aa_rr_mm(3) function is a simplified interface to _ss_ee_tt_ii_tt_ii_mm_ee_rr(2).

  RREETTUURRNN VVAALLUUEE

    The _uu_aa_ll_aa_rr_mm(3) function returns the number of microseconds that remain on
    the existing _uu_aa_ll_aa_rr_mm(3) call. If there is no active _uu_aa_ll_aa_rr_mm(3) call (it has
    not been called yet or no time-outs are pending), then _uu_aa_ll_aa_rr_mm(3) returns
    0.

  SSEEEE AALLSSOO

    _a_l_a_r_m(2)

    _s_e_t_i_t_i_m_e_r(2)

    _s_l_e_e_p(2)

  UUSSAAGGEE NNOOTTEESS

    The uuaallaarrmm function is thread safe.

    The uuaallaarrmm function is not async-signal safe.

