ntohs(3)                                                       ntohs(3)

  hhttoonnll(())

  NNAAMMEE

    htonl(), htons(), ntohl(), ntohs() - convert values between host and
    network byte order

  SSYYNNOOPPSSIISS

    #include <arpa/inet.h>

    in_addr_t htonl (unsigned long hostlong)
    in_port_t htons (unsigned short hostshort)
    in_addr_t ntohl (unsigned long netlong)
    in_port_t ntohs (unsigned short netshort)

  DDEESSCCRRIIPPTTIIOONN

    These routines convert 16 and 32 bit quantities between network byte order
    and host byte order.

    These routines are most often used in conjunction with Internet addresses
    and ports as returned by _gg_ee_tt_hh_oo_ss_tt_bb_yy_nn_aa_mm_ee(2).

  NNOOTTEESS

    The _hh_tt_oo_nn_ss(3) and _nn_tt_oo_hh_ss(3) functions must have the correct prototype. If
    not declared, they are assumed to return an iinntt, and there is no guarantee
    that the high word will be zeroed.

  SSEEEE AALLSSOO

    _g_e_t_h_o_s_t_b_y_n_a_m_e(2)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

