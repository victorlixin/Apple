ROUND(3)	                                        	      ROUND(3)

NAME
     round, roundf, roundl -- round to nearest integral value

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     round(double x);

     float
     roundf(float x);

     long double
     roundl(long double x);

DESCRIPTION
     The rroouunndd(), rroouunnddff(), and roundl() functions return the nearest integral
     value to x; if x lies halfway between two integral values, then these
     functions return the integral value with the larger absolute value (i.e.,
     they round away from zero).

SEE ALSO
     cceeiill(3), fflloooorr(3), ieee(3), llrriinntt(3), llrroouunndd(3), math(3), rriinntt(3),
     ttrruunncc(3)

STANDARDS
     These functions conform to ISO/IEC 9899:1999 (``ISO C99'').

HISTORY
     The rroouunndd() and rroouunnddff() functions appeared in FreeBSD 5.3.  The roundl()
     function appeared in FreeBSD 6.0.


USAGE NOTES

     The rroouunndd function is thread safe.
     The rroouunndd function is nnoott async-signal safe.

