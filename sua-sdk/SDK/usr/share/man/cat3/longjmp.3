longjmp(3)                                                   longjmp(3)

  sseettjjmmpp(())

  NNAAMMEE

    setjmp(), longjmp(), sigsetjmp(), siglongjmp() - non-local jumps

  SSYYNNOOPPSSIISS

    #include <setjmp.h>

    int setjmp (jmp_buf env)
    void longjmp (jmp_buf env, int val)
    int sigsetjmp (sigjmp_buf env, int savemask)
    void siglongjmp (sigjmp_buf env, int val)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_ee_tt_jj_mm_pp(3) and _ss_ii_gg_ss_ee_tt_jj_mm_pp(3) functions save their calling environment in
    env. Each of these functions returns 0.

    The corresponding _ll_oo_nn_gg_jj_mm_pp(3) functions restore the environment saved by
    their most recent respective invocations of the _ss_ee_tt_jj_mm_pp(3) function. They
    then return so that program execution continues as if the corresponding
    invocation of the _ss_ee_tt_jj_mm_pp(3) call had just returned the value specified by
    val, instead of 0.

    _SS_ii_gg_ss_ee_tt_jj_mm_pp(3) and _ss_ii_gg_ll_oo_nn_gg_jj_mm_pp(3) save and restore the signal mask if the
    argument savemask is non-zero; otherwise they behave identically with
    _ss_ee_tt_jj_mm_pp(3) and _ll_oo_nn_gg_jj_mm_pp(3).

    The _ll_oo_nn_gg_jj_mm_pp(3) routines may not be called after the routine which called
    the _ss_ee_tt_jj_mm_pp(3) routines returns.

    All accessible objects have values as of the time _ll_oo_nn_gg_jj_mm_pp(3) routine was
    called, except that the values of objects of automatic storage invocation
    duration that do not have the vvoollaattiillee type and have been changed between
    the _ss_ee_tt_jj_mm_pp(3) invocation and _ll_oo_nn_gg_jj_mm_pp(3) call are indeterminate.

    Pairs of calls may be intermixed, i.e. both _ss_ii_gg_ss_ee_tt_jj_mm_pp(3) and _ss_ii_gg_ll_oo_nn_gg_jj_mm_pp(3)
    and _ss_ee_tt_jj_mm_pp(3) and _ll_oo_nn_gg_jj_mm_pp(3) combinations may be used in the same program,
    however, individual calls may not, e.g. the env argument to _ss_ee_tt_jj_mm_pp(3) may
    not be passed to _ss_ii_gg_ll_oo_nn_gg_jj_mm_pp(3).

  RREETTUURRNN VVAALLUUEESS

    When invoked directly, _ss_ee_tt_jj_mm_pp(3) returns zero; when invoked as part of
    _ll_oo_nn_gg_jj_mm_pp(3), _ss_ee_tt_jj_mm_pp(3) returns non-zero.

    When invoked directly, _ss_ii_gg_ss_ee_tt_jj_mm_pp(3) returns zero; when invoked as part of
    _ss_ii_gg_ll_oo_nn_gg_jj_mm_pp(3), _ss_ii_gg_ss_ee_tt_jj_mm_pp(3) returns non-zero.

  SSEEEE AALLSSOO

    _s_i_g_a_c_t_i_o_n(2)

    _s_i_g_n_a_l(2)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

