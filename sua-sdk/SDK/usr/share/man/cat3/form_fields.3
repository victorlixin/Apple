form_fields(3)                                           form_fields(3)

  ffoorrmm__ffiieelldd(())

  NNAAMMEE

    set_form_fields(), form_fields, field_count(), move_field() - make and
    break connections between fields and forms

  SSYYNNOOPPSSIISS

    #include <form.h>

    int set_form_fields(FORM *form, FIELD **fields);
    FIELD **form_fields(FORM *form);
    int field_count(FORM *form);
    int move_field(FIELD *field, int frow, int fcol);

  DDEESSCCRRIIPPTTIIOONN

    The function _ss_ee_tt____ff_oo_rr_mm____ff_ii_ee_ll_dd_ss(3) changes the field pointer array of the
    given form. The array must be terminated by a NULL.

    The function _ff_oo_rr_mm____ff_ii_ee_ll_dd_ss(3) returns the field array of the given form.

    The function _ff_ii_ee_ll_dd____cc_oo_uu_nn_tt(3) returns the count of fields in form.

    The function _mm_oo_vv_ee____ff_ii_ee_ll_dd(3) move the given field (which must be
    disconnected) to a specified location on the screen.

  RREETTUURRNN VVAALLUUEESS

    The function _ff_oo_rr_mm____ff_ii_ee_ll_dd_ss(3) returns NULL on error.

    The function _ff_ii_ee_ll_dd____cc_oo_uu_nn_tt(3) returns ERR (the general ccuurrsseess error return
    value) on error.

    The functions _ss_ee_tt____ff_oo_rr_mm____ff_ii_ee_ll_dd_ss(3) and _mm_oo_vv_ee____ff_ii_ee_ll_dd(3) return one of the
    following codes on error:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_POSTED
        The form is already posted.

    E_CONNECTED
        The field is already connected to a form.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _f_o_r_m_s(3)

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

    The SVr4 forms library documentation specifies the _ff_ii_ee_ll_dd____cc_oo_uu_nn_tt(3) error
    value as -1 (which is the value of ERR).

