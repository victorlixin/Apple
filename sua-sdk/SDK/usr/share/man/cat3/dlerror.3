dlerror(3)                                                   dlerror(3)

  ddlleerrrroorr(())

  NNAAMMEE

    dlerror() - report diagnostic information on dynamic linking

  SSYYNNOOPPSSIISS

    #include <dlfcn.h>

    char *dlerror(void)

  DDEESSCCRRIIPPTTIIOONN

    The _dd_ll_ee_rr_rr_oo_rr(3) call returns a character string (null-terminated, with no
    trailing newline) that describes the most recent error during dynamic
    linking.

    If there have been no errors since the last time _dd_ll_ee_rr_rr_oo_rr(3) was called, it
    returns NULL.

  RREETTUURRNN VVAALLUUEESS

    After success, the _dd_ll_ee_rr_rr_oo_rr(3) call returns a null-terminated character
    string. If it fails, it returns NULL.

  SSEEEE AALLSSOO

    _d_l_c_l_o_s_e(3)

    _d_l_o_p_e_n(3)

  UUSSAAGGEE NNOOTTEESS

    The ddlleerrrroorr function is thread safe.

    The ddlleerrrroorr function is not async-signal safe.

