assert(3)                                                     assert(3)

  aasssseerrtt(())

  NNAAMMEE

    assert() - expression verification macro

  SSYYNNOOPPSSIISS

    #include <assert.h>

    assert (expression)

  DDEESSCCRRIIPPTTIIOONN

    The _aa_ss_ss_ee_rr_tt(3) macro tests the given expression. If it is true, _aa_ss_ss_ee_rr_tt(3)
    does nothing. If it is false, the calling process is terminated, a
    diagnostic message is written to the stderr and the function ____ee_xx_ii_tt(2) is
    called, effectively terminating the program.

    At compile time, you may remove the _aa_ss_ss_ee_rr_tt(3) macro by #defining the
    NDEBUG macro before the inclusion of <<aasssseerrtt..hh>>, usually by giving the
    compiler the --DDNNDDEEBBUUGG option (see your compiler documentation).

  DDIIAAGGNNOOSSTTIICCSS

    The following diagnostic message is written to stderr if expression is
    false:

    "assertion \"%s\" failed: file \"%s\", line %d0, \
                  "expression", __FILE__, __LINE__);

  UUSSAAGGEE NNOOTTEESS

    The aasssseerrtt function is thread safe.

    The aasssseerrtt function is not async-signal safe.

