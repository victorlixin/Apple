isspace(3)                                                   isspace(3)

  iissssppaaccee(())

  NNAAMMEE

    isspace() - white-space character test

  SSYYNNOOPPSSIISS

    #include <ctype.h>

    int isspace (int c)

  DDEESSCCRRIIPPTTIIOONN

    The _ii_ss_ss_pp_aa_cc_ee(3) function tests whether c is a character of class ssppaaccee in
    the program's current locale.

    In the POSIX locale, the character class ssppaaccee includes all characters
    classified as white-space characters. At a minimum, the characters SPACE,
    FORMFEED, NEWLINE, CARRIAGE RETURN, TAB, and VERTICAL TAB are included.

  RREETTUURRNN VVAALLUUEESS

    The _ii_ss_ss_pp_aa_cc_ee(3) function returns zero if the character tests false and
    returns non-zero if the character tests true.

  SSEEEE AALLSSOO

    _i_s_a_l_n_u_m(3)

    _i_s_a_l_p_h_a(3)

    _i_s_b_l_a_n_k(3)

    _i_s_c_n_t_r_l(3)

    _i_s_d_i_g_i_t(3)

    _i_s_g_r_a_p_h(3)

    _i_s_l_o_w_e_r(3)

    _i_s_p_r_i_n_t(3)

    _i_s_p_u_n_c_t(3)

    _i_s_u_p_p_e_r(3)

    _i_s_x_d_i_g_i_t(3)

    _t_o_l_o_w_e_r(3)

    _t_o_u_p_p_e_r(3)

  UUSSAAGGEE NNOOTTEESS

    The iissssppaaccee function is thread safe.

    The iissssppaaccee function is not async-signal safe.

