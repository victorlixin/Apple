new_form(3)                                                 new_form(3)

  ffoorrmm__nneeww(())

  NNAAMMEE

    new_form(), free_form() - create and destroy forms

  SSYYNNOOPPSSIISS

    #include <form.h>

    FORM *new_form(FIELD **fields);
    char *free_form(FORM *form);

  DDEESSCCRRIIPPTTIIOONN

    The function _nn_ee_ww____ff_oo_rr_mm(3) creates a new form connected to a specified field
    pointer array (which must be NULL-terminated).

    The function _ff_rr_ee_ee____ff_oo_rr_mm(3) disconnects form from its field array and frees
    the storage allocated for the form.

  RREETTUURRNN VVAALLUUEE

    The function _nn_ee_ww____ff_oo_rr_mm(3) returns NULL on error.

    The function _ff_rr_ee_ee____ff_oo_rr_mm(3) returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_POSTED
        The form has already been posted.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _f_o_r_m_s(3)

  NNOOTTEESS

    The header file <<ffoorrmm..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V forms library. They were not supported
    on Version 7 or BSD versions.

