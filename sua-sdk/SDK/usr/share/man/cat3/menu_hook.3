menu_hook(3)                                               menu_hook(3)

  mmeennuu__hhooookk(())

  NNAAMMEE

    set_item_init(), item_init(), set_item_term(), item_term(),
    set_menu_init(), menu_init(), set_menu_term(), menu_term() - set hooks for
    automatic invocation by applications

  SSYYNNOOPPSSIISS

    #include <menu.h>

    int set_item_init(MENU *menu, void (*func)(MENU *));
    void (*)(MENU *) item_init(MWNU *menu);
    int set_item_term(MENU *menu, void (*func)(MENU *));
    void (*)(MENU *) item_term(MWNU *menu);
    int set_menu_init(MENU *menu, void (*func)(MENU *));
    void (*)(MENU *) menu_init(MWNU *menu);
    int set_menu_term(MENU *menu, void (*func)(MENU *));
    void (*)(MENU *) menu_term(MWNU *menu);

  DDEESSCCRRIIPPTTIIOONN

    These functions make it possible to set hook functions to be called at
    various points in the automatic processing of input event codes by
    _mm_ee_nn_uu____dd_rr_ii_vv_ee_rr(3).

    The function _ss_ee_tt____ii_tt_ee_mm____ii_nn_ii_tt(3) sets a hook to be called at menu-post time
    and each time the selected item changes (after the change). _ii_tt_ee_mm____ii_nn_ii_tt(3)
    returns the current item init hook, if any (NULL if there is no such
    hook).

    The function _ss_ee_tt____ii_tt_ee_mm____tt_ee_rr_mm(3) sets a hook to be called at menu-unpost time
    and each time the selected item changes (before the change). _ii_tt_ee_mm____tt_ee_rr_mm(3)
    returns the current item term hook, if any (NULL if there is no such
    hook).

    The function _ss_ee_tt____mm_ee_nn_uu____ii_nn_ii_tt(3) sets a hook to be called at menu-post time
    and just after the top row on the menu changes once it is posted.
    _mm_ee_nn_uu____ii_nn_ii_tt(3) returns the current menu init hook, if any (NULL if there is
    no such hook).

    The function _ss_ee_tt____mm_ee_nn_uu____tt_ee_rr_mm(3) sets a hook to be called at menu-unpost time
    and just before the top row on the menu changes once it is posted.
    _mm_ee_nn_uu____tt_ee_rr_mm(3) returns the current menu term hook, if any (NULL if there is
    no such hook).

  RREETTUURRNN VVAALLUUEE

    Routines that return pointers return NULL on error. Other routines return
    one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3).

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

