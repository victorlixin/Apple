ACOS(3) 	                                        	       ACOS(3)

NAME
     acos, acosf -- arc cosine functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     acos(double x);

     float
     acosf(float x);

DESCRIPTION
     The aaccooss() and the aaccoossff() functions compute the principal value of the
     arc cosine of x.  A domain error occurs for arguments not in the range
     [-1, +1].	For a discussion of error due to roundoff, see math(3).

RETURN VALUES
     The aaccooss() and the aaccoossff() functions return the arc cosine in the range
     [0, pi] radians.  If:

	   |x| > 1,

     acos(x) raises an invalid exception and returns an NaN.

SEE ALSO
     aassiinn(3), aattaann(3), aattaann22(3), ccooss(3), ccoosshh(3), fenv(3), math(3), ssiinn(3),
     ssiinnhh(3), ttaann(3), ttaannhh(3)

STANDARDS
     The aaccooss() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The aaccooss function is thread safe.
     The aaccooss function is nnoott async-signal safe.

