menu_new(3)                                                 menu_new(3)

  mmeennuu__nneeww(())

  NNAAMMEE

    new_menu(), free_menu() - create and destroy menus

  SSYYNNOOPPSSIISS

    #include <menu.h>

    MENU *new_menu(ITEM **items);
    char *free_menu(MENU *menu);

  DDEESSCCRRIIPPTTIIOONN

    The function _nn_ee_ww____mm_ee_nn_uu(3) creates a new menu connected to a specified item
    pointer array (which must be NULL-terminated).

    The function _ff_rr_ee_ee____mm_ee_nn_uu(3) disconnects menu from its item array and frees
    the storage allocated for the menu.

  RREETTUURRNN VVAALLUUEE

    The function _nn_ee_ww____mm_ee_nn_uu(3) returns NULL on error.

    The function _ff_rr_ee_ee____mm_ee_nn_uu(3) returns one of the following:

    E_OK
        The routine succeeded.

    E_SYSTEM_ERROR
        System error occurred (see errno

    E_BAD_ARGUMENT
        Routine detected an incorrect or out-of-range argument.

    E_POSTED
        The menu has already been posted.

  NNOOTTEESS

    The header file <<mmeennuu..hh>> automatically includes the header file
    <<ccuurrsseess..hh>>.

  PPOORRTTAABBIILLIITTYY

    These routines emulate the System V menu library. They were not supported
    on Version 7 or BSD versions.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _m_e_n_u_s(3)

