usleep(3)                                                     usleep(3)

  uusslleeeepp(())

  NNAAMMEE

    usleep() - suspend process execution for interval of microseconds

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    int usleep(useconds_t microseconds)

  DDEESSCCRRIIPPTTIIOONN

    The _uu_ss_ll_ee_ee_pp(3) function suspends the calling thread for at least the number
    of microseconds specified by the microseconds argument. The value of
    microseconds must be less than one million. The function uses the
    SSYYSS__sslleeeepp system call to suspend the thread. There is only one timer per
    thread as a result.

    Because of other activity or time spent processing the call, the thread
    can be suspended for longer than the period specified by microseconds.

  RREETTUURRNN VVAALLUUEE

    The _uu_ss_ll_ee_ee_pp(3) function exits with status 0 for success, and -1 if an error
    occurred.

  EERRRROORRSS

    The _uu_ss_ll_ee_ee_pp(3) function may fail for these reasons:

    [EINVAL]
        The value of microseconds was greater than or equal to one million.

  SSEEEE AALLSSOO

    _a_l_a_r_m(2)

    _g_e_t_i_t_i_m_e_r(2)

    _s_i_g_a_c_t_i_o_n(2)

    _s_l_e_e_p(2)

  UUSSAAGGEE NNOOTTEESS

    The uusslleeeepp function is thread safe.

    The uusslleeeepp function is not async-signal safe.

