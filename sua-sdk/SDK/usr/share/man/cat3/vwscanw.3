vwscanw(3)                                                   vwscanw(3)

  ccuurrss__ssccaannww(())

  NNAAMMEE

    scanw(), wscanw(), mvscanw(), mvwscanw(), vwscanw() - convert formatted
    input from a curses window

  SSYYNNOOPPSSIISS

    #include <curses.h>

    int scanw(char *fmt [, arg] ...);
    int wscanw(WINDOW *win, char *fmt [, arg] ...);
    int mvscanw(int y, int x, char *fmt [, arg] ...);
    int mvwscanw(WINDOW *win, int y, int x, char *fmt [, arg] ...);

    #include <stdarg.h>

    int vwscanw(WINDOW *win, char *fmt, va_list varglist);
    int vw_scanw(WINDOW *win, char *fmt, va_list varglist);

  DDEESSCCRRIIPPTTIIOONN

    The _ss_cc_aa_nn_ww(3), _ww_ss_cc_aa_nn_ww(3) and _mm_vv_ss_cc_aa_nn_ww(3) routines are analogous to _ss_cc_aa_nn_ff(3)
    (see _ss_cc_aa_nn_ff(3)). The effect of these routines is as though _ww_gg_ee_tt_ss_tt_rr(3) were
    called on the window, and the resulting line used as input for _ss_ss_cc_aa_nn_ff(3).
    Fields which do not map to a variable in the fmt field are lost.

    The _vv_ww_ss_cc_aa_nn_ww(3) routine is similar to _vv_ww_pp_rr_ii_nn_tt_ww(3) in that it performs a
    _ww_ss_cc_aa_nn_ww(3) using a variable argument list. The third argument is a vvaa__lliisstt,
    a pointer to a list of arguments, as defined in <<ssttddaarrgg..hh>>. The
    _vv_ww____ss_cc_aa_nn_ww(3) routine is a ##ddeeffiinnee for _vv_ww_ss_cc_aa_nn_ww(3). _vv_ww____ss_cc_aa_nn_ww(3) is planned
    for the XSI Curses standard. Traditionally, _vv_ww_ss_cc_aa_nn_ww(3) made use of
    <<vvaarraarrggss..hh>> and _vv_ww____ss_cc_aa_nn_ww(3) made use of <<ssttddaarrgg..hh>>, but both of the
    routines in this package use <<ssttddaarrgg..hh>>.

  RREETTUURRNN VVAALLUUEE

    _vv_ww_ss_cc_aa_nn_ww(3) returns ERR on failure and an integer equal to the number of
    fields scanned on success.

    Applications may use the return value from the _ss_cc_aa_nn_ww(3), _ww_ss_cc_aa_nn_ww(3),
    _mm_vv_ss_cc_aa_nn_ww(3) and _mm_vv_ww_ss_cc_aa_nn_ww(3) routines to determine the number of fields
    which were mapped in the call.

  PPOORRTTAABBIILLIITTYY

    The XSI Curses standard, Issue 4 describes these functions. The function
    _vv_ww_ss_cc_aa_nn_ww(3) is marked TO BE WITHDRAWN, and is to be replaced by a function
    _vv_ww____ss_cc_aa_nn_ww(3) using the <<ssttddaarrgg..hh>> interface.

  SSEEEE AALLSSOO

    _c_u_r_s_e_s(3)

    _c_u_r_s___g_e_t_s_t_r(3)

    _c_u_r_s___p_r_i_n_t_w(3)

    _s_c_a_n_f(3)

