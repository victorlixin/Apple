localeconv(3)                                             localeconv(3)

  llooccaalleeccoonnvv(())

  NNAAMMEE

    localeconv() - Get locale-specific rules for formatting numeric quantities

  SSYYNNOOPPSSIISS

    #include <locale.h>

    struct lconv *localeconv (void)

  DDEESSCCRRIIPPTTIIOONN

    The _ll_oo_cc_aa_ll_ee_cc_oo_nn_vv(3) function returns a pointer to a ssttrruucctt llccoonnvv which
    describes the current locale. This is used to make a program specific to a
    particular language, cultural convention and codeset.

    The Subsystem for UNIX-based Applications supports the C and POSIX locales (which are
    identical), ja_JP.EUC-JP and ja_JP.eucJP locales (which are identical),
    and win-ja_JP.windows-932 and ja_JP.SJIS locales (which are identical).

    The llccoonnvv structure is described in the file <<llooccaallee..hh>>. It contains:
    MMeemmbbeerr                       DDeeffaauulltt        DDeessccrriippttiioonn

    cchhaarr ** decimal_point                        Character used for non-
                                                monetary quantities

    cchhaarr ** thousands_sep                        Character used to separate
                                                groups of digits for non-
                                                monetary quantities

    char * grouping                             String indicating size of
                                                each group of digits for
                                                non-monetary quantities
                                                *     0 -- repeat
                                                      previous element
                                                *     1..{CHAR_MAX}-
                                                      1 -- number of
                                                      digits in current
                                                      group
                                                *     {CHAR_MAX} -- no
                                                      further grouping

    cchhaarr ** int_curr_symbol                      International currency
                                                symbol

    cchhaarr ** currency_symbol                      Local currency symbol

    cchhaarr ** mon_decimal_point                    Character used for monetary
                                                quantities

    cchhaarr ** mon_thousands_sep                    Character used to separate
                                                groups of digits for
                                                monetary quantities

    cchhaarr ** mon_grouping                         Character used to separate
                                                groups of digits for
                                                monetary quantities

    cchhaarr ** positive_sign                        String to indicate monetary
                                                quantity with non-negative
                                                value

    cchhaarr ** negative_sign                        String used to indicate
                                                monetary quantity with
                                                negative value

    cchhaarr int_frac_digits         {CHAR_MAX}     Number of digits after
                                                decimal point for
                                                internationally-formatted
                                                monetary quantities

    cchhaarr frac_digits             {CHAR_MAX}     Number of digits after the
                                                decimal point for monetary
                                                quantities

    cchhaarr p_cs_precedes           {CHAR_MAX}     1 if currency symbol
                                                precedes positive monetary
                                                values, 0 if it follows

    cchhaarr p_sep_by_space          {CHAR_MAX}     1 if a space goes between
                                                currency symbol and digits
                                                for positive monetary
                                                values, 0 if no space

    cchhaarr n_cs_precedes           {CHAR_MAX}     1 if curency symbol
                                                precedes negative monetary
                                                values, 0 if it follows

    cchhaarr n_sep_by_space          {CHAR_MAX}     1 if a space goes between
                                                currency symbol and digits
                                                for negative monetary
                                                values, 0 if no space

    cchhaarr p_sign_posn             {CHAR_MAX}     Position of positive sign
                                                in monetary values:
                                                *     0 -- surround
                                                      with ( )
                                                *     1 -- sign string
                                                      precedes quantity and
                                                      currency symbol
                                                *     2 -- sign string
                                                      succeeds quantity and
                                                      currency symbol
                                                *     3 -- sign string
                                                      precedes currency
                                                      symbol
                                                *     4 -- sign string
                                                      immediately after
                                                      currency symbol

    cchhaarr n_sign_posn             {CHAR_MAX}     Position of negative sign
                                                in monetary values:
                                                *     0 -- surround
                                                      with ( )
                                                *     1 -- sign string
                                                      precedes quantity and
                                                      currency symbol
                                                *     2 -- sign string
                                                      succeeds quantity and
                                                      currency symbol
                                                *     3 -- sign string
                                                      precedes currency
                                                      symbol
                                                *     4 -- sign string
                                                      immediately after
                                                      currency symbol

  RREETTUURRNN VVAALLUUEE

    The _ll_oo_cc_aa_ll_ee_cc_oo_nn_vv(3) function returns a pointer to a ssttrruucctt llccoonnvv which
    describes the current locale.

  SSEEEE AALLSSOO

    _s_e_t_l_o_c_a_l_e(3)

  UUSSAAGGEE NNOOTTEESS

    The llooccaalleeccoonnvv function is not thread safe.

    The llooccaalleeccoonnvv function is not async-signal safe.

