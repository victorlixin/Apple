strcpy(3)                                                     strcpy(3)

  ssttrrccppyy(())

  NNAAMMEE

    strcpy(), strncpy() - copy strings

  SSYYNNOOPPSSIISS

    #include <string.h>

    char * strcpy (char *dst, const char *src)
    char * strncpy (char *dst, const char *src, size_t len)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_tt_rr_cc_pp_yy(3) and _ss_tt_rr_nn_cc_pp_yy(3) functions copy the string src to dst
    (including the terminating \0 character).

    The _ss_tt_rr_nn_cc_pp_yy(3) copies not more than len characters into dst, appending \0
    characters if src is less than len characters long, and not terminating
    dst if src is more than len characters long.

  RREETTUURRNN VVAALLUUEESS

    The _ss_tt_rr_cc_pp_yy(3) and _ss_tt_rr_nn_cc_pp_yy(3) functions return dst.

  EEXXAAMMPPLLEESS

    The following sets chararray to abc\0\0\0:

    (void)strncpy(chararray, "abc", 6).

    The following sets chararray to abcdef:

    (void)strncpy(chararray, "abcdefgh", 6);

  SSEEEE AALLSSOO

    _b_c_o_p_y(3)

    _m_e_m_c_p_y(3)

    _m_e_m_m_o_v_e(3)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

