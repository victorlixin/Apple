SIN(3)		                                        		SIN(3)

NAME
     sin, sinf -- sine functions

LIBRARY
     Math Library (libm, -lm)

SYNOPSIS
     #include <math.h>

     double
     sin(double x);

     float
     sinf(float x);

DESCRIPTION
     The ssiinn() and the ssiinnff() functions compute the sine of x (measured in
     radians).	A large magnitude argument may yield a result with little or
     no significance.

RETURN VALUES
     The ssiinn() and the ssiinnff() functions return the sine value.

SEE ALSO
     aaccooss(3), aassiinn(3), aattaann(3), aattaann22(3), ccooss(3), ccoosshh(3), math(3), ssiinnhh(3),
     ttaann(3), ttaannhh(3)

STANDARDS
     The ssiinn() function conforms to ISO/IEC 9899:1990 (``ISO C90'').


USAGE NOTES

     The ssiinnff function is thread safe.
     The ssiinnff function is nnoott async-signal safe.

