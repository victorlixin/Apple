alloca(3)                                                     alloca(3)

  aallllooccaa(())

  NNAAMMEE

    alloca() - memory allocator

  SSYYNNOOPPSSIISS

    #include <alloca.h>

    void * alloca (size_t *size)

  DDEESSCCRRIIPPTTIIOONN

    The aallllooccaa(3) function allocates size bytes of space in the stack frame of
    the caller. This temporary space is automatically freed on return.

    If the allocation request is too large or the stack is too deep, the
    application may terminate with a signal 7, indicating that the available
    stack spaces has been consumed.

  RREETTUURRNN VVAALLUUEESS

    If the aallllooccaa(3) call is successful it returns a pointer to the beginning
    of the allocated space. If the allocation failed, a NULL pointer is
    returned.

  EERRRROORRSS

    The aallllooccaa(3) function will fail if:

    [ENOMEM]
        Insufficient storage space is available.

  SSEEEE AALLSSOO

    _c_a_l_l_o_c(3)

    _m_a_l_l_o_c(3)

    _r_e_a_l_l_o_c(3)

  UUSSAAGGEE NNOOTTEESS

    The aallllooccaa function is not thread safe.

    The aallllooccaa function is not async-signal safe.

