sem_init(2)                                                 sem_init(2)

  sseemm__iinniitt(())

  NNAAMMEE

    sem_init() - initialize an unnamed semaphore

  SSYYNNOOPPSSIISS

    #include <semaphore.h>

    int sem_init(sem_t *sem, int pshared, unsigned int value);

  DDEESSCCRRIIPPTTIIOONN

    The sseemm__iinniitt(2) function initializes the unnamed semaphore referenced by
    the sem argument with its value set to the value specified by the value
    argument. If the pshared argument is nonzero, then the semaphore can be
    shared among processes; otherwise, the semaphore can be shared among
    threads of the calling process. The semaphore remains usable until it is
    destroyed by a call to _ss_ee_mm____dd_ee_ss_tt_rr_oo_yy(2).

  RREETTUURRNN VVAALLUUEESS

    On success, the function returns 0; otherwise, -1 is returned and errno is
    set to an error code to indicate the error.

  EERRRROORRSS

    The sseemm__iinniitt() function can fail for the following reasons:

    [EINVAL]
        The value argument is out of bounds.

    [ENOSPC]
        A resource required to initialize the semaphore has been exhausted, or
        the limit on semaphores has been reached

    [EPERM]
        The caller lacks the permissions required to perform the operation.

  SSEEEE AALLSSOO

    _s_e_m___d_e_s_t_r_o_y(2)

    _s_e_m___p_o_s_t(2)

    _s_e_m___t_i_m_e_d_w_a_i_t(2)

    _s_e_m___t_r_y_w_a_i_t(2)

    _s_e_m___w_a_i_t(2)

  UUSSAAGGEE NNOOTTEESS

    The sseemm__iinniitt function is thread safe.

    The sseemm__iinniitt function is not async-signal safe.

