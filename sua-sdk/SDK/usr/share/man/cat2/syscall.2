syscall(2)                                                   syscall(2)

  ssyyssccaallll(())

  NNAAMMEE

    syscall() - indirect system call

  SSYYNNOOPPSSIISS

    #include <unistd.h>
    #include <sys/syscall.h>

    int syscall(int number, ...)

  DDEESSCCRRIIPPTTIIOONN

    The ssyyssccaallll(2) function performs the system call whose assembly language
    interface has the specified number with the specified arguments. Symbolic
    constants for system calls can be found in the header file <<ssyyss//
    ssyyssccaallll..hh>>.

  RREETTUURRNN VVAALLUUEESS

    The return values are defined by the system call being invoked. In
    general, a 0 return value indicates success. A -1 return value indicates
    an error, and an error code is stored in errno.

  NNOOTTEE

    Currently, the following system calls are supported:

    *     SYS_accept
    *     SYS_fcntl
    *     SYS_read
    *     SYS_select
    *     SYS_sigaction
    *     SYS_sigprocmask
    *     SYS_sigsuspend
    *     SYS_write

    If ssyyssccaallll(2) is called with any other system-call number (including other
    system call-number constants set in <sys/syscall.h>), it returns -1 and
    sets errno to ENOSYS.

  HHIISSTTOORRYY

    The ssyyssccaallll() function call appeared in 4.0BSD.

  UUSSAAGGEE NNOOTTEESS

    The ssyyssccaallll function is thread safe.

    The ssyyssccaallll function is not async-signal safe.

