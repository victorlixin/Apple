listen(2)                                                     listen(2)

  lliisstteenn(())

  NNAAMMEE

    listen() - listen for connections on a socket

  SSYYNNOOPPSSIISS

    #include <sys/socket.h>

    int listen (int s, int backlog)

  DDEESSCCRRIIPPTTIIOONN

    To accept connections, a socket is first created with _ss_oo_cc_kk_ee_tt(2), a
    willingness to accept incoming connections and a queue limit for incoming
    connections are specified with _ll_ii_ss_tt_ee_nn(2), and then the connections are
    accepted with _aa_cc_cc_ee_pp_tt(2). The _ll_ii_ss_tt_ee_nn(2) call applies only to sockets of
    type SOCK_STREAM or SOCK_SEQPACKET.

    The backlog parameter defines the maximum length the queue of pending
    connections may grow to. If a connection request arrives with the queue
    full the client may receive an error with an indication of [ECONNREFUSED,]
    or, if the underlying protocol supports retransmission, the request may be
    ignored so that retries may succeed.

  RREETTUURRNN VVAALLUUEESS

    A 0 return value indicates success; -1 indicates an error.

  EERRRROORRSS

    The _ll_ii_ss_tt_ee_nn(2) call will fail if:

    [EBADF]
        The argument s is not a valid descriptor.

    [ENOTSOCK]
        The argument s is not a socket.

    [EOPNOTSUPP]
        The socket is not of a type that supports the operation _ll_ii_ss_tt_ee_nn(2).

  SSEEEE AALLSSOO

    _a_c_c_e_p_t(2)

    _c_o_n_n_e_c_t(2)

    _s_o_c_k_e_t(2)

  UUSSAAGGEE NNOOTTEESS

    The lliisstteenn function is thread safe.

    The lliisstteenn function is async-signal safe.

