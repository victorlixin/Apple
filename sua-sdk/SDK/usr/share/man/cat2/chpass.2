chpass(2)                                                     chpass(2)

  cchhppaassss(())

  NNAAMMEE

    chpass() - change information in the user database

  SSYYNNOOPPSSIISS

    #include <interix/interix.h>

    int chpass(char *fq_user, char *oldpw, char *newpw)

  DDEESSCCRRIIPPTTIIOONN

    This call, unique to Subsystem for UNIX-based Applications, changes a user's password in the user
    database. All three arguments are null-terminated strings. The fq_user
    argument is the name of the user whose password will be changed. It must
    be provided as a fully-qualified name (as in DOMAIN+fq_user).

  RREETTUURRNN VVAALLUUEESS

    On success, the _cc_hh_pp_aa_ss_ss(2) returns 0. On failure, it returns -1 and sets
    errno.

  EERRRROORRSS

    The _cc_hh_pp_aa_ss_ss(2) call can fail for the following reasons.

    [EINVAL]
        Invalid (NULL) arguments passed to cchhppaassss(2).

    [ENOMEM]
        No memory could be allocated.

    [EPERM]
        The password is not allowed to be changed for this account, or oldpw
        is incorrect, or the newpw does not meet the local security
        restrictions.

    [ESRCH]
        No process could be found corresponding to that specified by the given
        process ID. One of the arguments was not provided.

  SSEEEE AALLSSOO

    _p_a_s_s_w_d(1)

  UUSSAAGGEE NNOOTTEESS

    The cchhppaassss function is thread safe.

    The cchhppaassss function is not async-signal safe.

