endpwent(2)                                                 endpwent(2)

  eennddppwweenntt(())

  NNAAMMEE

    endpwent(), getpwent(), setpwent() - access user database

  SSYYNNOOPPSSIISS

    #include <pwd.h>

    void endpwent(void);
    struct passwd *getpwent(void);
    void setpwent(void);

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_pp_ww_ee_nn_tt(2) function returns a pointer to a ssttrruucctt ppaasssswwdd containing
    information from the user database for a user. Each entry in the user
    database contains a ppaasssswwdd structure. The first call to _gg_ee_tt_pp_ww_ee_nn_tt(2)
    returns a structure containing information from the first entry in the
    database. Each subsequent call proceeds through the database, returning a
    pointer to a structure containing information from the next line. In this
    way, _gg_ee_tt_pp_ww_ee_nn_tt(2) can be used to search the entire user database. When
    _gg_ee_tt_pp_ww_ee_nn_tt(2) reaches the end of the database or on error, it returns a null
    pointer.

    The _ss_ee_tt_pp_ww_ee_nn_tt(2) function rewinds the user database to allow repeated
    searches.

    The _ee_nn_dd_pp_ww_ee_nn_tt(2) function closes the user database when processing is
    complete.

  RREETTUURRNN VVAALLUUEE

    The ggeettppwweenntt(2) function returns a pointer to a structure on success; on
    end-of-file or on error, it returns a null pointer.

  EERRRROORRSS

    The _gg_ee_tt_pp_ww_ee_nn_tt(2), _ss_ee_tt_pp_ww_ee_nn_tt(2), and _ee_nn_dd_pp_ww_ee_nn_tt(2) functions may fail for this
    reason:

    [EIO]
        An I/O error occurred.

  UUSSAAGGEE NNOOTTEESS

    None of these functions are thread safe.

    None of these functions are async-signal safe.

