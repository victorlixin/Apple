endgrent(2)                                                 endgrent(2)

  eennddggrreenntt(())

  NNAAMMEE

    setgrent(), endgrent() - group database operations

  SSYYNNOOPPSSIISS

    #include <grp.h>

    void endgrent (void)
    void setgrent (void)

  DDEESSCCRRIIPPTTIIOONN

    These functions (along with _gg_ee_tt_gg_rr_nn_aa_mm(2), _gg_ee_tt_gg_rr_gg_ii_dd(2), and _gg_ee_tt_gg_rr_ee_nn_tt(2))
    operate on the group database (traditionally //eettcc//ggrroouupp, but part of the
    user database in Interix). Each line of the database is defined by the
    structure group found in the include file <<ggrrpp..hh>>:

    struct group {
         char *gr_name;           /* group name */
         gid_t     gr_gid;        /* group id */
         char **gr_mem;           /* group members */
         char *gr_passwd;         /* group password */
    };

    The _ss_ee_tt_gg_rr_ee_nn_tt(2) function opens the file, or rewinds it if it is already
    open.

    The _ee_nn_dd_gg_rr_ee_nn_tt(2) function closes any open files.

  RREETTUURRNN VVAALLUUEESS

    None.

  SSEEEE AALLSSOO

    _g_e_t_g_r_e_n_t(2)

    _g_e_t_g_r_n_a_m(2)

    _g_e_t_g_r_g_i_d(2)

    _g_e_t_p_w_e_n_t(2)

  UUSSAAGGEE NNOOTTEESS

    None of these functions are thread safe.

    None of these functions are async-signal safe.

