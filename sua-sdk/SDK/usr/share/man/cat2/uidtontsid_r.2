uidtontsid_r(2)                                         uidtontsid_r(2)

  uuiiddttoonnttssiidd(())

  NNAAMMEE

    uidtontsid(), uidtontsid_r() - retrieve user's Windows security ID

  SSYYNNOOPPSSIISS

    #include <interix/security.h>

    char * uidtontsid(uid_t uid)
    int  uidtontsid_r(uid_t uid, char *buf, int bufsize)

  DDEESSCCRRIIPPTTIIOONN

    The uuiiddttoonnttssiidd(2) function retrieves the Windows security ID of the user
    specified by uid, and returns a pointer to a static buffer that contains
    the security ID as an ASCII string.

    The reentrant uuiiddttoonnttssiidd__rr(2) function retrieves the Windows security ID
    of the user specified by uid and stores the ID in the location identified
    by buf, the length of which is specified by bufsize. The buffer must be at
    least SID_NAME_MAX (defined in <limits.h>) in size.

    Windows security IDs are unique throughout time and space; a POSIX user ID
    under Windows is only unique within a trusted domain network.

  RREETTUURRNN VVAALLUUEESS

    On success, uuiiddttoonnttssiidd(2) returns a pointer to a static buffer. On
    failure, it returns NULL and sets errno to indicate the cause of the
    error.

    Upon successful completion, uuiiddttoonnttssiidd__rr(2) returns 0; otherwise, it
    returns an error number to indicate the error.

  EERRRROORRSS

    Both functions can set the following error:

    [EINVAL]
        The uid is invalid.

    The uuiiddttoonnttssiidd__rr(2) function can fail for the following reason:

    ERANGE
        The size of the buffer (bufsize) provided is smaller than the result
        to be returned.

  SSEEEE AALLSSOO

  UUSSAAGGEE NNOOTTEESS

    The uuiiddttoonnttssiidd__rr function is thread safe. The uuiiddttoonnttssiidd function is not
    thread safe.

    None of these functions are async-signal safe.

