sigpending(2)                                             sigpending(2)

  ssiiggppeennddiinngg(())

  NNAAMMEE

    sigpending() - get pending signals

  SSYYNNOOPPSSIISS

    #include <signal.h>

    int sigpending(sigset_t *set)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_ii_gg_pp_ee_nn_dd_ii_nn_gg(2) function returns a mask of the signals pending for
    delivery to the calling process in the location indicated by set. Signals
    may be pending because they are currently masked, or transiently before
    delivery (although the latter case is not normally detectable).

  RREETTUURRNN VVAALLUUEESS

    A 0 value indicated that the call succeeded. A -1 return value indicates
    an error occurred and errno is set to indicated the reason.

  EERRRROORRSS

    The _ss_ii_gg_pp_ee_nn_dd_ii_nn_gg(2) function detects the following error:

    [EFAULT]
        The value of set is an invalid pointer.

  SSEEEE AALLSSOO

    _s_i_g_a_c_t_i_o_n(2)

    _s_i_g_p_r_o_c_m_a_s_k(2)

  UUSSAAGGEE NNOOTTEESS

    The ssiiggppeennddiinngg function is thread safe.

    The ssiiggppeennddiinngg function is async-signal safe.

