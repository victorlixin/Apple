tcgetpgrp(2)                                               tcgetpgrp(2)

  ttccggeettppggrrpp(())

  NNAAMMEE

    tcgetpgrp() - get foreground process group ID

  SSYYNNOOPPSSIISS

    #include <sys/types.h>
    #include <unistd.h>

    pid_t tcgetpgrp (int fd)

  DDEESSCCRRIIPPTTIIOONN

    This call returns the ID number of the terminal's foreground process
    group.

    A member of a background process group can call this routine, but the
    value of the process group ID may be later changed by a foreground
    process.

    If there is no foreground process group, _tt_cc_gg_ee_tt_pp_gg_rr_pp(2) returns an invalid
    number.

  RREETTUURRNN VVAALLUUEESS

    On success, _tt_cc_gg_ee_tt_pp_gg_rr_pp(2) returns a process group ID as a ppiidd__tt. On
    failure, it returns -1 and sets errno

  EERRRROORRSS

    _tt_cc_gg_ee_tt_pp_gg_rr_pp(2) can fail for the following reasons:

    [EBADF]
        The fd argument is not a valid file descriptor.

    [ENOTTY]
        The calling process does not have a controlling terminal or the
        underlying terminal device represented by fd is not the controlling
        terminal.

  SSEEEE AALLSSOO

    _s_e_t_p_g_i_d(2)

    _s_e_t_s_i_d(2)

    _t_c_s_e_t_p_g_r_p(2)

  UUSSAAGGEE NNOOTTEESS

    The ttccggeettppggrrpp function is thread safe.

    The ttccggeettppggrrpp function is async-signal safe.

