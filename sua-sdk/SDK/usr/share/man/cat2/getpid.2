getpid(2)                                                     getpid(2)

  ggeettppiidd(())

  NNAAMMEE

    getpid(), getppid() - get parent or calling process identification

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    pid_t getpid (void)
    pid_t getppid (void)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_pp_ii_dd(2) function returns the process ID of the calling process. The
    ID is guaranteed to be unique and is useful for constructing temporary
    file names.

    The _gg_ee_tt_pp_pp_ii_dd(2) function returns the process ID of the parent of the
    calling process.

  EERRRROORRSS

    The _gg_ee_tt_pp_ii_dd(2) and _gg_ee_tt_pp_pp_ii_dd(2) functions are always successful, and no
    return value is reserved to indicate an error.

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    All of these functions are async-signal safe.

