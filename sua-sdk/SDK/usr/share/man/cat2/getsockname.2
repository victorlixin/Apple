getsockname(2)                                           getsockname(2)

  ggeettssoocckknnaammee(())

  NNAAMMEE

    getsockname() - get socket name

  SSYYNNOOPPSSIISS

    int getsockname (int s, struct sockaddr *name, int *namelen)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_ss_oo_cc_kk_nn_aa_mm_ee(2) function returns the current name for the specified
    socket. The namelen parameter should be initialized to indicate the amount
    of space pointed to by name. On return it contains the actual size of the
    name returned (in bytes).

  RREETTUURRNN VVAALLUUEESS

    If the call succeeds, it returns 0. If it fails, it returns -1 and sets
    errno to indicate the error.

  EERRRROORRSS

    The call succeeds unless:

    [EBADF]
        The argument s is not a valid descriptor.

    [EFAULT]
        The name parameter points to memory not in a valid part of the process
        address space.

    [ENOTCONN]
        The socket s has not been bound. The call to _bb_ii_nn_dd(2) must succeed
        before _gg_ee_tt_ss_oo_cc_kk_nn_aa_mm_ee(2) can be called successfully.

    [ENOTSOCK]
        The argument s is a file, not a socket.

    [ENOBUFS]
        Insufficient resources were available in the system to perform the
        operation.

    [EOPNOTSUP]
        The operation was not supported on this socket type.

  SSEEEE AALLSSOO

    _b_i_n_d(2)

    _s_o_c_k_e_t(2)

  UUSSAAGGEE NNOOTTEESS

    The ggeettssoocckknnaammee function is thread safe.

    The ggeettssoocckknnaammee function is async-signal safe.

