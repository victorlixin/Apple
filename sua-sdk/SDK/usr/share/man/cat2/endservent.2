endservent(2)                                             endservent(2)

  ggeettsseerrvvbbyyppoorrtt(())

  NNAAMMEE

    getservbyport(), getservbyname(), getservent(), setservent(), endservent()
    - get service entry

  SSYYNNOOPPSSIISS

    #include <netdb.h>

    struct servent *getservent(void)
    struct servent *getservbyname (char *name, const char *proto)
    struct servent *getservbyport (int port, const char *proto)
    void setservent (int stayopen)
    void endservent (void)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_ss_ee_rr_vv_ee_nn_tt(2), _gg_ee_tt_ss_ee_rr_vv_bb_yy_nn_aa_mm_ee(2) and _gg_ee_tt_ss_ee_rr_vv_bb_yy_pp_oo_rr_tt(2) functions each
    return a pointer to an object with the following structure containing the
    information from a network services database.

    struct servent {
        char * s_name;      /* official name of service */
        char ** s_aliases;  /* alias list */
        int s_port;         /* port service resides at */
        char * s_proto;     /* protocol to use
    };

    The members of this structure are:

    s_name
        The official name of the service.

    s_aliases
        A zero terminated list of alternate names for the service.

    s_port
        The port number at which the service resides. Port numbers are
        returned in network byte order.

    s_proto
        The name of the protocol to use when contacting the service.

    The _gg_ee_tt_ss_ee_rr_vv_ee_nn_tt(2) function reads the next line of the file, opening the
    file if necessary.

    The _ss_ee_tt_ss_ee_rr_vv_ee_nn_tt(2) and _ee_nn_dd_ss_ee_rr_vv_ee_nn_tt(2) functions are no-ops, provided for
    portability.

    The _gg_ee_tt_ss_ee_rr_vv_bb_yy_nn_aa_mm_ee(2) and _gg_ee_tt_ss_ee_rr_vv_bb_yy_pp_oo_rr_tt(2) functions return information
    from a matching protocol name or port number. If a protocol name is also
    supplied (proto is non-NULL), searches must also match the protocol.

  NNOOTTEESS

    These functions use static data storage; if the data is needed for future
    use, it should be copied before any subsequent calls overwrite it.

  RREETTUURRNN VVAALLUUEESS

    Returns NULL pointer on end of file (EOF) or error.

  FFIILLEESS

    These functions make use of the following files:

    %%WWIINNDDIIRR%%//ssyysstteemm3322//ddrriivveerrss//eettcc//sseerrvviicceess
        The network services database, where %%WWIINNDDIIRR%% is the Windows
        installation directory.

  SSEEEE AALLSSOO

    _g_e_t_p_r_o_t_o_e_n_t(2)

  UUSSAAGGEE NNOOTTEESS

    None of these functions are thread safe.

    None of these functions are async-signal safe.

