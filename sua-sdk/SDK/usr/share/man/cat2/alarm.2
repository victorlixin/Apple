alarm(2)                                                       alarm(2)

  aallaarrmm(())

  NNAAMMEE

    alarm() - set signal timer alarm

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    unsigned int alarm (unsigned int seconds)

  DDEESSCCRRIIPPTTIIOONN

    The _aa_ll_aa_rr_mm(2) function sets a countdown timer that waits seconds before
    asserting the terminating signal SIGALRM; _aa_ll_aa_rr_mm(2) returns immediately
    after setting the timer.

    If there is a pending _aa_ll_aa_rr_mm(2) request, the call to _aa_ll_aa_rr_mm(2) replaces the
    prior call. In this case, the _aa_ll_aa_rr_mm(2) function returns the number of
    seconds left on the prior call's timer.

    To cancel an alarm without setting a new one, pass 0 as the seconds
    argument.

    The maximum number of seconds allowed is the maximum value of an uunnssiiggnneedd
    iinntt.

  RREETTUURRNN VVAALLUUEE

    If there is no other _aa_ll_aa_rr_mm(2) request pending, _aa_ll_aa_rr_mm(2) returns 0 after
    setting the timer.

    If there is another _aa_ll_aa_rr_mm(2) request pending, _aa_ll_aa_rr_mm(2) returns the number
    of seconds remaining until the previous request would have generated a
    SIGALRM signal.

  SSEEEE AALLSSOO

    _s_i_g_a_c_t_i_o_n(2)

    _s_i_g_n_a_l(2)

    _s_l_e_e_p(2)

  UUSSAAGGEE NNOOTTEESS

    The aallaarrmm function is thread safe.

    The aallaarrmm function is async-signal safe.

