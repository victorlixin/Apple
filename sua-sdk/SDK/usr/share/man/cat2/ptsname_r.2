ptsname_r(2)                                               ptsname_r(2)

  ppttssnnaammee(())

  NNAAMMEE

    ptsname(), ptsname_r() - get name of subordinate (slave) pseudo-terminal
    device

  SSYYNNOOPPSSIISS

    #include <stdlib.h>

    char * ptsname(int fildes)
    int ptsname_r (int filedes, char *buf, size_t len)

  DDEESSCCRRIIPPTTIIOONN

    The ppttssnnaammee(2) call returns the name of a subordinate (also called slave)
    pty that corresponds to the master pseudo-terminal device indicated by
    fildes. This call is normally used after opening //ddeevv//ppttmmxx.

    The format of the subordinate name is /dev/tty[p-zA-E][0-9a-f].


    The ppttssnnaammee__rr(2) function is similar to ppttssnnaammee(2) except that it places
    its result into the buffer identified by the buf argument, the length of
    which is specified by the len argument.

  RREETTUURRNN VVAALLUUEE

    On success, ppttssnnaammee(2) returns a pointer to a string which is the name of
    the subordinate pseudo terminal. On failure, it returns NULL. (It fails if
    fildes can't be associated with the master side of a valid pseudo
    terminal.)

    On success, ppttssnnaammee__rr(2) returns zero; otherwise it returns nonzero.

  SSEEEE AALLSSOO

    _g_r_a_n_t_p_t(2)

    _o_p_e_n(2)

    _t_t_y_n_a_m_e(2)

    _u_n_l_o_c_k_p_t(2)

    _p_t_y(4)

    _t_e_r_m_i_o_s(4)

    _t_t_y(4)

  UUSSAAGGEE NNOOTTEESS

    The ppttssnnaammee__rr function is thread safe. The ppttssnnaammee function is not thread
    safe.

    None of these functions are async-signal safe.

