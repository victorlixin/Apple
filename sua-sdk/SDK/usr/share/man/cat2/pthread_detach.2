pthread_detach(2)                                     pthread_detach(2)

  pptthhrreeaadd__ddeettaacchh(())

  NNAAMMEE

    pthread_detach() - detach a thread

  SSYYNNOOPPSSIISS

    #include <pthread.h>

    int pthread_detach(pthread_t thread);

  DDEESSCCRRIIPPTTIIOONN

    The pptthhrreeaadd__ddeettaacchh(2) function causes the storage for the thread
    referenced by the thread argument to be reclaimed when the thread
    terminates. Calling the pptthhrreeaadd__ddeettaacchh() function does not cause the
    thread to terminate if the thread has not done so already.

  RREETTUURRNN VVAALLUUEESS

    On success, the function returns 0; otherwise, an error code is returned.

  EERRRROORRSS

    The pptthhrreeaadd__ddeettaacchh() function can fail for the following reasons:

    [EINVAL]
        The thread argument does not identify a joinable thread.

    [ESRCH]
        The thread argument does not identify an existing thread.

    This function does not return [EINTR].

  NNOOTTEESS

    For every thread that is created, the application should call
    pptthhrreeaadd__ddeettaacchh(2) or the _pp_tt_hh_rr_ee_aa_dd____jj_oo_ii_nn(2) function to ensure that storage
    for the thread is reclaimed.

  SSEEEE AALLSSOO

    _p_t_h_r_e_a_d___c_r_e_a_t_e(2)

    _p_t_h_r_e_a_d___e_x_i_t(2)

    _p_t_h_r_e_a_d___j_o_i_n(2)

  UUSSAAGGEE NNOOTTEESS

    The pptthhrreeaadd__ddeettaacchh function is thread safe.

    The pptthhrreeaadd__ddeettaacchh function is not async-signal safe.

