gettzenv(2)                                                 gettzenv(2)

  ggeettttzzeennvv(())

  NNAAMMEE

    gettzenv() - retrieve system timezone information

  SSYYNNOOPPSSIISS

    #include <interix/interix.h>

    char * gettzenv(void)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_tt_zz_ee_nn_vv(2) call returns a string describing time zone information in
    the POSIX format. The call returns a pointer to a static buffer containing
    the system time zone, in the form TZ=value. (The exact format is described
    in the man page for _tt_zz_ss_ee_tt(3).)

    If the call cannot retrieve time zone information from the system, the
    call returns NULL.

  RREETTUURRNN VVAALLUUEE

    The _gg_ee_tt_tt_zz_ee_nn_vv(2) function returns a pointer to a static buffer. On success,
    this buffer contains the environment variable. On failure, the call
    returns NULL.

  SSEEEE AALLSSOO

    _t_z_s_e_t(3)

  UUSSAAGGEE NNOOTTEESS

    The ggeettttzzeennvv function is not thread safe.

    The ggeettttzzeennvv function is not async-signal safe.

