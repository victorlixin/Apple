pthread_join(2)                                         pthread_join(2)

  pptthhrreeaadd__jjooiinn(())

  NNAAMMEE

    pthread_join() - suspend execution until another thread terminates

  SSYYNNOOPPSSIISS

    #include <pthread.h>

    int pthread_join(pthread_t thread, void **value_ptr);

  DDEESSCCRRIIPPTTIIOONN

    The pptthhrreeaadd__jjooiinn(2) function suspends execution of the calling thread
    until the thread identified by the thread argument terminates, unless that
    thread has already terminated. If the value_ptr argument is not null, when
    pptthhrreeaadd__jjooiinn returns, the location referenced by value_ptr contains the
    value passed to _pp_tt_hh_rr_ee_aa_dd____ee_xx_ii_tt(2) when the thread identified by thread
    terminated. If the thread that called pptthhrreeaadd__jjooiinn() is cancelled, the
    thread identified by thread is not detached.

  RREETTUURRNN VVAALLUUEESS

    On success, the function returns 0; otherwise, an error code is returned.

  EERRRROORRSS

    The pptthhrreeaadd__jjooiinn() function can fail for the following reasons:

    [EDEADLK]
        A deadlock was detected, or thread identifies the calling thread.

    [EINVAL]
        The thread argument does not identify a joinable thread.

    [ESRCH]
        The thread argument does not identify an existing thread.

    This function does not return [EINTR].

  SSEEEE AALLSSOO

    _p_t_h_r_e_a_d___c_r_e_a_t_e(2)

    _p_t_h_r_e_a_d___e_x_i_t(2)

    _w_a_i_t(2)

  UUSSAAGGEE NNOOTTEESS

    The pptthhrreeaadd__jjooiinn function is thread safe.

    The pptthhrreeaadd__jjooiinn function is not async-signal safe.

