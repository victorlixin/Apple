setsid(2)                                                     setsid(2)

  sseettssiidd(())

  NNAAMMEE

    setsid() - create session and set process group ID

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    pid_t setsid(void)

  DDEESSCCRRIIPPTTIIOONN

    The _ss_ee_tt_ss_ii_dd(2) command creates a new session.
	The calling process is the session leader of the new session, is the
	process group leader of a new process group and has no controlling terminal.
	The calling process is the only process in either the session or the process group.

  RREETTUURRNN VVAALLUUEESS

    Upon successful completion, the _ss_ee_tt_ss_ii_dd(2) function returns the value of
    the process group ID of the new process group, which is the same as the
    process ID of the calling process.

    If an error occurs, _ss_ee_tt_ss_ii_dd(2) returns -1 and the global variable errno is
    set.

  EERRRROORRSS

    The _ss_ee_tt_ss_ii_dd(2) function can fail for the following reasons:

    [ENOMEM]
        Not enough free memory is available to create the session.

    [EPERM]
        The calling process is already a process group leader, or the process
        group ID of a process other than the calling process matches the
        process ID of the calling process.

  SSEEEE AALLSSOO

    _s_e_t_p_g_i_d(2)

    _t_c_g_e_t_p_g_r_p(2)

    _t_c_s_e_t_p_g_r_p(2)

  UUSSAAGGEE NNOOTTEESS

    The sseettssiidd function is thread safe.

    The sseettssiidd function is async-signal safe.

