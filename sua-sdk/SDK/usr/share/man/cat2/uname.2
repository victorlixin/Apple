uname(2)                                                       uname(2)

  uunnaammee(())

  NNAAMMEE

    uname() - get system identification

  SSYYNNOOPPSSIISS

    #include <sys/utsname.h>

    int uname (struct utsname *name)

  DDEESSCCRRIIPPTTIIOONN

    The _uu_nn_aa_mm_ee(2) function stores null-terminated strings of information
    identifying the current system into the structure referenced by name.

    The utsname structure is defined in the <<ssyyss//uuttssnnaammee..hh>> header file, and
    contains the following members:

    sysname
        Name of the operating system implementation.

    nodename
        Network name of this machine.

    release
        Release level of the operating system.

    version
        Version level of the operating system.

    machine
        Machine hardware platform.

  RREETTUURRNN VVAALLUUEESS

    If _uu_nn_aa_mm_ee(2) is successful, it returns 0; otherwise, it returns -1 and set
    errno to indicate the error.

  EERRRROORRSS

    The _uu_nn_aa_mm_ee(2) function detects the following errors:

    [EFAULT]
        The name argument is an invalid address.

  UUSSAAGGEE NNOOTTEESS

    The uunnaammee function is thread safe.

    The uunnaammee function is async-signal safe.

