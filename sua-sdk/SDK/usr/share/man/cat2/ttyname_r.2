ttyname_r(2)                                               ttyname_r(2)

  ttttyynnaammee(())

  NNAAMMEE

    ttyname(), ttyname_r(), isatty(), ttyslot() - get pathname of associated
    terminal (tty) from file descriptor

  SSYYNNOOPPSSIISS

    #include <unistd.h>

    char * ttyname (int fd)
    int ttyname_r (int fildes, char *name, size_t namesize)
    int isatty (int fd)
    int ttyslot (void)

  DDEESSCCRRIIPPTTIIOONN

    These functions operate on the system file descriptors for terminal type
    devices.

    The iissaattttyy(2) function determines if the file descriptor fd refers to a
    valid terminal type device (serial devices, console windows and pseudo
    ttys).

    The ttttyynnaammee(2) function gets the related device name of a file descriptor
    for which iissaattttyy(2) is true. The name has the form /dev/ttyident, where
    the ident string specifies the type of the device and identifies it.

    The reentrant ttttyynnaammee__rr(2) function stores the null-terminated pathname of
    the terminal associated with the file descriptor identified by the fildes
    argument in the character array referenced by name. The array is namesize
    characters long and must have space for the name and the terminating null
    character. The maximum length of the terminal name is {TTY_NAME_MAX}.

    The ttttyysslloott(2) function returns the index for the curent user's place in //
    vvaarr//aaddmm//uuttmmppxx file, the user accounting database. The first entry is 0;
    the record represents the user's line. (A user's line in the database is
    an entry where uuttlliinnee matches the terminal device for one of the standard
    streams.)

  RREETTUURRNN VVAALLUUEESS

    The ttttyynnaammee(2) function returns the null-terminated name if the tty device
    is found and iissaattttyy(2) is true; otherwise it returns NULL and sets errno.

    On success, the ttttyynnaammee__rr(2) function returns zero. Otherwise, it returns
    an error number to indicate the error.

    The ttttyysslloott(2) function returns the index number for the entry; on
    failure, it returns -1 if there was an error, or if none of standard
    input, output or error for the process is associated with a terminal
    device.

    The iissaattttyy(2) call returns zero if fd is not a valid terminal device and
    non-zero if it is.

  EERRRROORR

    The ttttyynnaammee(2), ttttyynnaammee__rr(2), and iissaattttyy(2) functions can fail for the
    following reasons:

    [EBADF]
        The fd argument is not a valid descriptor.

    [EIO]
        Although the fd argument refers to a valid descriptor, the terminal
        device is no longer responsive.

    [ENOTTY]
        The fd argument does not refer to a terminal device.

    In addition, the ttttyynnaammee__rr(2) function can fail for the following reason:

    [ERANGE]
        The value of namesize is smaller than the length of the string to be
        returned including the terminating null character

  FFIILLEESS

    //ddeevv//**
        The virtual device directory.

  SSEEEE AALLSSOO

    _c_t_e_r_m_i_d(3)

  UUSSAAGGEE NNOOTTEESS

    The iissaattttyy,, ttttyynnaammee__rr,, ttttyysslloott functions are thread safe. The ttttyynnaammee
    function is not thread safe.

    None of these functions are async-signal safe.

