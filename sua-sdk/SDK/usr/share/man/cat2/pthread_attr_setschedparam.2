pthread_attr_setschedparam(2)             pthread_attr_setschedparam(2)

  pptthhrreeaadd__aattttrr__ggeettsscchheeddppaarraamm(())

  NNAAMMEE

    pthread_attr_getschedparam(), pthread_attr_setschedparam() - get or set
    the thread scheduling parameters attribute

  SSYYNNOOPPSSIISS

    #include <pthread.h>

    int pthread_attr_getschedparam(const pthread_attr_t *attr, struct
    sched_param *param);
    int pthread_attr_setschedparam(pthread_attr_t *attr, const struct
    sched_param *param);

  DDEESSCCRRIIPPTTIIOONN

    The pptthhrreeaadd__aattttrr__sseettsscchheeddppaarraamm(2) sets the scheduling parameters attribute
    in the thread attributes object referenced by attr by using the values in
    the sscchheedd__ppaarraamm structure referenced by the param argument. The
    sscchheedd__ppaarraamm structure, defined in <sched.h> contains the following fields:

    struct sched_param {
        int     sched_priority; /* scheduling priority */
        int     sched_nicelim;  /* nice value limit for SCHED_OTHER policy */
        int     sched_nice;     /* nice value for SCHED_OTHER policy */
        int     sched_pad[6];   /* this is used for padding */
    };

    After the attributes have been set in the thread attributes object
    referenced by attr, the _pp_tt_hh_rr_ee_aa_dd____cc_rr_ee_aa_tt_ee(2) function can be called to create
    a new thread with the specified attributes. The effective thread priority
    of the new thread is equal to the process priority (determined from the
    _nn_ii_cc_ee value of the process) plus the thread priority that is set by this
    function. This function does not affect the current running thread.

    NNOOTTEE:: In this version of Interix, only the sscchheedd__pprriioorriittyy field of the
    sscchheedd__ppaarraamm structure is supported. Other fields are ignored.

    The pptthhrreeaadd__aattttrr__ggeettsscchheeddppaarraamm(2) function retrieves the scheduling
    parameter attributes of the thread object referenced by attr, storing the
    value in the location referenced by the param argument.

  RREETTUURRNN VVAALLUUEESS

    On success, both functions return 0; otherwise, they return an error
    number.

  EERRRROORRSS

    The pptthhrreeaadd__aattttrr__sseettsscchheeddppaarraamm() function can fail for the following
    reasons:

    [EINVAL]
        The param argument is invalid.

    [ENOTSUP]
        An attempt was made to set the scheduling parameters attribute to an
        unsupported value.

    Neither function returns [EINTR].

  SSEEEE AALLSSOO

    _p_t_h_r_e_a_d___a_t_t_r___d_e_s_t_r_o_y(2)

    _p_t_h_r_e_a_d___a_t_t_r___g_e_t_i_n_h_e_r_i_t_s_c_h_e_d(2)

    _p_t_h_r_e_a_d___a_t_t_r___g_e_t_s_c_h_e_d_p_o_l_i_c_y(2)

    _p_t_h_r_e_a_d___a_t_t_r___g_e_t_s_c_o_p_e(2)

    _p_t_h_r_e_a_d___c_r_e_a_t_e(2)

  UUSSAAGGEE NNOOTTEESS

    All of these functions are thread safe.

    None of these functions are async-signal safe.

