getpeername(2)                                           getpeername(2)

  ggeettppeeeerrnnaammee(())

  NNAAMMEE

    getpeername() - get name of connected peer

  SSYYNNOOPPSSIISS

    #include <sys/socket.h>

    int getpeername (int s, struct sockaddr *name, int *namelen)

  DDEESSCCRRIIPPTTIIOONN

    The _gg_ee_tt_pp_ee_ee_rr_nn_aa_mm_ee(2) function returns the name of the peer connected to
    socket s. The namelen parameter should be initialized to indicate the
    amount of space pointed to by name. On return it contains the actual size
    of the name returned (in bytes). The name is truncated if the buffer
    provided is too small.

  RREETTUURRNN VVAALLUUEESS

    If the call succeeds, it returns 0.

    If the call fails, it returns -1 and sets errno to indicate the error.

  EERRRROORRSS

    The call succeeds unless:

    [EBADF]
        The argument s is not a valid descriptor.

    [ENOTSOCK]
        The argument s is a file, not a socket.

    [ENOTCONN]
        The socket is not connected.

    [ENOBUFS]
        Insufficient resources were available in the system to perform the
        operation.

    [EFAULT]
        The name parameter points to memory not in a valid part of the process
        address space.

  SSEEEE AALLSSOO

    _a_c_c_e_p_t(2)

    _b_i_n_d(2)

    _s_o_c_k_e_t(2)

    _g_e_t_s_o_c_k_n_a_m_e(2)

  UUSSAAGGEE NNOOTTEESS

    The ggeettppeeeerrnnaammee function is thread safe.

    The ggeettppeeeerrnnaammee function is async-signal safe.

