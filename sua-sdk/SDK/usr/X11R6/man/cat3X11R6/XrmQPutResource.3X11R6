XrmQPutResource(3X11R6)                         XrmQPutResource(3X11R6)

  XXrrmmPPuuttRReessoouurrccee

  NNAAMMEE

    XrmPutResource, XrmQPutResource, XrmPutStringResource,
    XrmQPutStringResource, XrmPutLineResource - store database resources

  SSYYNNTTAAXX

    void XrmPutResource(database, specifier, type, value)
         XrmDatabase *database;
         char *specifier;
         char *type;
         XrmValue *value;

    void XrmQPutResource(database, bindings, quarks, type, value)
         XrmDatabase *database;
         XrmBindingList bindings;
         XrmQuarkList quarks;
         XrmRepresentation type;
         XrmValue *value;

    void XrmPutStringResource(database, specifier, value)
         XrmDatabase *database;
         char *specifier;
         char *value;

    void XrmQPutStringResource(database, bindings, quarks, value)
         XrmDatabase *database;
         XrmBindingList bindings;
         XrmQuarkList quarks;
         char *value;

    void XrmPutLineResource(database, line)
         XrmDatabase *database;
         char *line;

  AARRGGUUMMEENNTTSS

    bindings
        Specifies a list of bindings.

    database
        Specifies the resource database.

    line
        Specifies the resource name and value pair as a single string.

    quarks
        Specifies the complete or partial name or the class list of the
        resource.

    specifier
        Specifies a complete or partial specification of the resource.

    type
        Specifies the type of the resource.

    value
        Specifies the value of the resource, which is specified as a string.

  DDEESSCCRRIIPPTTIIOONN

    If database contains NULL, XXrrmmPPuuttRReessoouurrccee creates a new database and
    returns a pointer to it. XXrrmmPPuuttRReessoouurrccee is a convenience function that
    calls XXrrmmSSttrriinnggTTooBBiinnddiinnggQQuuaarrkkLLiisstt followed by:

    XrmQPutResource(database, bindings, quarks, XrmStringToQuark(type), value)

    If the specifier and type are not in the Host Portable Character Encoding,
    the result is implementation-dependent. The value is stored in the
    database without modification.

    If database contains NULL, XXrrmmQQPPuuttRReessoouurrccee creates a new database and
    returns a pointer to it. If a resource entry with the identical bindings
    and quarks already exists in the database, the previous type and value are
    replaced by the new specified type and value. The value is stored in the
    database without modification.

    If database contains NULL, XXrrmmPPuuttSSttrriinnggRReessoouurrccee creates a new database and
    returns a pointer to it. XXrrmmPPuuttSSttrriinnggRReessoouurrccee adds a resource with the
    specified value to the specified database. XXrrmmPPuuttSSttrriinnggRReessoouurrccee is a
    convenience function that first calls XXrrmmSSttrriinnggTTooBBiinnddiinnggQQuuaarrkkLLiisstt on the
    specifier and then calls XXrrmmQQPPuuttRReessoouurrccee, using a "String" representation
    type. If the specifier is not in the Host Portable Character Encoding, the
    result is implementation-dependent. The value is stored in the database
    without modification.

    If database contains NULL, XXrrmmQQPPuuttSSttrriinnggRReessoouurrccee creates a new database
    and returns a pointer to it. XXrrmmQQPPuuttSSttrriinnggRReessoouurrccee is a convenience
    routine that constructs an XXrrmmVVaalluuee for the value string (by calling
    strlen to compute the size) and then calls XXrrmmQQPPuuttRReessoouurrccee, using a
    "String" representation type. The value is stored in the database without
    modification.

    If database contains NULL, XXrrmmPPuuttLLiinneeRReessoouurrccee creates a new database and
    returns a pointer to it. XXrrmmPPuuttLLiinneeRReessoouurrccee adds a single resource entry
    to the specified database. The line should be in valid ResourceLine format
    (see section 15.1) terminated by a newline or null character; the database
    that results from using a string with incorrect syntax is implementation-
    dependent. The string is parsed in the locale of the database. If the
    ResourceName is not in the Host Portable Character Encoding, the result is
    implementation-dependent. Note that comment lines are not stored.

  SSEEEE AALLSSOO

    _X_r_m_G_e_t_R_e_s_o_u_r_c_e(3X11R6)

    _X_r_m_I_n_i_t_i_a_l_i_z_e(3X11R6)

    _X_r_m_M_e_r_g_e_D_a_t_a_b_a_s_e_s(3X11R6)

    _X_r_m_U_n_i_q_u_e_Q_u_a_r_k(3X11R6)

    Xlib - C Language X Interface

