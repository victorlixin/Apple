XtReservePropertyAtom(3X11R6)             XtReservePropertyAtom(3X11R6)

  XXttRReesseerrvveePPrrooppeerrttyyAAttoomm

  NNAAMMEE

    XtReservePropertyAtom, XtReleasePropertyAtom - maintain a cache of
    property atoms

  SSYYNNTTAAXX

    Widget XtReservePropertyAtom(w)
          Widget w;

    void XtReleasePropertyAtome(w, atom)
          Widget w;
          Atom atom;

  AARRGGUUMMEENNTTSS

    w
        Specifies the widget used to reserve the atom.

    atom
        Specifies the atom whose reservation is to be released.

  DDEESSCCRRIIPPTTIIOONN

    XXttRReesseerrvveePPrrooppeerrttyyAAttoomm returns an atom that may be used for properties in
    conjunction with conversion requests from widget w. The atom returned will
    be unique for the display of the widget specified.

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

