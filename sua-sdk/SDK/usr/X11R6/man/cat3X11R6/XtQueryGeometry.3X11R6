XtQueryGeometry(3X11R6)                         XtQueryGeometry(3X11R6)

  XXttQQuueerryyGGeeoommeettrryy

  NNAAMMEE

    XtQueryGeometry - query the preferred geometry of a child widget

  SSYYNNTTAAXX

    XtGeometryResult XtQueryGeometry(w, intended, preferred_return)
         Widget w;
         XtWidgetGeometry *intended, *preferred_return;

  AARRGGUUMMEENNTTSS

    intended
        Specifies any changes the parent plans to make to the child's geometry
        or NULL.

    preferred_return
        Returns the child widget's preferred geometry.

    w
        Specifies the widget.

  DDEESSCCRRIIPPTTIIOONN

    To discover a child's preferred geometry, the child's parent sets any
    changes that it intends to make to the child's geometry in the
    corresponding fields of the intended structure, sets the corresponding
    bits in intended.request_mode, and calls XXttQQuueerryyGGeeoommeettrryy.

    XXttQQuueerryyGGeeoommeettrryy clears all bits in the preferred_return->request_mode and
    checks the query_geometry field of the specified widget's class record. If
    query_geometry is not NULL, XXttQQuueerryyGGeeoommeettrryy calls the query_geometry
    procedure and passes as arguments the specified widget, intended, and
    preferred_return structures. If the intended argument is NULL,
    XXttQQuueerryyGGeeoommeettrryy replaces it with a pointer to an XtWidgetGeometry
    structure with request_mode=0 before calling query_geometry.

  SSEEEE AALLSSOO

    _X_t_C_o_n_f_i_g_u_r_e_W_i_d_g_e_t(3X11R6)

    _X_t_M_a_k_e_G_e_o_m_e_t_r_y_R_e_q_u_e_s_t(3X11R6)

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

