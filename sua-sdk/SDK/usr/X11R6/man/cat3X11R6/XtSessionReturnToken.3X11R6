XtSessionReturnToken(3X11R6)               XtSessionReturnToken(3X11R6)

  XXttSSeessssiioonnGGeettTTookkeenn

  NNAAMMEE

    XtSessionGetToken, XtSessionReturnToken - token management for
    checkpointing

  SSYYNNTTAAXX

    XtCheckpointToken XtSessionGetToken(w, type)
          Widget w;
          int type;

    void XtSessionReturntoken(token)
          XtCheckpointToken token;

  AARRGGUUMMEENNTTSS

    w
        Specifies the session widget.

    type
        Specifies the value in the type field of the token.

    token
        Specifies the token to be returned.

  DDEESSCCRRIIPPTTIIOONN

    The XXttSSeessssiioonnGGeettTTookkeenn function will return NULL if the checkpoint
    operation instigated by the session manager is not currently under way.

    To indicate the completion of a deferred save operation and to return the
    checkpoint token use XXttSSeessssiioonnRReettuurrnnTTookkeenn.

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

