XtRealloc(3X11R6)                                     XtRealloc(3X11R6)

  XXttMMaalllloocc

  NNAAMMEE

    XtMalloc, XtCalloc, XtRealloc, XtFree, XtNew, XtNewString - memory
    management functions

  SSYYNNTTAAXX

    char *XtMalloc(size);
          Cardinal size;

    char *XtCalloc(num, size);
          Cardinal num;
          Cardinal size;

    char *XtRealloc(ptr, num);
          char *ptr;
          Cardinal num;

    void XtFree(ptr);
          char *ptr;

    type *XtNew(type);
          type;

    String XtNewString(string);
          String string;

  AARRGGUUMMEENNTTSS

    num
        Specifies the number of bytes or array elements.

    ptr
        Specifies a pointer to the old storage or to the block of storage that
        is to be freed.

    size
        Specifies the size of an array element (in bytes) or the number of
        bytes desired.

    string
        Specifies a previously declared string.

    type
        Specifies a previously declared data type.

  DDEESSCCRRIIPPTTIIOONN

    The XXttMMaalllloocc functions returns a pointer to a block of storage of at least
    the specified size bytes. If there is insufficient memory to allocate the
    new block, XXttMMaalllloocc calls XXttEErrrroorrMMssgg.

    The XXttCCaalllloocc function allocates space for the specified number of array
    elements of the specified size and initializes the space to zero. If there
    is insufficient memory to allocate the new block, XXttCCaalllloocc calls
    XXttEErrrroorrMMssgg.

    The XXttRReeaalllloocc function changes the size of a block of storage (possibly
    moving it). Then, it copies the old contents (or as much as will fit) into
    the new block and frees the old block. If there is insufficient memory to
    allocate the new block, XXttRReeaalllloocc calls XXttEErrrroorrMMssgg. If ptr is NULL,
    XXttRReeaalllloocc allocates the new storage without copying the old contents; that
    is, it simply calls XXttMMaalllloocc.

    The XXttFFrreeee function returns storage and allows it to be reused. If ptr is
    NULL, XXttFFrreeee returns immediately.

    XXttNNeeww returns a pointer to the allocated storage. If there is insufficient
    memory to allocate the new block, XXttNNeeww calls XXttEErrrroorrMMssgg. XXttNNeeww is a
    convenience macro that calls XXttMMaalllloocc with the following arguments
    specified:

    ((type *) XtMalloc((unsigned) sizeof(type))

    XXttNNeewwSSttrriinngg returns a pointer to the allocated storage. If there is
    insufficient memory to allocate the new block, XXttNNeewwSSttrriinngg calls
    XXttEErrrroorrMMssgg. XXttNNeewwSSttrriinngg is a convenience macro that calls XXttMMaalllloocc with
    the following arguments specified:

    (strcpy(XtMalloc((unsigned) strlen(str) + 1), str))

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

