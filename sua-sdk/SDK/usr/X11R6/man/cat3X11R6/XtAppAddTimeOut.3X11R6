XtAppAddTimeOut(3X11R6)                         XtAppAddTimeOut(3X11R6)

  XXttAAppppAAddddTTiimmeeOOuutt

  NNAAMMEE

    XtAppAddTimeOut, XtRemoveTimeOut - register and remove timeouts

  SSYYNNTTAAXX

    XtIntervalId XtAppAddTimeOut(app_context, interval, proc, client_data)
          XtAppContext app_context;
          unsigned long interval;
          XtTimerCallbackProc proc;
          XtPointer client_data;

    void XtRemoveTimeOut(timer)
          XtIntervalId timer;

  AARRGGUUMMEENNTTSS

    app_context
        Specifies the application context for which the timer is to be set.

    client_data
        Specifies the argument that is to be passed to the specified procedure
        when .

    interval
        Specifies the time interval in milliseconds.

    proc
        Specifies the procedure that is to be called when time expires.

    timer
        Specifies the ID for the timeout request to be destroyed.

  DDEESSCCRRIIPPTTIIOONN

    The XXttAAppppAAddddTTiimmeeOOuutt function creates a timeout and returns an identifier
    for it. The timeout value is set to interval. The callback procedure is
    called when the time interval elapses, and then the timeout is removed.

    The XXttRReemmoovveeTTiimmeeOOuutt function removes the timeout. Note that timeouts are
    automatically removed once they trigger.

  SSEEEE AALLSSOO

    _X_t_A_p_p_A_d_d_I_n_p_u_t(3X11R6)

    _X_t_A_p_p_A_d_d_S_i_g_n_a_l(3X11R6)

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

