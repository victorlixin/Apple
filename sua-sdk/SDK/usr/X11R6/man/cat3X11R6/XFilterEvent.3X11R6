XFilterEvent(3X11R6)                               XFilterEvent(3X11R6)

  XXFFiilltteerrEEvveenntt

  NNAAMMEE

    XFilterEvent - filter X events for an input method

  SSYYNNTTAAXX

    Bool XFilterEvent(event, w)
          XEvent *event;
          Window w;

  AARRGGUUMMEENNTTSS

    event
        Specifies the event to filter.

    w
        Specifies the window for which the filter is to be applied.

  DDEESSCCRRIIPPTTIIOONN

    If the window argument is None, XXFFiilltteerrEEvveenntt applies the filter to the
    window specified in the XXEEvveenntt structure. The window argument is provided
    so that layers above Xlib that do event redirection can indicate to which
    window an event has been redirected.

    If XXFFiilltteerrEEvveenntt returns True, then some input method has filtered the
    event, and the client should discard the event. If XXFFiilltteerrEEvveenntt returns
    False, then the client should continue processing the event.

    If a grab has occurred in the client and XXFFiilltteerrEEvveenntt returns True, the
    client should ungrab the keyboard.

  SSEEEE AALLSSOO

    _X_N_e_x_t_E_v_e_n_t(3X11R6)

    Xlib - C Language X Interface

