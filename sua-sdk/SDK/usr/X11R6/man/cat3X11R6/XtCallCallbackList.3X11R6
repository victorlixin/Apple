XtCallCallbackList(3X11R6)                   XtCallCallbackList(3X11R6)

  XXttCCaallllCCaallllbbaacckkss

  NNAAMMEE

    XtCallCallbacks, XtCallCallbackList, XtHasCallbacks - process callbacks

  SSYYNNTTAAXX

    void XtCallCallbacks(w, callback_name, call_data)
          Widget w;
          String callback_name;
          XtPointer call_data;

    void XtCallCallbackList(w, callbacks, call_data)
          Widget w;
          XtCallbackList callbacks;
          XtPointer call_data;

    typedef enum {XtCallbackNoList, XtCallbackHasNone, XtCallbackHasSome}
    XtCallbackStatus;
    XtCallbackStatus XtHasCallbacks(w, callback_name)
          Widget w;
          String callback_name;

  AARRGGUUMMEENNTTSS

    callback_name
        Specifies the callback list to be executed or checked.

    call_data
        Specifies a callback-list specific data value to pass to each of the
        callback procedure in the list.

    callbacks
        Specifies the callback list to be executed.

    w
        Specifies the widget.

  DDEESSCCRRIIPPTTIIOONN

    The XXttCCaallllCCaallllbbaacckkss function calls each procedure that is registered in
    the specified widget's callback list.

    If callbacks is not NULL, XXttCCaallllCCaallllbbaacckkLLiisstt calls each of the callback
    procedures in the list, passing client data and call_data. The callbacks
    parameter must specify the contents of a widget or object resource
    declared with representation type XtRCallback.

    The XXttHHaassCCaallllbbaacckkss function first checks to see if the widget has a
    callback list identified by callback_name. If the callback list does not
    exist, XXttHHaassCCaallllbbaacckkss returns XtCallbackNoList. If the callback list
    exists but is empty, it returns XtCallbackHasNone. If the callback list
    exists and has at least one callback registered, it returns
    XtCallbackHasSome.

  SSEEEE AALLSSOO

    _X_t_A_d_d_C_a_l_l_b_a_c_k(3X11R6)

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

