XtConfigureWidget(3X11R6)                     XtConfigureWidget(3X11R6)

  XXttCCoonnffiigguurreeWWiiddggeett

  NNAAMMEE

    XtConfigureWidget, XtMoveWidget, XtResizeWidget - move and resize widgets

  SSYYNNTTAAXX

    void XtConfigureWidget(w, x, y, width, height, border_width)
          Widget w;
          Position x;
          Position y;
          Dimension width;
          Dimension height;
          Dimension border_width;

    void XtMoveWidget(w, x, y)
          Widget w;
          Position x;
          Position y;

    void XtResizeWidget(w, width, height, border_width)
          Widget w;
          Dimension width;
          Dimension height;
          Dimension border_width;

    void XtResizeWindow(w)
          Widget w;

  AARRGGUUMMEENNTTSS
    width
    height
    border_width
        Specify the new widget size.

    w
        Specifies the widget.
    x
    y
        Specify the new widget x and y coordinates.

  DDEESSCCRRIIPPTTIIOONN

    The XXttCCoonnffiigguurreeWWiiddggeett function returns immediately if the specified
    geometry fields are the same as the old values. Otherwise,
    XXttCCoonnffiigguurreeWWiiddggeett writes the new x, y, width, height, and border_width
    values into the widget and, if the widget is realized, makes an Xlib
    XXCCoonnffiigguurreeWWiinnddooww call on the widget's window.

    If either the new width or height is different from its old value,
    XXttCCoonnffiigguurreeWWiiddggeett calls the widget's resize procedure to notify it of the
    size change; otherwise, it simply returns.

    The XXttMMoovveeWWiiddggeett function returns immediately if the specified geometry
    fields are the same as the old values. Otherwise, XXttMMoovveeWWiiddggeett writes the
    new x and y values into the widget and, if the widget is realized, issues
    an Xlib XXMMoovveeWWiinnddooww call on the widget's window.

    The XXttRReessiizzeeWWiiddggeett function returns immediately if the specified geometry
    fields are the same as the old values. Otherwise, XXttRReessiizzeeWWiiddggeett writes
    the new width, height, and border_width values into the widget and, if the
    widget is realized, issues an XXCCoonnffiigguurreeWWiinnddooww call on the widget's
    window.

    If the new width or height are different from the old values,
    XXttRReessiizzeeWWiiddggeett calls the widget's resize procedure to notify it of the
    size change.

    The XtResizeWindow function calls the XXCCoonnffiigguurreeWWiinnddooww Xlib function to
    make the window of the specified widget match its width, height, and
    border width. This request is done unconditionally because there is no way
    to tell if these values match the current values. Note that the widget's
    resize procedure is not called.

    There are very few times to use XtResizeWindow; instead, you should use
    XXttRReessiizzeeWWiiddggeett.

  SSEEEE AALLSSOO

    _X_t_M_a_k_e_G_e_o_m_e_t_r_y_R_e_q_u_e_s_t(3X11R6)

    _X_t_Q_u_e_r_y_G_e_o_m_e_t_r_y(3X11R6)

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

