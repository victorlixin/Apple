XGrabServer(3X11R6)                                 XGrabServer(3X11R6)

  XXGGrraabbSSeerrvveerr

  NNAAMMEE

    XGrabServer, XUngrabServer - grab the server

  SSYYNNTTAAXX

    XGrabServer(display)
          Display *display;

    XUngrabServer(display)
          Display *display;

  AARRGGUUMMEENNTTSS

    display
        Specifies the connection to the X server.

  DDEESSCCRRIIPPTTIIOONN

    The XXGGrraabbSSeerrvveerr function disables processing of requests and close downs
    on all other connections than the one this request arrived on. You should
    not grab the X server any more than is absolutely necessary.

    The XXUUnnggrraabbSSeerrvveerr function restarts processing of requests and close downs
    on other connections. You should avoid grabbing the X server as much as
    possible.

  SSEEEE AALLSSOO

    _X_G_r_a_b_B_u_t_t_o_n(3X11R6)

    _X_G_r_a_b_K_e_y(3X11R6)

    _X_G_r_a_b_K_e_y_b_o_a_r_d(3X11R6)

    _X_G_r_a_b_P_o_i_n_t_e_r(3X11R6)

    Xlib - C Language X Interface

