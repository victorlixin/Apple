XmbResetIC(3X11R6)                                   XmbResetIC(3X11R6)

  XXmmbbRReesseettIICC

  NNAAMMEE

    XmbResetIC, XwcResetIC - reset the state of an input context

  SSYYNNTTAAXX

    char * XmbResetIC(ic)
          XIC ic;

    wchar_t * XwcResetIC(ic)
          XIC ic; 

  AARRGGUUMMEENNTTSS

    ic
        Specifies the input context.

  DDEESSCCRRIIPPTTIIOONN

    When XNResetState is set to XIMInitialState, XXmmbbRReesseettIICC and XXwwccRReesseettIICC
    reset an input context to its initial state; when XNResetState is set to
    XIMPreserveState, the current input context state is preserved. In both
    cases, any input pending on that context is deleted. The input method is
    required to clear the preedit area, if any, and update the status
    accordingly. Calling XXmmbbRReesseettIICC or XXwwccRReesseettIICC does not change the focus.

    The return value of XXmmbbRReesseettIICC is its current preedit string as a
    multibyte string. If there is any preedit text drawn or visible to the
    user, then these procedures must return a non-NULL string. If there is no
    visible preedit text, then it is input method implementation-dependent
    whether these procedures return a non-NULL string or NULL.

    The client should free the returned string by calling XXFFrreeee.

  SSEEEE AALLSSOO

    _X_C_r_e_a_t_e_I_C(3X11R6)

    _X_O_p_e_n_I_M(3X11R6)

    _X_S_e_t_I_C_F_o_c_u_s(3X11R6)

    _X_S_e_t_I_C_V_a_l_u_e_s(3X11R6)

    Xlib - C Language X Interface

