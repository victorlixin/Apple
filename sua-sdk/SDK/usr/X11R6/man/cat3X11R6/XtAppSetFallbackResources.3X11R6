XtAppSetFallbackResources(3X11R6)        XtAppSetFallbackResources(3X11R6)

  XXttAAppppSSeettFFaallllbbaacckkRReessoouurrcceess

  NNAAMMEE

    XtAppSetFallbackResources - set fallback resources

  SSYYNNTTAAXX

    void XtAppSetFallbackResources(app_context, specification_list)
          XtAppContext app_context;
          String* specification_list;

  AARRGGUUMMEENNTTSS

    app_context
        Specifies the application context.

    specification_list
        Specifies a NULL-terminated list of resource specifications to pre-
        load the database, or NULL.

  DDEESSCCRRIIPPTTIIOONN

    Each entry in specification_list points to a string in the format
    XXrrmmPPuuttLLiinneeRReessoouurrccee. following a call to XXttAAppppSSeettFFaallllbbaacckkRReessoouurrcceess, when a
    resource database is being created for a particular screen and the
    Intrinsics are not able to find or read an application-specific class
    resource file and if specification_list is not NULL the resource
    specification in specification_list will be merged into the screen
    resource database in place of the application-specific class resource
    file. XXttAAppppSSeettFFaallllbbaacckkRReessoouurrcceess is not required to copy
    specification_list; the caller must ensure that the contents of the list
    and the strings addressed by the list remain valid until all displys are
    initialized or until XXttAAppppSSeettFFaallllbbaacckkRReessoouurrcceess is called again. The value
    NULL for specification_list removes any previous fallback resource
    specification for the application context. The intended use for fallback
    resources is to provide a minimal number of resources that will amke the
    application usable (or at least terminate with helful diagnostic messages)
    when some problem exists in finding and loading the application defaults
    file.

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

