XtGetActionKeysym(3X11R6)                     XtGetActionKeysym(3X11R6)

  XXttGGeettAAccttiioonnKKeeyyssyymm

  NNAAMMEE

    XtGetActionKeysym - obtain corresponding keysym

  SSYYNNTTAAXX

    KeySym XtGetActionKeysym(event, modifiers_return)
          XEvent* event;
          Modifiers* modifiers_return;

  AARRGGUUMMEENNTTSS

    event
        Specifies the event pointer passed to the action procedure by the
        Intrinsics.

    modifiers_return
        Returns the modifiers that caused the match, if non-NULL.

  DDEESSCCRRIIPPTTIIOONN

    If XXttGGeettAAccttiioonnKKeeyyssyymm is called after an action procedure has been invoked
    by the Intrinsics and before that action procedure returns, and if the
    event pointer has the same value as the event pointer passed to that
    action routine, and if the event is a KeyPress or KeyRelease event, then
    XXttGGeettAAccttiioonnKKeeyyssyymm returns the KeySym that matched the final event
    specification in the translation table and, if /fImodifiers_return/fP is
    non-NULL, the modifier state actually used to generate this KeySym;
    otherwise, if the event is a KeyPress or KeyRelease event, then
    XXttGGeettAAccttiioonnKKeeyyssyymm calls XXttTTrraannssllaatteeKKeeyyccooddee and returns the results; else
    it returns NoSymbol and does not examine modifiers_return.

  SSEEEE AALLSSOO

    X Toolkit Intrinsics - C Language Interface
    Xlib - C Language X Interface

